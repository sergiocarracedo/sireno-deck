---
wave: 3
depends_on:
  - 26-01-PLAN.md
  - 26-02-PLAN.md
files_modified:
  - packages/cli/src/render/startup-placeholder.ts
  - packages/cli/src/cli/commands/start.ts
  - packages/cli/src/cli/commands/start.test.ts
  - assets/logoFull.png
autonomous: true
single_layer_justified: false
objective: "Startup loading stays on the existing pre-browser placeholder seam, but uses `assets/logoFull.png` in a simple non-React loading card or mosaic treatment that remains honest before the browser renderer is ready."
must_haves:
  truths:
    - "The startup loading state still works before the browser page exists and does not depend on the new shared React document."
    - "`packages/cli/src/render/startup-placeholder.ts` uses `assets/logoFull.png` for the new loading treatment instead of the repeated `SIRENO / STARTING` tile."
    - "The placeholder remains temporary and still hands off cleanly to the first real browser-backed deck render or honest failure path."
    - "Focused startup tests prove the new placeholder contract on the existing hardware/startup seam rather than through browser-only document assertions."
  artifacts:
    - packages/cli/src/render/startup-placeholder.ts
    - packages/cli/src/cli/commands/start.ts
    - packages/cli/src/cli/commands/start.test.ts
    - assets/logoFull.png
  key_links:
    - "packages/cli/src/render/startup-placeholder.ts remains the pre-browser loading seam."
    - "packages/cli/src/cli/commands/start.ts continues to own placeholder write-before-first-render and handoff behavior."
---

# Plan 26-03: Pre-Browser Logo Loading Card

<objective>
Keep startup loading on the existing hardware/pre-browser seam, but replace the repeated branded placeholder tile with a simple `assets/logoFull.png`-backed loading treatment that still appears before the browser is ready and still clears on the first real render.
</objective>

## Tasks

<task id="26-03-01">
<title>Replace the repeated startup tile with a simple logo-backed pre-browser loading treatment</title>
<files>
- packages/cli/src/render/startup-placeholder.ts
- assets/logoFull.png
</files>
<action>
Refactor `packages/cli/src/render/startup-placeholder.ts` so the startup placeholder no longer generates one repeated `SIRENO / STARTING` SVG tile for every key. Use `assets/logoFull.png` to produce a simple loading card or mosaic treatment that still works entirely on the pre-browser buffer-rendering seam. Keep this slice honest and simple: do not move startup rendering into React, do not depend on the browser page being available first, and preserve per-key buffer output for the hardware path.
</action>
<verify>
`grep -n "logoFull\|createStartupPlaceholderBuffers\|sharp" packages/cli/src/render/startup-placeholder.ts` shows the new logo-backed placeholder path stays on the existing buffer-rendering seam.
</verify>
<done>
The startup placeholder uses the new logo-backed loading treatment while staying browser-independent and pre-browser.
</done>
</task>

<task id="26-03-02">
<title>Prove the new placeholder still hands off cleanly through the startup/runtime seam</title>
<files>
- packages/cli/src/cli/commands/start.ts
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Update the focused startup-path tests so they still prove the placeholder is written before the first real browser-backed render, then cleared or superseded by the first honest runtime-owned deck surface or failure path. Keep the assertions on the startup seam itself rather than browser-document markup, since the user explicitly chose a simple non-React loading treatment. If helper seams or placeholder wiring need small adjustments in `start.ts` to make the new placeholder testable, keep them narrow and subordinate to the handoff contract.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts -t "writes placeholder buffers before the first real render|clears the placeholder if the first real render fails"`.
</verify>
<done>
The repo has focused proof that the new logo-backed startup loading state still obeys the original pre-browser handoff contract.
</done>
</task>

<must_haves>
- Startup loading remains outside the browser-only React page and must still work before the browser renderer exists.
- This slice changes the visual/content contract of the placeholder, not the ownership boundary of the startup seam.
- The placeholder must remain obviously temporary and must not mask browser startup failures.
</must_haves>
