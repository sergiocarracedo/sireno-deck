---
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/index.ts
  - packages/cli/src/render/jsx.ts
  - packages/cli/package.json
  - packages/cli/tsdown.config.ts
autonomous: true
single_layer_justified: false
objective: "The built `sireno-deck-cli` package emits the exact public root and `./jsx` entrypoints promised by `package.json`, with root imports exposing the helper authoring API through narrow public facades rather than internal source paths."
must_haves:
  truths:
    - "`pnpm --filter sireno-deck-cli build` emits the files referenced by `packages/cli/package.json#exports` for `.` and `./jsx`."
    - "Helper-based authoring imports resolve from `sireno-deck-cli` rather than repo-shaped internal modules."
    - "The fix uses explicit public facades and intentional build entries instead of redefining exports around incidental internal build layout."
  artifacts:
    - packages/cli/src/index.ts
    - packages/cli/src/render/jsx.ts
    - packages/cli/package.json
    - packages/cli/tsdown.config.ts
  key_links:
    - "packages/cli/src/index.ts re-exports the helper constructors already owned by `src/render/reconciler.ts`"
    - "packages/cli/tsdown.config.ts intentionally builds the public package surface promised by `packages/cli/package.json`"
    - "packages/cli/package.json points at files the build now really emits"
---

# Plan 10-01: Emit The Real Public Authoring Surface

<objective>
Close the core release blocker first by making the packaged CLI emit the public addon-authoring files it already claims to expose. A completed plan should leave the build output and package exports aligned, while keeping the public API narrow: helper constructors from `sireno-deck-cli`, explicit JSX opt-in from `sireno-deck-cli/jsx`, and no dependency on leaked internal source layout.
</objective>

## Tasks

<task id="10-01-01">
<title>Add a narrow root public facade for helper-based authoring</title>
<files>
- packages/cli/src/index.ts
- packages/cli/package.json
</files>
<action>
Add one dedicated root public entrypoint for the helper-based addon authoring API. Re-export only the helper constructors and any directly required public authoring types from the existing render contract, keeping the facade narrow instead of exposing internal render modules wholesale. Update `packages/cli/package.json` only as needed so the root export clearly maps to this intentional public entrypoint and does not rely on incidental internal file layout.
</action>
<verify>
Run `grep -n '"exports"\|"\."\|"./jsx"' packages/cli/package.json && grep -n 'createDeckButtonElement\|createDeckSurfaceElement\|createDeckTextElement' packages/cli/src/index.ts`.
</verify>
<done>
Addon authors have one real root import path for helper-based authoring, and the package contract reflects that path intentionally.
</done>
</task>

<task id="10-01-02">
<title>Make the build emit the documented root and JSX export files</title>
<files>
- packages/cli/src/render/jsx.ts
- packages/cli/package.json
- packages/cli/tsdown.config.ts
</files>
<action>
Update the build wiring so `pnpm --filter sireno-deck-cli build` emits the exact files promised by `packages/cli/package.json` for both the root export and the explicit `./jsx` opt-in subpath. Prefer explicit public build entries or equivalent facade wiring over changing the package contract to match the current nested internal output. Keep `src/render/jsx.ts` as the explicit runtime stub for JSX opt-in, but ensure both its runtime file and declaration file land at the documented `dist/render/jsx.*` paths.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli build` and then `test -f packages/cli/dist/index.js && test -f packages/cli/dist/index.d.ts && test -f packages/cli/dist/render/jsx.js && test -f packages/cli/dist/render/jsx.d.ts`.
</verify>
<done>
The built package now emits the public root and JSX files that the release-facing exports advertise, removing the direct package/build mismatch.
</done>
</task>

<must_haves>
- `UIW-01` remains shippable through the built `sireno-deck-cli/jsx` entrypoint.
- `UIW-02` becomes shippable from the package root instead of repo-local internals.
- The deliverable is release-facing: artifact paths in `dist/` match the package contract exactly after build.
</must_haves>
