---
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/config/theme.ts
  - packages/cli/src/config/theme.test.ts
  - packages/cli/src/cli/commands/start.test.ts
  - packages/cli/src/themes/default/manifest.yml
  - packages/cli/src/themes/default/index.ts
  - packages/cli/src/themes/default/ButtonFrame.tsx
autonomous: true
single_layer_justified: false
objective: "The shipped default theme loads its `buttonFrame` through the real manifest-backed TypeScript/TSX runtime path, and theme reload/watch coverage truthfully reflects the `index.ts` plus `ButtonFrame.tsx` file graph instead of pretending the built-in proof is still `.js` only."
must_haves:
  truths:
    - "`packages/cli/src/config/theme.ts` honestly loads manifest-backed `.ts` / `.tsx` theme runtime entries through one fixed-policy runtime seam while preserving tolerant `buttonFrame` export lookup."
    - "Resolving the built-in default theme by name returns the real `index.ts` / `ButtonFrame.tsx` runtime graph in `filePaths`, so watch/review surfaces match the shipped theme contract."
    - "A second theme resolution still sees updated runtime exports, proving fresh reload behavior did not regress when the import path switched away from plain `import()`."
    - "Focused theme/startup tests pin the built-in TypeScript proof path without regressing JS-authored themes."
  artifacts:
    - packages/cli/src/config/theme.ts
    - packages/cli/src/config/theme.test.ts
    - packages/cli/src/cli/commands/start.test.ts
  key_links:
    - "packages/cli/src/config/theme.ts remains the single manifest.main contract seam for theme runtime loading."
    - "packages/cli/src/config/theme.test.ts and packages/cli/src/cli/commands/start.test.ts prove the shipped default-theme graph is watched and reloadable through the real resolver path."
---

# Plan 25-01: Make The Built-In TSX Theme Contract Real

<objective>
Make the shipped default theme truthful end to end: `manifest.main` can point at `index.ts`, that entry can import `ButtonFrame.tsx`, the resolver loads it through the real theme runtime seam, and the repo's tests/watch expectations stop lying about the built-in proof theme being `.js` only.
</objective>

## Tasks

<task id="25-01-01">
<title>Switch the theme runtime import seam onto honest TypeScript/TSX loading</title>
<files>
- packages/cli/src/config/theme.ts
</files>
<action>
Refactor `packages/cli/src/config/theme.ts` so `importThemeButtonFrame(...)` keeps `manifest.main` as the only runtime entrypoint and still accepts the tolerant export shapes from Phase 25 context, but honestly supports `.js`, `.jsx`, `.ts`, and `.tsx` entries through one fixed-policy runtime import seam. Reuse the narrow raw-source philosophy already present for addons instead of inventing a second transpile system: preserve fresh-load behavior across repeated `resolveTheme()` calls, keep the manifest-backed snapshot/import discipline or an equivalent cache-busting strategy, and do not broaden the contract into tsconfig-aware project loading or a second theme entrypoint.
</action>
<verify>
`grep -n "tsx/esm/api\|buttonFrame\|ButtonFrame\|manifest.main" packages/cli/src/config/theme.ts` shows the fixed-policy import seam and the preserved tolerant export contract in the single theme resolver module.
</verify>
<done>
The theme resolver can honestly load the real built-in `index.ts` plus `ButtonFrame.tsx` runtime path without changing the public theme API shape.
</done>
</task>

<must_haves>
- Phase 25 is post-roadmap follow-on work and does not claim a new v1.2 requirement ID; this slice makes the shipped theme runtime contract truthful without inventing a broader theme plugin system.
- `manifest.main` remains the only supported runtime entrypoint and continues to tolerate `buttonFrame`, `ButtonFrame`, `default.buttonFrame`, and `default.ButtonFrame` exports.
- Built-in default-theme proof coverage is part of the slice, not cleanup, because the repo already ships `index.ts` plus `ButtonFrame.tsx`.
</must_haves>

<task id="25-01-02">
<title>Update focused tests and startup mocks to assert the real built-in theme graph</title>
<files>
- packages/cli/src/config/theme.test.ts
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Revise the focused theme/startup test coverage so the built-in default theme is asserted through its real shipped graph rather than stale `.js` expectations. Keep the tests narrow: prove built-in theme resolution returns `themes/default/index.ts` and `themes/default/ButtonFrame.tsx` in `filePaths`, prove a second load sees updated exports so reload behavior stays fresh, and keep at least one JS-authored theme path green so widening to TSX support does not become a hidden JS regression. Update only the startup mocks/expectations that are downstream of `resolveTheme()` file-path truthfulness.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/config/theme.test.ts src/cli/commands/start.test.ts`.
</verify>
<done>
The repo has honest, reviewable proof that the shipped default theme really loads as TypeScript/TSX and that watched file paths match what ships.
</done>
</task>
