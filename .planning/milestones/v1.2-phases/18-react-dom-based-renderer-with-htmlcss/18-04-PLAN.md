---
wave: 4
depends_on:
  - 18-03-PLAN.md
files_modified:
  - packages/cli/src/render/browser-renderer.ts
  - packages/cli/src/render/browser-renderer.test.ts
  - packages/cli/src/render/dom-host.tsx
  - packages/cli/src/render/dom-host.test.tsx
  - packages/cli/src/builtin-addons/core-buttons/index.ts
  - packages/cli/src/builtin-addons/core-buttons/index.test.ts
  - packages/cli/fixtures/phase-18/config.media-sampling.yml
  - .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-UAT.md
  - .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-VERIFICATION.md
autonomous: true
single_layer_justified: false
objective: "A reviewer can run a real-device Phase 18 fixture that shows bounded media sampling through the browser-only TSX surface and verify that the legacy SVG renderer path is fully gone from the shipped product."
must_haves:
  truths:
    - "The browser renderer supports bounded sampled media capture for TSX-authored buttons without promising continuous playback"
    - "A committed Phase 18 media fixture makes sampled rich-media behavior reviewable on the real device"
    - "Phase 18 UAT and verification artifacts explicitly confirm the old SVG renderer path and legacy render contract are gone from the shipped product"
  artifacts:
    - packages/cli/fixtures/phase-18/config.media-sampling.yml
    - .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-UAT.md
    - .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-VERIFICATION.md
  key_links:
    - "packages/cli/src/render/browser-renderer.ts exposes bounded sampled media capture behavior"
    - ".planning/phases/18-react-dom-based-renderer-with-htmlcss/18-VERIFICATION.md maps the removal of text-image and legacy render seams to shipped code and fixtures"
---

# Plan 18-04: Media Sampling and Final Browser-Only Verification

<objective>
Finish Phase 18 with the richer-media proof that motivated the browser renderer and the explicit evidence that the old renderer is gone. This final plan adds bounded media sampling on the browser-only TSX surface, writes the final UAT flow, and produces the verification artifact that reviewers will use to judge the cleanup honestly.
</objective>

## Tasks

<task id="18-04-01">
<title>Add bounded sampled media capture for TSX-authored browser surfaces</title>
<files>
- packages/cli/src/render/browser-renderer.ts
- packages/cli/src/render/browser-renderer.test.ts
- packages/cli/src/render/dom-host.tsx
- packages/cli/src/render/dom-host.test.tsx
</files>
<action>
Extend the browser renderer and DOM host so a TSX-authored button can declare media-oriented config and participate in bounded screenshot sampling instead of static first-frame rendering. Keep the behavior honest: sampled snapshots only, no promise of continuous playback. Add focused renderer and host tests that prove media sampling obeys the bounded queue/coalescing rules on the browser-only surface.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/browser-renderer.test.ts src/render/dom-host.test.tsx`.
</verify>
<done>
The browser-only renderer can express sampled media buttons and prove that captures stay bounded.
</done>
</task>

<task id="18-04-02">
<title>Create the real-device media fixture and final Phase 18 UAT flow</title>
<files>
- packages/cli/fixtures/phase-18/config.media-sampling.yml
- .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-UAT.md
</files>
<action>
Create the Phase 18 media fixture that reviewers can run on the real device to inspect sampled GIF/video-style behavior through the browser-only renderer. Update `18-UAT.md` so it covers the first browser-rendered deck, live browser-rendered buttons, runtime-owned system surfaces, and the new media-sampling review, with explicit pass/fail checks that the browser path is the only shipped renderer.
</action>
<verify>
Run `grep -n "media\|video\|sampling\|browser" .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-UAT.md && ls packages/cli/fixtures/phase-18/config.media-sampling.yml`.
</verify>
<done>
Phase 18 has a real-device UAT script and fixture for bounded rich-media browser rendering with no legacy fallback caveat.
</done>
</task>

<task id="18-04-03">
<title>Write the final verification artifact for the browser-only renderer replacement</title>
<files>
- .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-VERIFICATION.md
- packages/cli/src/builtin-addons/core-buttons/index.ts
- packages/cli/src/builtin-addons/core-buttons/index.test.ts
</files>
<action>
Write the final Phase 18 verification artifact mapping the phase must-haves to the browser renderer, TSX authoring contract, `buttonFrame`, runtime-owned browser surfaces, and bounded media-sampling review path. Ensure the shipped built-in surface and its tests prove the old SVG/text-image path and legacy render contract have been fully removed, not just demoted.
</action>
<verify>
Run `grep -n "buttonFrame\|browser\|media\|react-dom\|text-image\|legacy" .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-VERIFICATION.md && pnpm --filter sireno-deck-cli exec vitest run src/builtin-addons/core-buttons/index.test.ts`.
</verify>
<done>
Phase 18 has a verification artifact that can be used by `verify-work` to judge the full browser-only renderer replacement honestly.
</done>
</task>
