---
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/render/startup-placeholder.ts
  - packages/cli/src/render/startup-placeholder.test.ts
autonomous: false
single_layer_justified: true
objective: "The startup placeholder logo fills 100% of the device surface using sharp `fit: 'contain'` at full canvas dimensions, with the logo's own border color (`#efe3e1`) as the solid background, replacing the previous centered 88%×36% layout on `#0f1720`."
---

## Tasks

### 38-01-01: Update resize to full canvas dimensions

- **file**: `packages/cli/src/render/startup-placeholder.ts`
- **action**: |
  In `createStartupPlaceholderDeckBuffer`, change the logo resize call from:
  ```ts
  .resize({
    fit: "contain",
    height: Math.max(40, Math.round(height * 0.36)),
    width: Math.max(72, Math.round(width * 0.88)),
  })
  ```
  to use the full canvas dimensions:
  ```ts
  .resize({
    fit: "contain",
    height,
    width,
  })
  ```
  This removes the `Math.max(72, ...)` / `Math.max(40, ...)` minimum clamps and the `0.36`/`0.88` percentage factors. The logo will now scale to fill whichever canvas dimension it hits first while preserving aspect ratio.
- **verify**: `node -e "const m=require('fs').readFileSync('packages/cli/src/render/startup-placeholder.ts','utf8');console.log(m.includes('Math.max(72') ? 'CLAMPS_STILL_PRESENT' : 'CLAMPS_REMOVED');m.includes('height: height')?console.log('100%_HEIGHT'):console.log('HEIGHT_UNEXPECTED')"`
- **done**: When the resize call uses `height` and `width` directly (full canvas), and no `Math.max(...)` or percentage multiplication remains in the resize options.

### 38-01-02: Update background color

- **file**: `packages/cli/src/render/startup-placeholder.ts`
- **action**: |
  Replace `STREAM_DECK_KEY_PRESET.background` in the `sharp({ create: { background: ... } })` call with the hardcoded hex string `"#efe3e1"`.

  Import note: keep the `STREAM_DECK_KEY_PRESET` import if it's still needed for `keyWidth`/`keyHeight` extraction in `createStartupPlaceholderBuffers`. If it's only used for `background`, remove the import and destructure `{ keyHeight, keyWidth }` directly from the preset object inline or keep the import — up to discretion.
- **verify**: `node -e "const m=require('fs').readFileSync('packages/cli/src/render/startup-placeholder.ts','utf8');console.log(m.includes('efe3e1')?'BG_UPDATED':'BG_NOT_UPDATED')"`
- **done**: When `"#efe3e1"` is used as the background color in the `sharp({ create: ... })` call instead of `STREAM_DECK_KEY_PRESET.background`.

### 38-01-03: Update test expectations

- **file**: `packages/cli/src/render/startup-placeholder.test.ts`
- **action**: |
  Update the startup placeholder test to reflect the new background color. The test currently checks that the background matches `STREAM_DECK_KEY_PRESET.background`. Change the expected color to `"#efe3e1"`. Also verify the logo still spans multiple keys as a deck-wide treatment (not identical per-key buffers), which is the existing assertion pattern.
- **verify**: `npx vitest run packages/cli/src/render/startup-placeholder.test.ts --reporter=verbose 2>&1`
- **done**: When all placeholder tests pass with the new background color and the deck-wide logo treatment is still verified.

## Must Haves

- [ ] The startup placeholder background is `#efe3e1` (light warm beige) instead of `#0f1720` (dark navy)
- [ ] The logo fills to the canvas edge on one axis with `fit: "contain"` — no more centered small logo treatment
- [ ] No `Math.max(72, ...)` / `Math.max(40, ...)` minimum clamps remain in the resize call
- [ ] Deck-wide logo treatment is preserved (different keys have different content, not repeated tiles)
- [ ] All existing tests pass
