---
wave: 2
depends_on:
  - 18-01-PLAN.md
files_modified:
  - packages/cli/src/render/reconciler.ts
  - packages/cli/src/render/reconciler.test.tsx
  - packages/cli/src/render/types.ts
  - packages/cli/src/addon/api.ts
  - packages/cli/src/builtin-addons/core-buttons/index.test.ts
  - packages/cli/src/builtin-addons/date-time/index.ts
  - packages/cli/src/builtin-addons/date-time/index.test.ts
  - packages/cli/src/core/schemas.ts
  - packages/cli/src/config/loader.test.ts
  - packages/cli/fixtures/phase-18/config.dom-frame-defaults.yml
autonomous: true
single_layer_justified: false
objective: "Additional shipped buttons beyond the first tracer bullet use normal React TSX through the browser/react-dom path, and the repo no longer treats the old DeckButtonProps/custom reconciler shape as the primary shipped authoring model."
must_haves:
  truths:
    - "The old DeckButtonProps/reconciler contract is demoted or rewritten so the shipped Phase 18 button surface is centered on TSX authoring through react-dom"
    - "Additional shipped buttons beyond action/change-deck render through the new browser/react-dom contract"
    - "A committed fixture demonstrates default buttonFrame behavior on the broader migrated builtin path"
  artifacts:
    - packages/cli/fixtures/phase-18/config.dom-frame-defaults.yml
  key_links:
    - "packages/cli/src/render/reconciler.ts and packages/cli/src/render/types.ts no longer define the primary shipped button surface around SVG-era DeckButtonProps"
    - "packages/cli/src/builtin-addons/date-time/index.ts renders through normal TSX authoring on the browser/react-dom path"
---

# Plan 18-02: Broaden the Hard-Switch TSX Contract

<objective>
Broaden the first hard-switch tracer bullet into the actual shipped contract. This plan demotes the old `DeckButtonProps` shape from the center of the button authoring path, migrates additional builtin buttons beyond action/change-deck to normal TSX authoring, and adds fixture/test proof that default `buttonFrame` behavior now applies across the broader browser-backed surface.
</objective>

## Tasks

<task id="18-02-01">
<title>Demote the old SVG-era render description seam</title>
<files>
- packages/cli/src/render/reconciler.ts
- packages/cli/src/render/reconciler.test.tsx
- packages/cli/src/render/types.ts
- packages/cli/src/addon/api.ts
- packages/cli/src/core/schemas.ts
- packages/cli/src/config/loader.test.ts
</files>
<action>
Rewrite or demote the old `DeckButtonProps` / custom reconciler description seam so it is no longer treated as the primary shipped authoring model for buttons. Update the surrounding tests and schema/loader coverage so the hard switch is reflected in the core contract instead of leaving the SVG-era prop model as a shadow primary API. Keep only the minimum compatibility needed for in-flight migration inside the phase; do not preserve the old helper path as the intended public surface.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/reconciler.test.tsx src/config/loader.test.ts`.
</verify>
<done>
The repo no longer presents the old reconciler/types surface as the primary shipped button authoring contract.
</done>
</task>

<task id="18-02-02">
<title>Migrate additional builtins onto TSX authoring through react-dom</title>
<files>
- packages/cli/src/builtin-addons/date-time/index.ts
- packages/cli/src/builtin-addons/date-time/index.test.ts
- packages/cli/src/builtin-addons/core-buttons/index.test.ts
</files>
<action>
Migrate additional shipped builtins beyond action/change-deck onto the browser/react-dom contract using normal React TSX authoring. Start with date/time-style buttons because they are visually obvious and already isolated from the old text helper path. Update builtin tests so the broader shipped button surface is clearly on the TSX/react-dom path rather than split between old and new authoring contracts.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/builtin-addons/date-time/index.test.ts src/builtin-addons/core-buttons/index.test.ts`.
</verify>
<done>
More than the first migrated buttons now ship through the new browser/react-dom contract with focused builtin coverage proving it.
</done>
</task>

<task id="18-02-03">
<title>Add a broader default-frame review fixture for the TSX contract</title>
<files>
- packages/cli/fixtures/phase-18/config.dom-frame-defaults.yml
</files>
<action>
Add a Phase 18 fixture that demonstrates default `buttonFrame` behavior on the broader migrated builtin surface after the first tracer bullet. The fixture must make it easy to compare ordinary framed buttons against any `full_surface` button that intentionally bypasses the frame.
</action>
<verify>
Run `grep -n "full_surface\|date-time\|action\|change-deck" packages/cli/fixtures/phase-18/config.dom-frame-defaults.yml`.
</verify>
<done>
There is a committed review fixture proving the broader default-frame browser/react-dom contract on more than the first migrated buttons.
</done>
</task>
