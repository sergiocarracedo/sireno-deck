---
wave: 1
depends_on: []
files_modified:
  - builtin-addons/date-time/src/index.ts
  - builtin-addons/date-time/src/index.test.ts
  - packages/cli/src/render/types.ts
  - packages/cli/src/render/reconciler.ts
  - packages/cli/src/render/reconciler.test.tsx
  - packages/cli/src/render/text-image.ts
  - packages/cli/src/render/text-image.test.ts
  - packages/cli/fixtures/phase-9/config.calendar-sheet.yml
  - .planning/phases/09-calendar-authoring-clarity/09-UAT.md
autonomous: true
single_layer_justified: false
objective: "Users can configure and review a separate built-in `calendar-sheet` button that renders a readable tear-sheet visual through the existing deck-button contract and core-owned 60000ms scheduler."
must_haves:
  truths:
    - "The bundled date-time addon exposes a third separate button type `calendar-sheet` with `defaultIntervalMs: 60000`."
    - "The calendar renders as a tear-sheet with a dominant day number and small weekday/month context, not as a cramped month grid or fallback text card."
    - "The shipped review path includes a committed config fixture and UAT instructions for the real CLI/device surface."
  artifacts:
    - builtin-addons/date-time/src/index.ts
    - builtin-addons/date-time/src/index.test.ts
    - packages/cli/src/render/types.ts
    - packages/cli/src/render/reconciler.ts
    - packages/cli/src/render/text-image.ts
    - packages/cli/src/render/text-image.test.ts
    - packages/cli/fixtures/phase-9/config.calendar-sheet.yml
    - .planning/phases/09-calendar-authoring-clarity/09-UAT.md
  key_links:
    - "builtin-addons/date-time/src/index.ts emits a deck-button with `variant: 'calendar-sheet'` and no fallback label/subtitle shell"
    - "packages/cli/src/render/text-image.ts renders the tear-sheet calendar through a dedicated `calendar-sheet` branch"
    - "packages/cli/fixtures/phase-9/config.calendar-sheet.yml` exercises the real bundled button type used in UAT"
---

# Plan 09-01: Ship The Calendar-Sheet Visual

<objective>
Deliver the final built-in date/time visual for the milestone by adding a separate `calendar-sheet` button type that uses the existing narrow render contract and core scheduler. A completed plan should let someone run one committed config and review a real tear-sheet calendar on-device without needing the docs/example work from the next slice.
</objective>

## Tasks

<task id="09-01-01">
<title>Add a separate bundled `calendar-sheet` button type with the agreed cadence</title>
<files>
- builtin-addons/date-time/src/index.ts
- builtin-addons/date-time/src/index.test.ts
</files>
<action>
Extend `builtin-addons/date-time/src/index.ts` so the bundled addon exposes `calendar-sheet` as a third separate button type alongside `date-time` and `analog-clock`. Give it its own strict config schema, keep it visually text-free at the addon seam by emitting a `deck-button` with `variant: 'calendar-sheet'` instead of label/subtitle text, and declare `defaultIntervalMs: 60000` so the core runtime owns the refresh cadence. Update `builtin-addons/date-time/src/index.test.ts` to prove the addon now exposes all three button types, that `calendar-sheet` keeps the digital/analog contracts intact, and that the definition advertises the expected cadence.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run ../../builtin-addons/date-time/src/index.test.ts`.
</verify>
<done>
The bundled addon exposes a real `calendar-sheet` button definition with `defaultIntervalMs: 60000`, and tests pin that contract explicitly.
</done>
</task>

<task id="09-01-02">
<title>Carry `calendar-sheet` through the render contract and draw the tear-sheet visual</title>
<files>
- packages/cli/src/render/types.ts
- packages/cli/src/render/reconciler.ts
- packages/cli/src/render/reconciler.test.tsx
- packages/cli/src/render/text-image.ts
- packages/cli/src/render/text-image.test.ts
</files>
<action>
Extend the narrow variant unions in `packages/cli/src/render/types.ts` and `packages/cli/src/render/reconciler.ts` to include `calendar-sheet`, and thread that value through direct `deck-button` usage plus `deck-surface` button collections exactly like the existing analog-clock path. Then add a dedicated tear-sheet renderer branch in `packages/cli/src/render/text-image.ts` that uses a dominant day number with small weekday/month context and remains visually distinct from the default shared text card. Update `packages/cli/src/render/reconciler.test.tsx` and `packages/cli/src/render/text-image.test.ts` so helper/JSX parity remains intact and the calendar-sheet render path is protected from silently falling back to a text card or cramped multi-cell layout.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/reconciler.test.tsx src/render/text-image.test.ts`.
</verify>
<done>
`calendar-sheet` flows end-to-end through the existing `deck-button` seam and renders as a readable tear-sheet visual rather than a fallback card or month grid.
</done>
</task>

<task id="09-01-03">
<title>Ship the committed review path for the calendar visual</title>
<files>
- packages/cli/fixtures/phase-9/config.calendar-sheet.yml
- .planning/phases/09-calendar-authoring-clarity/09-UAT.md
</files>
<action>
Add a committed Phase 9 review fixture under `packages/cli/fixtures/phase-9/` that exercises the real bundled `calendar-sheet` button through the normal CLI/addon path. Then create or update `.planning/phases/09-calendar-authoring-clarity/09-UAT.md` so the reviewer is told exactly how to run the fixture and what to inspect: dominant day number, supporting weekday/month context, absence of a fallback text card, and stable minute-level date semantics. Keep the review path narrow and focused on the new calendar visual rather than bundling unrelated milestone behavior into the same config.
</action>
<verify>
Run `grep -n "calendar-sheet\|60000\|tear-sheet" packages/cli/fixtures/phase-9/config.calendar-sheet.yml .planning/phases/09-calendar-authoring-clarity/09-UAT.md`.
</verify>
<done>
The repo contains a committed calendar-sheet review path that can be used in UAT without inventing ad hoc config or expectations.
</done>
</task>
