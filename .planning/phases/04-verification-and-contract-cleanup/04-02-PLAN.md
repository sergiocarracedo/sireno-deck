---
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - .planning/phases/03-rich-date-time-formatting-surface/03-VERIFICATION.md
  - .planning/STATE.md
autonomous: true
single_layer_justified: true
objective: "Active Phase 3/4 workflow artifacts tell the truth about the already-completed rerun and review state, so operators are no longer routed back into stale rerun instructions after the milestone contract is locked." 
must_haves:
  truths:
    - "`.planning/phases/03-rich-date-time-formatting-surface/03-VERIFICATION.md` no longer claims `verify-work 3` still needs a rerun after the rerun UAT already passed and was committed."
    - "Phase 3 verification evidence reflects the truthful rerun outcome recorded in `03-UAT.md` while preserving the original failure history there."
    - "`.planning/STATE.md` points current workflow continuity at the real Phase 4 execution handoff instead of preserving stale Phase 3 rerun/planning language."
    - "The cleanup preserves active workflow truth without broad archive gardening or rewriting historical blocker evidence."
  artifacts:
    - .planning/phases/03-rich-date-time-formatting-surface/03-VERIFICATION.md
    - .planning/STATE.md
  key_links:
    - "`.planning/phases/03-rich-date-time-formatting-surface/03-VERIFICATION.md` must mirror the rerun-pass truth already preserved in `.planning/phases/03-rich-date-time-formatting-surface/03-UAT.md`."
    - "`.planning/STATE.md` is part of the active workflow routing surface, so its handoff text must match the real post-Phase-4 position instead of stale Phase 3 rerun guidance."
---

# Plan 04-02: Reconcile Active Workflow Truth After The Phase 3 Rerun

<objective>
Close the operator-facing truth gap left behind by the successful Phase 3 rerun and auto-review path. This slice is intentionally single-layer because Phase 4 is a verification-and-contract-cleanup phase: the behavior being delivered is truthful workflow routing and milestone-state reporting, not a new runtime feature, and the active docs/state currently disagree about whether Phase 3 rerun work is still pending.
</objective>

## Tasks

<task id="04-02-01">
<title>Update Phase 3 verification history so it reflects the rerun-pass truth without erasing blocker evidence</title>
<files>
- .planning/phases/03-rich-date-time-formatting-surface/03-VERIFICATION.md
</files>
<action>
Revise `.planning/phases/03-rich-date-time-formatting-surface/03-VERIFICATION.md` so it no longer claims `verify-work 3` must be rerun, no longer reports the manual UAT as `0 passed, 1 issue`, and instead records the truthful post-rerun state already captured in `03-UAT.md` and the committed rerun UAT result. Keep the historical blocker story inspectable by pointing back to the preserved UAT history rather than rewriting it out of existence, but make the current summary, evidence, residual notes, and next-step language match the fact that the rerun passed and review already happened.
</action>
<verify>
Run `! rg -n "0 passed, 1 issue|Next: rerun verify-work 3|verify-work 3 should now be rerun" .planning/phases/03-rich-date-time-formatting-surface/03-VERIFICATION.md && rg -n "pass \(rerun after 03-03\)|passed: 1|issues: 0|rerun_passed_via_03-03-PLAN.md" .planning/phases/03-rich-date-time-formatting-surface/03-UAT.md`.
</verify>
<done>
Phase 3 verification no longer contradicts the already-passed rerun UAT, and the file points readers at the real completed state instead of stale rerun instructions.
</done>
</task>

<task id="04-02-02">
<title>Sync top-level workflow continuity to the real Phase 4 execution handoff</title>
<files>
- .planning/STATE.md
</files>
<action>
Update `.planning/STATE.md` so the current focus, last activity, pending todo, and session continuity text stop implying that Phase 3 rerun/planning work is still the active handoff. Keep the file aligned with the live workflow state after Phase 4 planning/execution work lands: Phase 4 is the active cleanup phase, the known Phase 3 rerun gap is already closed, and the next operator should be routed toward the truthful Phase 4 execution/verification path instead of stale Phase 3 language. Keep the edit narrow to active routing truth; do not turn this into a repo-wide state-history rewrite.
</action>
<verify>
Run `rg -n "Phase 4|Verification and Contract Cleanup|execute-phase 4" .planning/STATE.md && ! rg -n "verify-work 3|Phase 3 rerun|plan-phase 4" .planning/STATE.md`.
</verify>
<done>
The top-level state file routes the next operator through the real Phase 4 path and no longer preserves misleading Phase 3 rerun/planning handoff text as if it were still current.
</done>
</task>

<must_haves>
- Preserve historical failure evidence in `03-UAT.md`; this slice is about current truth, not rewriting history.
- Do not widen into archive-wide prose cleanup or old unreferenced fixture gardening.
- Keep the cleanup on active workflow-routing artifacts only.
</must_haves>
