# Plan 17-04

**Archival note:** The original Phase 17 plan files were not preserved in the repo. This file was reconstructed during v1.2 milestone closeout reconciliation so the surviving execution summaries have a truthful plan-side anchor.

## Objective

Close the real CLI/device transport gap that was preventing explicit `full_surface` buttons from bypassing the default base shape on the shipped path.

## Closure path

- Executed outcome is recorded in `17-04-SUMMARY.md`.
- The later shipped state records that the real-device rerun passed after this transport fix landed.
