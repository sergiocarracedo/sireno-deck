---
wave: 3
depends_on:
  - 27-01-PLAN.md
  - 27-02-PLAN.md
gap_closure: true
files_modified:
  - tsconfig.json
  - packages/cli/src/cli/commands/start.test.ts
  - .planning/phases/27-theme-fallback-and-emulator-shell/27-UAT.md
  - .planning/phases/27-theme-fallback-and-emulator-shell/27-VERIFICATION.md
autonomous: true
single_layer_justified: true
objective: "Close the Phase 27 UAT blocker by making the exact repo-root raw-source CLI command (`pnpm exec tsx packages/cli/src/cli/index.ts ...`) inherit the same JSX policy as the package runtime, then prove that seam and rerun the blocked legacy-YAML rejection check honestly."
must_haves:
  truths:
    - "Running `pnpm exec tsx packages/cli/src/cli/index.ts emulate --config config.yml` from the repo root no longer throws `React is not defined` from `renderDomDeck(...)` on startup."
    - "Focused regression coverage proves the exact repo-root raw-source CLI startup seam, not only the neighboring package-root `node --import tsx/esm` proof seam."
    - "Phase 27 UAT and verification artifacts preserve the blocker evidence, record the repo-root TSX-policy root cause, and point rerun at this closure plan."
    - "After the runtime blocker is closed, the legacy YAML theme check can be rerun and must fail with explicit manifest-backed package guidance instead of silently loading YAML themes."
  artifacts:
    - tsconfig.json
    - packages/cli/src/cli/commands/start.test.ts
    - .planning/phases/27-theme-fallback-and-emulator-shell/27-UAT.md
    - .planning/phases/27-theme-fallback-and-emulator-shell/27-VERIFICATION.md
  key_links:
    - "The failing UAT command is the repo-root raw-source CLI path (`pnpm exec tsx packages/cli/src/cli/index.ts ...`), so the closure must prove that exact seam rather than only package-root runtime imports."
    - "The blocked YAML-rejection check depends on the same startup path surviving long enough to reach theme validation, so one upstream TSX-policy fix should unblock both recorded gaps."
---

# Plan 27-03: Close Repo-Root TSX CLI Drift Blocking Phase 27 UAT

<objective>
Close the upstream runtime-policy gap that Phase 27 verification missed: the repo-root raw-source CLI command still compiles JSX-authored runtime modules under a different TSX policy than the package-local seams. This plan adds one truthful policy anchor for that exact startup path, replaces the neighboring-seam regression with a proof on the real repo-root command, and keeps the UAT/verification docs synchronized so rerun evidence points at this closure work.
</objective>

## Tasks

<task id="27-03-01">
<title>Make the repo-root raw-source CLI command inherit the package JSX policy and prove that exact seam</title>
<files>
- tsconfig.json
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Add the minimal workspace-root TSX policy anchor needed so `pnpm exec tsx packages/cli/src/cli/index.ts ...` from the repo root picks up the same JSX/runtime behavior as `packages/cli/tsconfig.json`. Do not redesign package build settings or widen the scope to unrelated workspace config. Then update the focused runtime regression in `start.test.ts` so it shells the exact repo-root CLI startup seam that failed in UAT, proving that startup no longer throws `React is not defined` and that the command reaches a healthy startup boundary. Keep the existing package-root `node --import tsx/esm` proof only if it still adds distinct value after the repo-root seam is covered.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts` and a repo-root smoke command `pnpm exec tsx packages/cli/src/cli/index.ts emulate --config config.yml --port 0` long enough to confirm startup no longer crashes with `React is not defined`.
</verify>
<done>
The exact repo-root raw-source CLI startup seam now uses the correct JSX policy and has focused regression coverage that would fail if the same runtime drift returned.
</done>
</task>

<task id="27-03-02">
<title>Keep Phase 27 blocker evidence truthful and point rerun at this closure plan</title>
<files>
- .planning/phases/27-theme-fallback-and-emulator-shell/27-UAT.md
- .planning/phases/27-theme-fallback-and-emulator-shell/27-VERIFICATION.md
</files>
<action>
Update the Phase 27 UAT and verification artifacts so they preserve the user-reported repo-root crash, record that the root cause was repo-root TSX CLI policy drift rather than a second `dom-host.tsx` bug, and make `27-03-PLAN.md` the explicit rerun path. Once the runtime blocker is fixed, rerun the blocked legacy-YAML verification truth and record the result explicitly so Phase 27 verification is no longer claiming `passed` while a blocker still exists.
</action>
<verify>
Run `test -f .planning/phases/27-theme-fallback-and-emulator-shell/27-UAT.md && test -f .planning/phases/27-theme-fallback-and-emulator-shell/27-VERIFICATION.md && rg -n "27-03-PLAN.md|React is not defined|root_cause|repo-root|rerun" .planning/phases/27-theme-fallback-and-emulator-shell/27-UAT.md .planning/phases/27-theme-fallback-and-emulator-shell/27-VERIFICATION.md`.
</verify>
<done>
Phase 27’s blocker history remains inspectable, both docs point rerun directly at this closure plan, and the blocked YAML-rejection truth has an explicit rerun path instead of being silently forgotten.
</done>
</task>
