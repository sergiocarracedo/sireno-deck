---
wave: 5
depends_on:
  - 31-04-PLAN.md
gap_closure: true
files_modified:
  - package.json
  - packages/cli/src/cli/dev-watch.ts
  - packages/cli/src/cli/commands/start.test.ts
  - .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md
autonomous: true
single_layer_justified: false
objective: "Developers can run bare `pnpm run cli:dev` from the current worktree and hit the same stable default-start watch seam that Phase 31 already verified, because the live root script, launcher, and regression test are re-pinned to the committed contract."
must_haves:
  truths:
    - "Root `package.json#scripts.cli:dev` again uses the verified `pnpm exec tsx watch` seam, targets `packages/cli/src/cli/dev-watch.ts`, and keeps the full include graph for CLI source, config, themes, addons, and built-in addons."
    - "`packages/cli/src/cli/dev-watch.ts` stays a narrow argv resolver for default `start --config config.yml` plus forwarded args, without ad-hoc debug logging or behavior drift beyond that contract."
    - "The shipped `start.test.ts` regression once again pins the exact live root-script/watch contract strongly enough that future worktree drift in `cli:dev` is visible before UAT."
    - "Phase 31 UAT points the remaining rerun gap at this live-seam restoration plan instead of the already-closed 31-03/31-04 runtime defects."
  artifacts:
    - package.json
    - packages/cli/src/cli/dev-watch.ts
    - packages/cli/src/cli/commands/start.test.ts
    - .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md
  key_links:
    - "The remaining rerun failure is now tied to dirty worktree drift in the exact Phase 31 seam files, not to the already-closed theme-runtime watch loop or synchronous cleanup bug."
    - "`packages/cli/src/cli/commands/start.test.ts` is the repo-owned seam that must fail visibly if `package.json#scripts.cli:dev` drifts away from the verified contract again."
---

# Plan 31-05: Restore The Live cli:dev Seam

<objective>
Close the remaining Phase 31 rerun gap by restoring the live worktree back to the same `cli:dev` contract that 31-01 through 31-04 already verified. The problem is no longer the earlier runtime bugs; it is drift in the exact root script, launcher, and regression seam files that UAT is currently exercising. This slice should re-pin those files to the committed Phase 31 contract and make the active UAT history point at this specific live-seam repair.
</objective>

## Tasks

<task id="31-05-01">
<title>Restore the current root cli:dev script and launcher to the verified Phase 31 contract</title>
<files>
- package.json
- packages/cli/src/cli/dev-watch.ts
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Revert the current live `cli:dev` seam to the previously verified Phase 31 shape. In `package.json`, `scripts.cli:dev` must again use `pnpm exec tsx watch`, target `packages/cli/src/cli/dev-watch.ts`, and keep the full include graph for `./packages/cli/src/**/*`, `./config.yml`, `./themes/**/*`, `./addons/**/*`, and `./builtin-addons/**/*`. In `packages/cli/src/cli/dev-watch.ts`, keep only the narrow default-start and forwarded-args resolution behavior; remove ad-hoc startup/debug logging or any other local behavior drift that is not part of the Phase 31 contract. In `packages/cli/src/cli/commands/start.test.ts`, ensure the shipped regression once again asserts the exact root script/watch graph and launcher contract strongly enough to catch this drift before manual UAT.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/cli/dev-watch.test.ts`, `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts -t "documents the workspace-root cli:dev script as the full-process raw-source restart seam"`, and `grep -n 'pnpm exec tsx watch\|--include ./themes/\*\*/*\|--include ./addons/\*\*/*\|--include ./builtin-addons/\*\*/*\|packages/cli/src/cli/dev-watch.ts' package.json`.
</verify>
<done>
The live worktree once again matches the exact Phase 31 root watch contract that the repo previously verified, instead of a drifted local variant.
</done>
</task>

<task id="31-05-02">
<title>Point the active UAT rerun gap at this live-seam restoration slice</title>
<files>
- .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md
</files>
<action>
Update the remaining rerun-specific bare-path gap in `31-UAT.md` so it clearly distinguishes the already-closed 31-03/31-04 runtime defects from the newly diagnosed live-worktree seam drift, and make `31-05-PLAN.md` the explicit rerun plan for that remaining gap. Preserve the original failed reports and the passed rerun outcomes for forwarded args and README truth; do not rewrite history into a clean pass.
</action>
<verify>
Run `rg -n "31-05-PLAN.md|worktree drift|package.json|dev-watch.ts|start.test.ts" .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md`.
</verify>
<done>
The only remaining Phase 31 rerun gap now points at the correct live-seam restoration plan, with prior runtime closures and rerun passes preserved accurately.
</done>
</task>
