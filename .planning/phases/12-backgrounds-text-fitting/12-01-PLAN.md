---
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/core/schemas.ts
  - packages/cli/src/config/loader.test.ts
  - packages/cli/src/cli/commands/start.ts
  - packages/cli/src/render/types.ts
  - packages/cli/src/render/reconciler.ts
  - packages/cli/src/render/reconciler.test.tsx
  - packages/cli/src/render/text-image.ts
  - packages/cli/src/render/text-image.test.ts
  - packages/cli/fixtures/phase-12/config.background-precedence.yml
  - .planning/phases/12-backgrounds-text-fitting/12-UAT.md
autonomous: true
single_layer_justified: false
objective: "Users can configure color-only backgrounds with explicit button -> deck -> theme precedence, and the shipped review path proves the renderer resolves the same contract consistently instead of relying on per-variant fallback rules."
must_haves:
  truths:
    - "A button can render with its own configured background color without affecting sibling buttons or the deck default."
    - "When a button has no override, the active deck background color is used before falling back to the theme background."
    - "The shared/default render path resolves one explicit color-only background contract end-to-end through config validation, render descriptions, and final SVG generation."
    - "The repo ships a committed Phase 12 review path and focused renderer coverage for button override, deck fallback, and theme fallback behavior."
  artifacts:
    - packages/cli/src/core/schemas.ts
    - packages/cli/src/render/types.ts
    - packages/cli/src/render/reconciler.ts
    - packages/cli/src/render/text-image.ts
    - packages/cli/src/render/text-image.test.ts
    - packages/cli/fixtures/phase-12/config.background-precedence.yml
    - .planning/phases/12-backgrounds-text-fitting/12-UAT.md
  key_links:
    - "packages/cli/src/core/schemas.ts validates button-level and deck-level color-only background fields that map onto the new render contract."
    - "packages/cli/src/cli/commands/start.ts or an equivalent runtime seam must materialize deck fallback onto button render data before `renderTextImage()` runs, because the current render path is button-local at that boundary."
    - "packages/cli/src/render/reconciler.ts and packages/cli/src/render/types.ts carry resolved background information through the existing deck-button seam."
    - "packages/cli/src/render/text-image.ts uses the resolved background color in the shared/default renderer path instead of re-deciding fallback precedence internally."
---

# Plan 12-01: Ship Explicit Background Precedence

<objective>
Deliver the first half of Phase 12 as one demoable tracer bullet: a user can configure color-only backgrounds at button and deck scope, the renderer applies the exact agreed precedence `button -> deck -> theme`, and the repo includes a committed review path plus tests that prove the contract on the real shared/default render seam.
</objective>

## Tasks

<task id="12-01-01">
<title>Add validated color-only background fields at the config seams</title>
<files>
- packages/cli/src/core/schemas.ts
- packages/cli/src/config/loader.test.ts
</files>
<action>
Extend the validated config surface in `packages/cli/src/core/schemas.ts` so Phase 12 can express color-only background choices at the two approved levels: per-button override and per-deck fallback. Keep the shape narrow and explicit; do not add image, gradient, or asset-backed background semantics, and do not add a top-level global override. Update `packages/cli/src/config/loader.test.ts` so valid background fields survive loading and invalid shapes still preserve the repo’s existing path/line-aware diagnostics. Use field names and validation that can be materialized later onto per-button render data without needing deck context inside `renderTextImage()` itself.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/config/loader.test.ts`.
</verify>
<done>
The config path can express button and deck background colors cleanly, and invalid Phase 12 background config still fails with readable diagnostics.
</done>
</task>

<task id="12-01-02">
<title>Carry resolved background precedence through the render contract and shared SVG path</title>
<files>
- packages/cli/src/cli/commands/start.ts
- packages/cli/src/render/types.ts
- packages/cli/src/render/reconciler.ts
- packages/cli/src/render/reconciler.test.tsx
- packages/cli/src/render/text-image.ts
- packages/cli/src/render/text-image.test.ts
</files>
<action>
Evolve the narrow render contract in `packages/cli/src/render/types.ts` and `packages/cli/src/render/reconciler.ts` so a `deck-button` can carry explicit background information through the existing seam. Because the live rendering path currently calls `renderTextImage()` one button at a time from `packages/cli/src/cli/commands/start.ts`, add the minimal runtime/start seam needed to materialize `button -> deck -> theme` fallback onto each rendered button before image generation. Then update the shared/default rendering path in `packages/cli/src/render/text-image.ts` so the configured background color becomes the base tint source for the default card branch instead of acting like a separate overlay or silently falling back to theme background inside variant code. Keep this Phase 12-scoped: color-only, shared/default path first, and no forced rewrite of bespoke variants. Add focused reconciler and text-image tests that prove button override beats deck fallback, deck fallback beats theme background, and unrelated buttons do not drift when one button overrides its background.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/reconciler.test.tsx src/render/text-image.test.ts`.
</verify>
<done>
Background precedence is explicit end-to-end on the shared/default button path, and tests prove the exact `button -> deck -> theme` behavior.
</done>
</task>

<task id="12-01-03">
<title>Ship the committed Phase 12 background review path</title>
<files>
- packages/cli/fixtures/phase-12/config.background-precedence.yml
- .planning/phases/12-backgrounds-text-fitting/12-UAT.md
</files>
<action>
Add a committed Phase 12 fixture under `packages/cli/fixtures/phase-12/` that makes the three precedence levels reviewable on the real CLI/device path: one button with its own background override, one button inheriting the deck background, and one button inheriting only the theme background through a different deck or surface path. Then create or update `.planning/phases/12-backgrounds-text-fitting/12-UAT.md` so the reviewer is told exactly what to inspect and compare. Keep the fixture narrow and phase-specific; do not bundle wrap-mode testing or unrelated visual changes into the same review path.
</action>
<verify>
Run `grep -n "background" packages/cli/fixtures/phase-12/config.background-precedence.yml .planning/phases/12-backgrounds-text-fitting/12-UAT.md`.
</verify>
<done>
The repo contains a committed, reviewable Phase 12 background-precedence path instead of leaving the contract proven only by unit tests.
</done>
</task>

<must_haves>
- `SCS-03` is satisfied for the color-only subset shipped in this phase slice: button override, deck fallback, and theme fallback are explicit and shared.
- The config/render contract stays narrow and does not widen into gradients, images, or global background override semantics.
- The deliverable is demoable through a committed fixture path and focused renderer coverage.
</must_haves>
