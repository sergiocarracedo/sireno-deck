---
wave: 4
depends_on:
  - 16-01-PLAN.md
  - 16-02-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/config/theme.ts
  - packages/cli/src/config/theme.test.ts
  - packages/cli/src/config/loader.ts
  - packages/cli/src/config/loader.test.ts
  - packages/cli/src/cli/commands/start.ts
  - packages/cli/src/cli/commands/start.test.ts
  - .planning/phases/16-config-reload-wrapper-polish/16-UAT.md
autonomous: true
single_layer_justified: false
objective: "Close the Phase 16 UAT startup blocker by making theme resolution follow config ownership instead of launch cwd, preserving a real watched config-file graph for deck refs, and recording the closure path so manual Tests 1-4 can be rerun on-device."
must_haves:
  truths:
    - "Starting `pnpm exec tsx src/cli/index.ts start --config /tmp/sireno-phase16-uat/config.yml` from `packages/cli` succeeds when the config uses the built-in `dark` theme and a referenced deck file."
    - "Built-in theme names resolve independently of the process launch directory, and relative custom theme paths resolve from the owning config file directory."
    - "The watched config source graph includes the root config file plus loaded deck-ref files without injecting `undefined` entries into `fs.watch()`."
    - "Focused tests cover both the theme-resolution regression and the watched-file-path regression on the startup path."
    - "The Phase 16 UAT record links both diagnosed gaps to this closure plan and notes that Tests 1-4 must be rerun on the real fixture path."
  artifacts:
    - packages/cli/src/config/theme.ts
    - packages/cli/src/config/theme.test.ts
    - packages/cli/src/config/loader.ts
    - packages/cli/src/config/loader.test.ts
    - packages/cli/src/cli/commands/start.ts
    - packages/cli/src/cli/commands/start.test.ts
    - .planning/phases/16-config-reload-wrapper-polish/16-UAT.md
  key_links:
    - "`packages/cli/src/cli/commands/start.ts` must pass the owning config directory into `resolveTheme()` so startup follows the config contract instead of `process.cwd()`."
    - "`packages/cli/src/config/loader.ts` must return a real watched file list because reload for deck refs depends on the root config path and referenced deck paths both reaching `fs.watch()`."
    - "`packages/cli/src/config/theme.test.ts`, `packages/cli/src/config/loader.test.ts`, and `packages/cli/src/cli/commands/start.test.ts` must pin the exact regressions that blocked UAT startup."
---

# Plan 16-05: Close The Phase 16 UAT Startup Blocker

<objective>
Fix the specific Phase 16 verification regression that prevented manual UAT from even starting. A completed plan should make the exact `/tmp/sireno-phase16-uat` start command run successfully from `packages/cli`, prove the startup path with focused tests, and leave the UAT record ready for a real rerun instead of remaining stuck in diagnosis mode.
</objective>

## Tasks

<task id="16-05-01">
<title>Fix config-owned theme resolution and watched file-path construction on the startup path</title>
<files>
- packages/cli/src/config/theme.ts
- packages/cli/src/config/loader.ts
- packages/cli/src/cli/commands/start.ts
</files>
<action>
Update theme resolution so built-in theme names no longer depend on the launch cwd and relative custom theme paths resolve from the owning config directory. In the same slice, correct the loader's watched-file-path output so the root config file path and loaded deck-ref file paths are returned as real strings instead of mixing a raw string with deck-source objects. Keep the fix narrow to the startup blocker; do not redesign the broader reload manager or widen Phase 16 scope.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/config/theme.test.ts src/config/loader.test.ts src/cli/commands/start.test.ts`.
</verify>
<done>
The Phase 16 startup path no longer fails because of cwd-relative theme lookup or `undefined` watcher paths.
</done>
</task>

<task id="16-05-02">
<title>Prove the exact UAT startup path and record the closure plan in Phase 16 UAT</title>
<files>
- packages/cli/src/config/theme.test.ts
- packages/cli/src/config/loader.test.ts
- packages/cli/src/cli/commands/start.test.ts
- .planning/phases/16-config-reload-wrapper-polish/16-UAT.md
</files>
<action>
Add or update focused regression tests so they pin the exact startup failures discovered during UAT: theme lookup when launching from `packages/cli`, and the watched file graph used by `watchConfigFiles()`. Then update `16-UAT.md` so both diagnosed gaps point at this closure plan and clearly state that the remaining work is a manual rerun of Tests 1-4 on the real `/tmp/sireno-phase16-uat` fixture path.
</action>
<verify>
Run `test -f .planning/phases/16-config-reload-wrapper-polish/16-UAT.md && grep -n "closure_plan\|fixed_pending_rerun\|pending_rerun" .planning/phases/16-config-reload-wrapper-polish/16-UAT.md`.
</verify>
<done>
The UAT record links the diagnosed startup blocker to a concrete closure plan and the codebase carries regression coverage for the exact path that blocked manual verification.
</done>
</task>
