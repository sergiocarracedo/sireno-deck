---
wave: 2
depends_on:
  - 10-01-PLAN.md
files_modified:
  - README.md
  - packages/cli/fixtures/phase-9/jsx-addon-authoring-example.tsx
  - packages/cli/fixtures/phase-9/tsconfig.jsx-authoring-example.json
  - packages/cli/package.json
  - packages/cli/src/render/reconciler.test.tsx
  - .planning/phases/10-public-authoring-exports/README.md
autonomous: true
single_layer_justified: false
objective: "The shipped docs and example use the real public package imports, and verification proves addon authoring resolves against the built package surface instead of repo-local path mappings or source inclusion."
must_haves:
  truths:
    - "The authoring example imports helpers from `sireno-deck-cli` and JSX types from `sireno-deck-cli/jsx`."
    - "Verification fails if the example only works through repo-local `paths` mapping or direct source files."
    - "Shipped docs point at the same public imports that the built package now emits."
  artifacts:
    - README.md
    - packages/cli/fixtures/phase-9/jsx-addon-authoring-example.tsx
    - packages/cli/fixtures/phase-9/tsconfig.jsx-authoring-example.json
    - packages/cli/src/render/reconciler.test.tsx
    - .planning/phases/10-public-authoring-exports/README.md
  key_links:
    - "README.md and the fixture example teach the same import shape: `sireno-deck-cli` plus `sireno-deck-cli/jsx`"
    - "The Phase 9 example stops importing from `../../src/...`"
    - "Verification resolves the example through the built package surface after build"
---

# Plan 10-02: Prove Docs And Examples Against The Built Package

<objective>
Finish the gap-closure phase by making the shipped addon authoring guidance tell the truth about the packaged surface and by verifying that truth directly against the built package. A completed plan should catch the exact class of drift that the milestone audit found: docs and examples appearing valid in-repo while the distributable package is missing the imports they rely on.
</objective>

## Tasks

<task id="10-02-01">
<title>Realign the shipped docs and authoring example to the public package imports</title>
<files>
- README.md
- packages/cli/fixtures/phase-9/jsx-addon-authoring-example.tsx
- .planning/phases/10-public-authoring-exports/README.md
</files>
<action>
Update the shipped addon authoring docs and the focused Phase 9 example so they both use the real public imports exposed by the package: helper constructors from `sireno-deck-cli` and explicit JSX opt-in from `sireno-deck-cli/jsx`. Remove the repo-local helper import from the example so it stops cheating. Update the phase README only as needed so the recorded planning focus matches the now-concrete release path.
</action>
<verify>
Run `grep -n 'sireno-deck-cli/jsx\|createDeckButtonElement\|createDeckSurfaceElement\|createDeckTextElement' README.md packages/cli/fixtures/phase-9/jsx-addon-authoring-example.tsx .planning/phases/10-public-authoring-exports/README.md`.
</verify>
<done>
The docs, example, and phase notes all teach the same real public import surface that external addon authors will consume.
</done>
</task>

<task id="10-02-02">
<title>Add release-facing verification that resolves authoring imports from built output</title>
<files>
- packages/cli/fixtures/phase-9/tsconfig.jsx-authoring-example.json
- packages/cli/package.json
- packages/cli/src/render/reconciler.test.tsx
</files>
<action>
Replace the current repo-local verification shape with one that proves the example resolves against the built package surface after `pnpm --filter sireno-deck-cli build`. Adjust the fixture `tsconfig` and any adjacent verification support only as needed so the check no longer depends on direct `../../src/...` files or a fake `paths` mapping to source declarations. Add or update automated coverage near the render authoring seam if needed so future drift between package exports, build output, and example imports becomes test-visible.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli build && pnpm --filter sireno-deck-cli exec tsc -p fixtures/phase-9/tsconfig.jsx-authoring-example.json --noEmit`.
</verify>
<done>
The repo now contains a release-facing authoring check that fails unless the built package surface really supports the documented helper and JSX imports.
</done>
</task>

<must_haves>
- `UIW-03` remains satisfied by shipped docs that still explain the non-DOM contract, now using the real package surface.
- The phase closes only when docs, examples, package exports, build output, and verification all point at the same public import paths.
- The deliverable specifically guards against repeating the milestone-audit regression.
</must_haves>
