---
wave: 2
depends_on:
  - 09-01-PLAN.md
files_modified:
  - README.md
  - packages/cli/fixtures/phase-9/jsx-addon-authoring-example.tsx
  - packages/cli/fixtures/phase-9/tsconfig.jsx-authoring-example.json
  - packages/cli/src/render/reconciler.test.tsx
  - .planning/phases/09-calendar-authoring-clarity/09-UAT.md
autonomous: true
single_layer_justified: false
objective: "Addon authors can read one focused guide and one explicit example that explain the Sireno custom JSX render contract as non-DOM authoring, while the example remains verified against the shipped API."
must_haves:
  truths:
    - "Shipped docs explain that `deck-button`, `deck-text`, and `deck-surface` target the Sireno renderer contract rather than the DOM."
    - "The repo contains one explicit addon-style authoring example that shows the current JSX opt-in path and helper-based alternative accurately."
    - "Verification proves the example and docs still match the real package API instead of silently drifting."
  artifacts:
    - README.md
    - packages/cli/fixtures/phase-9/jsx-addon-authoring-example.tsx
    - packages/cli/fixtures/phase-9/tsconfig.jsx-authoring-example.json
    - .planning/phases/09-calendar-authoring-clarity/09-UAT.md
  key_links:
    - "README.md points addon authors at `sireno-deck-cli/jsx` and explains the non-DOM contract explicitly"
    - "The Phase 9 authoring example typechecks only when the explicit JSX opt-in path is wired correctly"
---

# Plan 09-02: Explain The Non-DOM Authoring Contract Clearly

<objective>
Close the milestone's authoring clarity requirement with one focused docs surface and one verified addon-style example. A completed plan should let an addon author understand what the custom elements are, how to opt into JSX, and how helper-based authoring differs, without needing to infer the contract from internal tests or scattered planning notes.
</objective>

## Tasks

<task id="09-02-01">
<title>Write one focused non-DOM authoring guide in the shipped docs</title>
<files>
- README.md
</files>
<action>
Add a focused section to the shipped docs that explains the Sireno custom render model clearly. It must say that `deck-button`, `deck-text`, and `deck-surface` are custom renderer elements rather than DOM nodes, explain the explicit JSX opt-in path through `sireno-deck-cli/jsx`, and show the helper-based alternative without implying that JSX is ambient or DOM-like. Keep the docs page concrete and example-driven instead of writing abstract framework prose.
</action>
<verify>
Run `grep -n "sireno-deck-cli/jsx\|not the DOM\|deck-button\|deck-text\|deck-surface" README.md`.
</verify>
<done>
The shipped docs contain one clear, discoverable explanation of the non-DOM authoring contract using the real package API names.
</done>
</task>

<task id="09-02-02">
<title>Add and verify one explicit addon-style authoring example</title>
<files>
- packages/cli/fixtures/phase-9/jsx-addon-authoring-example.tsx
- packages/cli/fixtures/phase-9/tsconfig.jsx-authoring-example.json
- packages/cli/src/render/reconciler.test.tsx
</files>
<action>
Add one Phase 9 addon-style example file under `packages/cli/fixtures/phase-9/` that demonstrates the current JSX authoring contract with explicit opt-in, plus a small helper-based counterpart in the same example or adjacent snippet so the relationship is obvious. Add a dedicated fixture `tsconfig` that opts into `sireno-deck-cli/jsx` intentionally rather than relying on ambient declarations. Update verification only as needed so the example is checked against the real package API and does not drift quietly from the shipped docs.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec tsc -p fixtures/phase-9/tsconfig.jsx-authoring-example.json --noEmit`.
</verify>
<done>
The repo contains a real, verified addon-style authoring example that matches the docs and the current JSX/helper contract.
</done>
</task>

<task id="09-02-03">
<title>Fold docs/example review into the Phase 9 verification path</title>
<files>
- .planning/phases/09-calendar-authoring-clarity/09-UAT.md
</files>
<action>
Extend the Phase 9 UAT script so reviewers do not only inspect the calendar visual. Add one authoring-clarity check that points at the shipped docs section and the addon-style example, and tells the reviewer what should now be obvious: the custom elements are non-DOM, the JSX path is explicit opt-in, and a helper-based alternative still exists. Keep the check concrete enough that documentation drift becomes review-visible.
</action>
<verify>
Run `grep -n "non-DOM\|sireno-deck-cli/jsx\|helper" .planning/phases/09-calendar-authoring-clarity/09-UAT.md`.
</verify>
<done>
Phase 9 review covers both the calendar widget and the authoring clarity promise, not just the runtime visual.
</done>
</task>
