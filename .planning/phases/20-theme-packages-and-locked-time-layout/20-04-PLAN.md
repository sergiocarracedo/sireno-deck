---
wave: 4
depends_on:
  - 20-02-PLAN.md
  - 20-03-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/core/schemas.ts
  - packages/cli/src/addon/api.ts
  - packages/cli/src/render/dom-host.test.tsx
  - packages/cli/src/config/loader.test.ts
  - packages/cli/src/builtin-addons/emoji-selector/index.test.ts
  - .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md
autonomous: true
single_layer_justified: false
objective: "Close the Phase 20 UAT gap where addon/config-authored image assets resolve through the shared registry but still render as broken squares on the shipped browser/device path, by normalizing browser-consumed image refs to valid file URLs and pinning the real fixture contract in focused tests."
must_haves:
  truths:
    - "Config-authored and addon-authored image asset refs used on the browser path render as actual images instead of broken placeholder squares on the shipped asset-pipeline fixture."
    - "The fix stays in the shared asset/browser seam rather than adding another emoji-selector-specific patch."
    - "Focused tests prove browser-consumed image refs are normalized to `file://` URLs while missing asset refs still fail explicitly during config validation/load."
    - "The Phase 20 UAT record links the shared-asset image-rendering gap to this closure plan so the asset fixture gets rerun after the fix lands."
  artifacts:
    - packages/cli/src/core/schemas.ts
    - packages/cli/src/addon/api.ts
    - packages/cli/src/render/dom-host.test.tsx
    - packages/cli/src/config/loader.test.ts
    - .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md
  key_links:
    - "`packages/cli/src/core/schemas.ts` must keep the package-root-aware asset resolver authoritative while producing browser-safe image refs for config/addon assets."
    - "`packages/cli/src/addon/api.ts` and the browser host tests must prove `<img src>` receives a browser-loadable URL instead of a raw filesystem path."
    - "`packages/cli/src/config/loader.test.ts` must continue proving missing asset refs fail path-aware during load instead of degrading silently."
---

# Plan 20-04: Close The Shared Asset Image Rendering UAT Gap

<objective>
Fix the specific Phase 20 UAT regression where the shared asset fixture resolves addon/config image references but the shipped browser/device path still shows broken squares instead of the expected icons. A completed plan should normalize those image refs into browser-loadable URLs at the shared resolver/render seam, keep the fix generic to all browser-consumed image assets, and wire the failed UAT case to a concrete rerun path.
</objective>

## Tasks

<task id="20-04-01">
<title>Normalize shared image asset refs for the browser/device render path</title>
<files>
- packages/cli/src/core/schemas.ts
- packages/cli/src/addon/api.ts
- packages/cli/src/render/dom-host.test.tsx
</files>
<action>
Trace the browser image path from config/addon asset expansion into `createDomIcon()` and fix the smallest shared seam that leaves `<img src>` with a raw filesystem path. Keep config/addon asset ownership in the existing package-root-aware resolver, but ensure browser-consumed image refs become valid `file://` URLs before static HTML reaches Playwright/Chromium. Do not add an emoji-selector-specific workaround and do not weaken the existing missing-asset failure behavior.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/dom-host.test.tsx src/config/loader.test.ts src/builtin-addons/emoji-selector/index.test.ts`.
</verify>
<done>
The shared browser render path loads addon/config-authored icon assets as real images instead of broken placeholder squares.
</done>
</task>

<task id="20-04-02">
<title>Pin the failed asset-fixture expectation and link the rerun path in Phase 20 UAT</title>
<files>
- packages/cli/src/config/loader.test.ts
- .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md
</files>
<action>
Strengthen focused verification so the Phase 20 asset pipeline coverage proves that resolved browser-consumed icon refs are emitted in a browser-loadable form, not just that they contain an asset filename. Then update `20-UAT.md` so the failed shared-asset gap explicitly references this closure plan and tells downstream work to rerun Fixture 2 on `packages/cli/fixtures/phase-20/config.asset-pipeline.yml` after the fix lands.
</action>
<verify>
Run `test -f .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md && grep -n "closure_plan\|Fixture 2\|asset pipeline\|quare\|imagess" .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md`.
</verify>
<done>
The failed shared-asset image-rendering contract is pinned in focused verification and the UAT record points to a concrete rerun path.
</done>
</task>
