---
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/builtin-addons/weather/buttons/components/DailyForecast.test.tsx
  - packages/cli/src/builtin-addons/weather/buttons/weather.test.tsx
  - packages/cli/src/ui/surfaces/__tests__/Bars.test.tsx
  - packages/cli/src/ui/utils/__tests__/negative-color.test.ts
  - packages/cli/src/builtin-addons/brightness/buttons/brightness.test.ts
  - .planning/phases/56-v1.5-verification-sweep/56-VERIFICATION.md
  - .planning/REQUIREMENTS.md (VERIFY-01 status)
autonomous: true
single_layer_justified: true
objective: Close remaining medium-priority test coverage gaps across weather, bars, and brightness, and generate the VERIFICATION.md sweep document
created: 2026-06-10
---

# 56-02 — Medium-priority coverage gaps + sweep document

> Closes remaining medium-priority test gaps across all v1.5 feature areas, then generates the VERIFICATION.md sweep document that traces VERIFY-01 requirement criteria to test evidence.

## Context

Research identified these medium-priority gaps:

**Weather:**
- Daily forecast WMO icon rendering not tested (only text values checked)
- Weather button imperial unit rendering not tested (only metric tested)

**Bars:**
- `label_color` prop not tested on Bars component
- Near-gray DOM path not tested (sharp path is tested, DOM `mix-blend-mode: difference` path isn't)

**Brightness:**
- Brightness addon button rendering (BrightnessSurface) not tested
- No render-assertion test for the brightness button Surface component

## Tasks

### Task 1: Add weather daily forecast WMO icon rendering test

**File:** `packages/cli/src/builtin-addons/weather/buttons/components/DailyForecast.test.tsx`

Add tests for WMO icon rendering in the DailyForecast component:
- Clear sky (code 0) renders the correct icon/emoji
- Rain (code 65) renders the correct icon/emoji
- Snow (code 85) renders the correct icon/emoji
- Ensure the icon element is present alongside day label, high/low temps, precipitation

**Action:** Add 3-4 tests covering icon rendering for different WMO codes. Use `screen.getByRole('img')` or element text assertions.
**Verify:** `pnpm --filter sireno-deck-cli test src/builtin-addons/weather/buttons/components/DailyForecast.test.tsx` — passes.
**Done:** Tests committed.

### Task 2: Add weather daily forecast `timezone=auto` test

**File:** `packages/cli/src/builtin-addons/weather/domain/open-meteo-client.test.ts`

Read the existing `fetchOpenMeteoSnapshot` tests to understand the test setup. Add a test asserting that the API request URL includes `timezone=auto` when building the daily forecast URL. The test should verify the query parameter is present in the final request URL.

This covers the VERIFY-01 requirement: "Daily forecast tests assert the request includes `timezone=auto`."

**Action:** Add 1 test.
**Verify:** `pnpm --filter sireno-deck-cli test src/builtin-addons/weather/domain/open-meteo-client.test.ts -t "timezone"` — passes.
**Done:** Test committed.

### Task 3: Add weather imperial unit rendering test

**File:** `packages/cli/src/builtin-addons/weather/buttons/weather.test.tsx`

Add a test that renders the weather button with imperial unit config and asserts:
- Temperature displayed in °F (not °C)
- Wind speed in mph (not km/h)

**Action:** Add 1-2 tests with imperial config passed to the button renderer.
**Verify:** `pnpm --filter sireno-deck-cli test src/builtin-addons/weather/buttons/weather.test.tsx` — passes.
**Done:** Tests committed.

### Task 4: Add Bars label_color + near-gray DOM path test

**File:** `packages/cli/src/ui/surfaces/__tests__/Bars.test.tsx`

Add tests:
- When a Bars item has no `color`, its label uses the theme primary color token (BARS-01 requirement)
- The DOM path uses `mix-blend-mode: difference` for in-bar value text

**File:** `packages/cli/src/ui/utils/__tests__/negative-color.test.ts`

Add test:
- `computeNegativeColor` with a color near gray (luma within 32 of 128) returns white-on-dark or black-on-light — confirm the DOM path logic is tested (BARS-03 requirement)

**Action:** Add 3 tests (2 for Bars component, 1 for negative-color utility).
**Verify:** `pnpm --filter sireno-deck-cli test src/ui/surfaces/__tests__/Bars.test.tsx` — passes. `pnpm --filter sireno-deck-cli test src/ui/utils/__tests__/negative-color.test.ts` — passes.
**Done:** Tests committed.

### Task 5: Add brightness button rendering test

**File:** `packages/cli/src/builtin-addons/brightness/buttons/brightness.test.ts`

Read the existing brightness test file and the BrightnessSurface component to understand the rendering pattern. Then add:
- A test that renders the brightness button Surface with a given brightness percentage
- Assert the percentage text is displayed (e.g., "50%")

**Action:** Add 1-2 rendering tests. If the component uses shared Surface helpers, follow the pattern from other addon button tests.
**Verify:** `pnpm --filter sireno-deck-cli test src/builtin-addons/brightness/buttons/brightness.test.ts` — passes.
**Done:** Tests committed.

### Task 6: Generate VERIFICATION.md sweep document

**File:** `.planning/phases/56-v1.5-verification-sweep/56-VERIFICATION.md`

Create a verification document following the existing pattern (see 55-VERIFICATION.md, 54-VERIFICATION.md) that maps VERIFY-01 criteria to test evidence. The document must cover:

**VERIFY-01 requirements:**
- Geocoder tests cover cache miss, cache hit, invalid city name, and network failure
- Daily forecast tests assert the request includes `timezone=auto` and the 2-day window
- Bars tests assert label color, in-bar value rendering, and the near-gray auto-contrast fallback for both DOM and sharp paths
- Brightness tests cover the single-device, multi-device, and rollback paths with a mock SDK
- Lock-deck tests assert that back injection is skipped when locked and present when unlocked
- Active-app tests assert: process match, overlay render, toggle behavior, double-tap back, multi-addon conflict warning
- All existing v1.4 tests still pass

For each criterion, list:
- Status (✓ or pending)
- Evidence path (file + line number or describe block name)
- Which plan added the coverage (56-01 or 56-02, or pre-existing)

**Action:** Write the VERIFICATION.md file.
**Verify:** The document reads correctly — all VERIFY-01 sub-items are traced to evidence.
**Done:** Document committed.

## Must Haves

- [ ] Daily forecast API URL includes `timezone=auto` query parameter
- [ ] DailyForecast WMO icon rendering tested for at least 3 weather codes
- [ ] Weather button imperial unit rendering tested (°F, mph)
- [ ] Bars DOM path tested for `mix-blend-mode: difference` and theme primary label color
- [ ] Brightness button Surface rendering tested (percentage text displayed)
- [ ] `56-VERIFICATION.md` maps every VERIFY-01 sub-criterion to test evidence
- [ ] All new tests pass in full-file run — no pre-existing state pollution
