---
wave: 4
depends_on:
  - 26-03-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/render/dom-host.tsx
  - packages/cli/src/render/button-frame.tsx
  - packages/cli/src/cli/commands/start.test.ts
  - .planning/phases/26-browser-deck-react-shell-polish/26-UAT.md
  - .planning/phases/26-browser-deck-react-shell-polish/26-VERIFICATION.md
autonomous: true
single_layer_justified: true
objective: "Close the Phase 26 UAT blocker where the new JSX-authored browser deck render modules crash on the real CLI/emulator `tsx` runtime with `React is not defined`, so the shared shell, inline warning path, and startup handoff can actually render on the shipped path."
must_haves:
  truths:
    - "`packages/cli/src/render/dom-host.tsx` and `packages/cli/src/render/button-frame.tsx` are compatible with the real CLI/emulator runtime path and no longer throw `React is not defined` when `renderDomDeck(...)` or `ButtonFrame(...)` execute outside Vitest's transform behavior."
    - "Focused runtime-path regression coverage proves the shipped CLI/emulator seam reaches a valid deck HTML render without ambient-React failures."
    - "Phase 26 UAT and verification artifacts preserve the blocker evidence and point the rerun at this closure plan."
  artifacts:
    - packages/cli/src/render/dom-host.tsx
    - packages/cli/src/render/button-frame.tsx
    - packages/cli/src/cli/commands/start.test.ts
    - .planning/phases/26-browser-deck-react-shell-polish/26-UAT.md
    - .planning/phases/26-browser-deck-react-shell-polish/26-VERIFICATION.md
  key_links:
    - "`packages/cli/src/cli/commands/start.ts` still reaches `createDeckHtml(...) -> renderDomDeck(...)` on both emulator and normal startup, so the fix must land at that shared JSX runtime seam rather than in separate emulator/startup branches."
    - "`packages/cli/src/render/button-frame.tsx` and `packages/cli/src/render/dom-host.tsx` must stay aligned on the same runtime-safe JSX policy or the shared shell and default frame will drift again."
---

# Plan 26-04: Fix Phase 26 JSX Runtime Drift On The Real CLI Path

<objective>
Close the UAT blocker at the actual upstream seam: Phase 26 moved core browser-shell render modules onto JSX syntax, but the shipped CLI/emulator runtime path still executes them in a way that expects a runtime `React` value in scope. This plan makes those render modules runtime-safe on the real path, adds a focused regression on the real shared deck HTML seam, and preserves the UAT/verification evidence so the rerun path is explicit.
</objective>

## Tasks

<task id="26-04-01">
<title>Make the shared deck render modules runtime-safe on the real CLI/emulator JSX path</title>
<files>
- packages/cli/src/render/dom-host.tsx
- packages/cli/src/render/button-frame.tsx
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Refactor the Phase 26 JSX-authored runtime modules so they no longer depend on an ambient/classic `React` global when executed through the real `tsx` CLI/emulator path. Keep the Phase 26 ownership and visual contracts intact; this is a runtime-compatibility closure, not another shell redesign. Then add one focused regression that exercises the shared startup/runtime seam honestly enough to fail if `createDeckHtml(...) -> renderDomDeck(...)` or the default button frame drift back to `React is not defined` outside Vitest-only transforms.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts src/render/dom-host.test.tsx src/render/button-frame.test.tsx` and a focused `pnpm exec tsx --eval "(async () => { const { renderDomDeck } = await import('./packages/cli/src/render/dom-host.tsx'); console.log(renderDomDeck([], { keyCount: 1 }).slice(0, 40)); })().catch((error) => { console.error(error); process.exit(1); });"` smoke check from the repo root.
</verify>
<done>
The shared deck document and default frame render on the real CLI/emulator runtime path without `React is not defined`, and focused coverage proves the fix on the actual seam that UAT hit.
</done>
</task>

<task id="26-04-02">
<title>Keep the Phase 26 blocker evidence linked to this runtime-seam closure plan</title>
<files>
- .planning/phases/26-browser-deck-react-shell-polish/26-UAT.md
- .planning/phases/26-browser-deck-react-shell-polish/26-VERIFICATION.md
</files>
<action>
Update the Phase 26 UAT and verification artifacts so they preserve the manual failure evidence showing `React is not defined`, record that the root cause is JSX runtime drift in `packages/cli/src/render/dom-host.tsx` and `packages/cli/src/render/button-frame.tsx`, and make `26-04-PLAN.md` the explicit rerun path. Keep both docs synchronized so the blocker history stays truthful even after the runtime seam is corrected.
</action>
<verify>
Run `test -f .planning/phases/26-browser-deck-react-shell-polish/26-UAT.md && test -f .planning/phases/26-browser-deck-react-shell-polish/26-VERIFICATION.md && rg -n "26-04-PLAN.md|React is not defined|root_cause|rerun" .planning/phases/26-browser-deck-react-shell-polish/26-UAT.md .planning/phases/26-browser-deck-react-shell-polish/26-VERIFICATION.md`.
</verify>
<done>
The Phase 26 blocker remains documented and both verification artifacts point the rerun directly at this JSX-runtime closure plan.
</done>
</task>
