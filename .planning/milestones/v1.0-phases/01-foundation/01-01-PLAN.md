---
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.json
  - packages/sireno-deck/package.json
  - packages/sireno-deck/tsconfig.json
  - packages/sireno-deck/tsdown.config.ts
  - packages/sireno-deck/vitest.config.ts
  - packages/sireno-deck/src/core/schemas.ts
  - packages/sireno-deck/src/config/loader.ts
  - packages/sireno-deck/src/util/errors.ts
  - packages/sireno-deck/src/cli/index.ts
  - packages/sireno-deck/src/cli/commands/start.ts
  - packages/sireno-deck/src/cli/commands/stop.ts
  - packages/sireno-deck/src/cli/commands/status.ts
  - packages/sireno-deck/src/util/logger.ts
  - packages/sireno-deck/tsconfig.json
  - packages/sireno-deck/package.json
autonomous: true
single_layer_justified: false
objective: "User can run `sireno --help` to see available commands, define a `config.yml` with complete schema, and see the CLI load and validate it with colored errors on invalid input"
must_haves:
  truths:
    - "pnpm install succeeds with exit code 0"
    - "tsdown build produces packages/sireno-deck/dist/cli.js"
    - "sireno --help prints 'start', 'stop', 'status' in command list"
    - "Valid config.yml in CWD parses into typed SirenoConfig"
    - "Invalid config.yml prints colored error with file path, line number, and suggestion"
    - "vs test --run passes with exit code 0"
  artifacts:
    - packages/sireno-deck/src/core/schemas.ts
    - packages/sireno-deck/src/config/loader.ts
    - packages/sireno-deck/src/cli/index.ts
    - packages/sireno-deck/src/util/errors.ts
    - packages/sireno-deck/src/util/logger.ts
  key_links:
    - "packages/sireno-deck/src/cli/index.ts imports yargs and registers commands"
    - "packages/sireno-deck/src/config/loader.ts imports schemas from core/schemas"
    - "packages/sireno-deck/src/util/errors.ts is imported by config/loader"
---

# Plan 01-01: Project scaffold, config loading, and CLI skeleton

<objective>
Set up the pnpm monorepo, build tooling, and the core CLI binary with full config schema and validation. After this plan, the user can install dependencies, build the CLI, run `sireno --help` to see available commands, and have the config loading pipe work end-to-end with beautiful error messages.
</objective>

## Tasks

<task id="01-01-01">
<title>Initialize pnpm monorepo with root workspace config</title>
<files>
- package.json
- pnpm-workspace.yaml
- .gitignore
- .npmrc
</files>
<action>
Create the root `package.json` with:
- `"type": "module"`
- `"packageManager": "pnpm@10.x"` 
- `"private": true`
- `"scripts": { "build": "pnpm -r build", "dev": "pnpm -r dev", "lint": "oxlint packages/", "format": "oxfmt packages/", "test": "vitest run" }`

Create `pnpm-workspace.yaml` with:
```yaml
packages:
  - "packages/*"
  - "builtin-addons/*"
```

Create `.npmrc` with `shamefully-hoist=true` and `strict-peer-dependencies=false`.

Update existing `.gitignore` to include `node_modules/`, `dist/`, and `.tsdown/`.
</action>
<verify>
ls package.json pnpm-workspace.yaml .npmrc
grep '"private": true' package.json
grep '"packages/*"' pnpm-workspace.yaml
</verify>
<done>
Root config files exist with correct workspace declaration and ESM mode
</done>
</task>

<task id="01-01-02">
<title>Set up packages/sireno-deck CLI binary package</title>
<files>
- packages/sireno-deck/package.json
- packages/sireno-deck/tsconfig.json
- packages/sireno-deck/tsdown.config.ts
- packages/sireno-deck/vitest.config.ts
- packages/sireno-deck/src/cli/index.ts
</files>
<action>
Create `packages/sireno-deck/package.json`:
```json
{
  "name": "sireno-deck",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "sireno": "./dist/cli.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "yargs": "^18.0.0",
    "js-yaml": "^4.1.0",
    "zod": "^3.24.0",
    "pino": "^9.0.0"
  },
  "devDependencies": {
    "tsdown": "^0.22.0",
    "vitest": "^3.0.0",
    "typescript": "~5.7.0",
    "oxlint": "latest",
    "oxfmt": "latest",
    "@types/yargs": "^18.0.0",
    "@types/js-yaml": "^4.0.0",
    "pino-pretty": "^13.0.0"
  }
}
```

Create `packages/sireno-deck/tsconfig.json` with strict mode:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "dist",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src"]
}
```

Create `packages/sireno-deck/tsdown.config.ts`:
```ts
import { defineConfig } from "tsdown"

export default defineConfig({
  entry: ["./src/cli/index.ts"],
  format: "esm",
  target: "node20",
  clean: true,
  dts: true,
  bundle: false,
})
```

Create `packages/sireno-deck/vitest.config.ts`:
```ts
import { defineConfig } from "vitest/config"

export default defineConfig({
  test: {
    globals: true,
    environment: "node",
    include: ["src/**/*.test.ts"],
  },
})
```

Create minimal `packages/sireno-deck/src/cli/index.ts`:
```ts
#!/usr/bin/env node
import yargs from "yargs"
import { hideBin } from "yargs/helpers"

const cli = () => {
  yargs(hideBin(process.argv))
    .scriptName("sireno")
    .usage("$0 <command> [options]")
    .command("start", "Start the sireno-deck daemon", () => {}, () => {
      console.log("start command — not yet implemented")
    })
    .command("stop", "Stop the running daemon", () => {}, () => {
      console.log("stop command — not yet implemented")
    })
    .command("status", "Check daemon status", () => {}, () => {
      console.log("status command — not yet implemented")
    })
    .demandCommand(1, "Run $0 --help to see available commands")
    .strict()
    .help()
    .parse()
}

cli()
```
</action>
<verify>
node -e "const p=require('fs').readFileSync('packages/sireno-deck/package.json','utf8');const j=JSON.parse(p);console.log(j.bin.sireno === './dist/cli.js'?'bin ok':'bin wrong')"
grep '"strict": true' packages/sireno-deck/tsconfig.json
grep 'defineConfig' packages/sireno-deck/tsdown.config.ts
</verify>
<done>
CLI binary package exists with tsconfig, tsdown config, vitest config, and a minimal yargs entry point
</done>
</task>

<task id="01-01-03">
<title>Define full forward-looking zod schema</title>
<files>
- packages/sireno-deck/src/core/schemas.ts
</files>
<action>
Create `packages/sireno-deck/src/core/schemas.ts` with the complete forward-looking config schema definition. Use zod with `.strict()` at the top level. Group by domain:

```ts
import { z } from "zod"

export const ThemeSchema = z.object({
  name: z.string().min(1),
  background: z.string().optional(),
  accent: z.string().optional(),
  primary: z.string().optional(),
})

export const ButtonInstanceSchema = z.object({
  type: z.string().min(1),
  position: z.number().int().min(0).max(31),
  label: z.string().optional(),
  icon: z.string().optional(),
  config: z.record(z.unknown()).optional(),
})

export const DeckSchema = z.object({
  id: z.string().min(1),
  name: z.string().optional(),
  buttons: z.array(ButtonInstanceSchema),
})

export const AddonSchema = z.object({
  name: z.string().min(1),
  enabled: z.boolean().default(true),
  source: z.enum(["local", "npm"]).default("local"),
  path: z.string().optional(),
})

export const LoggingSchema = z.object({
  level: z.enum(["debug", "info", "warn", "error"]).default("info"),
  verbose: z.boolean().default(false),
})

export const SirenoConfigSchema = z.object({
  device: z.object({
    model: z.string().optional(),
    path: z.string().optional(),
  }).optional(),
  theme: z.string().default("dark"),
  decks: z.record(DeckSchema).optional(),
  addons: z.array(AddonSchema).default([]),
  logging: LoggingSchema.default({}),
}).strict()

export type SirenoConfig = z.infer<typeof SirenoConfigSchema>
```

Also export a `validateConfig` function that wraps parse with typed error:
```ts
export class ConfigValidationError extends Error {
  constructor(
    message: string,
    public readonly filePath?: string,
    public readonly lineNumber?: number,
  ) {
    super(message)
    this.name = "ConfigValidationError"
  }
}

export function validateConfig(data: unknown): SirenoConfig {
  const result = SirenoConfigSchema.safeParse(data)
  if (!result.success) {
    const firstIssue = result.error.issues[0]
    throw new ConfigValidationError(
      firstIssue.message,
      undefined,
      firstIssue.path.length > 0 ? undefined : undefined,
    )
  }
  return result.data
}
```
</action>
<verify>
grep "export const SirenoConfigSchema" packages/sireno-deck/src/core/schemas.ts
grep ".strict()" packages/sireno-deck/src/core/schemas.ts
grep "validateConfig" packages/sireno-deck/src/core/schemas.ts
</verify>
<done>
Full zod schema exists with all top-level groups (device, theme, decks, addons, logging), strict mode, and validated export
</done>
</task>

<task id="01-01-04">
<title>Implement config loader with CWD + XDG discovery</title>
<files>
- packages/sireno-deck/src/config/loader.ts
- packages/sireno-deck/src/config/loader.test.ts
</files>
<action>
Create `packages/sireno-deck/src/config/loader.ts`:

```ts
import { readFileSync, existsSync } from "node:fs"
import { homedir } from "node:os"
import { join } from "node:path"
import yaml from "js-yaml"
import { validateConfig, type SirenoConfig, ConfigValidationError } from "../core/schemas.js"

const CONFIG_FILENAME = "config.yml"

function findConfigPath(customPath?: string): string | undefined {
  if (customPath) return customPath
  if (existsSync(join(process.cwd(), CONFIG_FILENAME))) {
    return join(process.cwd(), CONFIG_FILENAME)
  }
  const xdgConfig = join(homedir(), ".config", "sireno-deck", CONFIG_FILENAME)
  if (existsSync(xdgConfig)) return xdgConfig
  return undefined
}

export function loadConfig(configPath?: string): SirenoConfig {
  const foundPath = findConfigPath(configPath)
  if (!foundPath) {
    throw new ConfigValidationError(
      "No config.yml found. Create one in the current directory or ~/.config/sireno-deck/config.yml",
    )
  }
  const raw = readFileSync(foundPath, "utf-8")
  let parsed: unknown
  try {
    parsed = yaml.load(raw)
  } catch (err) {
    const msg = err instanceof Error ? err.message : String(err)
    throw new ConfigValidationError(`YAML parse error: ${msg}`, foundPath)
  }
  return validateConfig(parsed)
}
```

Create `packages/sireno-deck/src/config/loader.test.ts` with tests for:
- Loading valid config.yml from CWD
- Throwing ConfigValidationError on invalid YAML
- Throwing ConfigValidationError on missing required fields
- Throwing ConfigValidationError on unknown keys (strict mode)
- XDG fallback when no CWD config exists
</action>
<verify>
grep "export function loadConfig" packages/sireno-deck/src/config/loader.ts
grep "YAML parse error" packages/sireno-deck/src/config/loader.ts
ls packages/sireno-deck/src/config/loader.test.ts
</verify>
<done>
Config loader exists with CWD → XDG discovery, js-yaml parsing, zod validation, and tests
</done>
</task>

<task id="01-01-05">
<title>Implement colored error formatting utility</title>
<files>
- packages/sireno-deck/src/util/errors.ts
- packages/sireno-deck/src/util/errors.test.ts
</files>
<action>
Create `packages/sireno-deck/src/util/errors.ts`:

```ts
import { ConfigValidationError } from "../core/schemas.js"

const RED = "\x1b[31m"
const YELLOW = "\x1b[33m"
const CYAN = "\x1b[36m"
const RESET = "\x1b[0m"
const BOLD = "\x1b[1m"

export function formatConfigError(error: ConfigValidationError): string {
  let msg = `\n${BOLD}${RED}config error${RESET}\n\n`
  if (error.filePath) {
    msg += `  ${CYAN}file:${RESET} ${error.filePath}\n`
  }
  if (error.lineNumber !== undefined) {
    msg += `  ${CYAN}line:${RESET} ${error.lineNumber}\n`
  }
  msg += `  ${YELLOW}problem:${RESET} ${error.message}\n`
  msg += `\n  Tip: Check your config.yml syntax at ${error.filePath || "the config file"}.\n`
  return msg
}
```

Create `packages/sireno-deck/src/util/errors.test.ts` testing formatConfigError output includes "config error", file path, and problem message.
</action>
<verify>
grep "formatConfigError" packages/sireno-deck/src/util/errors.ts
ls packages/sireno-deck/src/util/errors.test.ts
</verify>
<done>
Error formatting utility exists with colored output and tests
</done>
</task>

<task id="01-01-06">
<title>Set up shared pino logger</title>
<files>
- packages/sireno-deck/src/util/logger.ts
</files>
<action>
Create `packages/sireno-deck/src/util/logger.ts`:

```ts
import pino from "pino"

export function createLogger(options?: { level?: string; verbose?: boolean }): pino.Logger {
  return pino({
    level: options?.verbose ? "debug" : options?.level ?? "info",
    transport: process.stdout.isTTY
      ? { target: "pino-pretty", options: { colorize: true } }
      : undefined,
  })
}
```
</action>
<verify>
grep "createLogger" packages/sireno-deck/src/util/logger.ts
grep "pino" packages/sireno-deck/src/util/logger.ts
</verify>
<done>
Logger utility exists with pino and TTY detection for pino-pretty
</done>
</task>

<task id="01-01-07">
<title>Wire config loading + error formatting into CLI entry point</title>
<files>
- packages/sireno-deck/src/cli/index.ts
</files>
<action>
Update `packages/sireno-deck/src/cli/index.ts` to:
1. Load config on startup for commands that need it
2. Wrap config loading with colored error formatting
3. Make yargs --help and --version work
4. Pass config and logger to command handlers via shared state

```ts
#!/usr/bin/env node
import yargs from "yargs"
import { hideBin } from "yargs/helpers"
import { loadConfig } from "../config/loader.js"
import { formatConfigError } from "../util/errors.js"
import { createLogger } from "../util/logger.js"
import { ConfigValidationError } from "../core/schemas.js"
import type pino from "pino"

export interface CliContext {
  logger: pino.Logger
  config?: ReturnType<typeof loadConfig>
}

const cli = () => {
  const logger = createLogger({ verbose: process.argv.includes("--verbose") })
  const context: CliContext = { logger }

  yargs(hideBin(process.argv))
    .scriptName("sireno")
    .usage("$0 <command> [options]")
    .option("verbose", {
      alias: "v",
      type: "boolean",
      description: "Enable verbose debug logging",
      default: false,
    })
    .option("config", {
      type: "string",
      description: "Path to config.yml",
    })
    .command("start", "Start the sireno-deck daemon", () => {}, async (argv) => {
      try {
        context.config = loadConfig(argv.config as string | undefined)
        logger.info("config loaded successfully")
        logger.info({ config: context.config }, "starting daemon")
        // Phase 2 will implement actual daemon behavior
        process.exit(0)
      } catch (err) {
        if (err instanceof ConfigValidationError) {
          console.error(formatConfigError(err))
          process.exit(1)
        }
        throw err
      }
    })
    .command("stop", "Stop the running daemon", () => {}, () => {
      console.log("stop command — not yet implemented")
    })
    .command("status", "Check daemon status", () => {}, () => {
      console.log("status command — not yet implemented")
    })
    .demandCommand(1, "Run $0 --help to see available commands")
    .strict()
    .help()
    .alias("help", "h")
    .version()
    .alias("version", "V")
    .parse()
}

cli()
```
</action>
<verify>
node -e "
const content = require('fs').readFileSync('packages/sireno-deck/src/cli/index.ts', 'utf-8');
console.log(content.includes('loadConfig') ? 'loadConfig imported OK' : 'missing loadConfig');
console.log(content.includes('formatConfigError') ? 'formatConfigError imported OK' : 'missing formatConfigError');
"
</verify>
<done>
CLI entry point wires config loading with colored error formatting, --verbose flag, and --config flag
</done>
</task>
</objective>

</plan>
