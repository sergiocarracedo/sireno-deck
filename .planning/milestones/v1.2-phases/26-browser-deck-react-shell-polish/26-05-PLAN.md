---
wave: 5
depends_on:
  - 26-04-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/render/startup-placeholder.ts
  - packages/cli/src/render/startup-placeholder.test.ts
  - .planning/phases/26-browser-deck-react-shell-polish/26-UAT-rerun-2026-05-27.md
  - .planning/phases/26-browser-deck-react-shell-polish/26-VERIFICATION.md
autonomous: true
single_layer_justified: true
objective: "Close the remaining Phase 26 rerun gap where the startup placeholder still composites shell/card/label artwork on top of `logoFull.png` instead of showing only the full-deck logo image during pre-browser startup."
must_haves:
  truths:
    - "`packages/cli/src/render/startup-placeholder.ts` keeps the existing pre-browser deck-wide crop seam but no longer draws extra shell/card/`STARTING` overlay elements over `logoFull.png`."
    - "Focused placeholder coverage proves the startup placeholder still returns one raw key buffer per key and still produces a deck-wide non-repeating image treatment after the simplification."
    - "The rerun UAT/verification artifacts preserve the remaining visual-gap evidence and point the next rerun directly at `26-05-PLAN.md`."
  artifacts:
    - packages/cli/src/render/startup-placeholder.ts
    - packages/cli/src/render/startup-placeholder.test.ts
    - .planning/phases/26-browser-deck-react-shell-polish/26-UAT-rerun-2026-05-27.md
    - .planning/phases/26-browser-deck-react-shell-polish/26-VERIFICATION.md
  key_links:
    - "`packages/cli/src/cli/commands/start.ts` should remain unchanged as the owner of the pre-browser handoff seam; the visual fix belongs in `packages/cli/src/render/startup-placeholder.ts`."
    - "The placeholder tests must keep proving the deck-wide crop seam while avoiding assertions that depend on the removed overlay artwork."
---

# Plan 26-05: Remove Extra Overlay Chrome From The Startup Logo Placeholder

<objective>
Close the remaining rerun UAT gap at the actual visual source: the startup placeholder no longer crashes, but it still draws extra shell/card/label artwork that the user explicitly does not want. This plan keeps the pre-browser ownership and deck-wide crop seam intact while simplifying the placeholder so startup shows only the full-deck `logoFull.png` image treatment.
</objective>

## Tasks

<task id="26-05-01">
<title>Simplify the startup placeholder composition to the full-deck logo image only</title>
<files>
- packages/cli/src/render/startup-placeholder.ts
- packages/cli/src/render/startup-placeholder.test.ts
</files>
<action>
Refactor `packages/cli/src/render/startup-placeholder.ts` so the deck-wide placeholder buffer is composed from the shipped `logoFull.png` image on the startup background without adding the extra shell/card/accent/`STARTING` overlay artwork. Preserve the current ownership boundary (pre-browser raw buffer generation) and the deck-wide crop-back-to-keys behavior. Then keep `packages/cli/src/render/startup-placeholder.test.ts` focused on the real seam: one raw key buffer per key and non-repeating deck-wide output, but no assertions or helper code that depend on the removed overlay composition.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/startup-placeholder.test.ts src/cli/commands/start.test.ts -t "startup placeholder|writes placeholder buffers before the first real render and clears the pending state after handoff|clears the placeholder if the first real render fails"`.
</verify>
<done>
The startup placeholder still writes one deck-wide logo-based placeholder across the hardware deck and still hands off on the original seam, but the extra overlay elements are gone.
</done>
</task>

<task id="26-05-02">
<title>Keep the rerun visual-gap evidence linked to the placeholder simplification plan</title>
<files>
- .planning/phases/26-browser-deck-react-shell-polish/26-UAT-rerun-2026-05-27.md
- .planning/phases/26-browser-deck-react-shell-polish/26-VERIFICATION.md
</files>
<action>
Update the rerun UAT and verification artifacts so they preserve the user's report that startup still shows other elements beyond `logoFull.png`, record that the root cause is the extra overlay composition inside `packages/cli/src/render/startup-placeholder.ts`, and make `26-05-PLAN.md` the explicit next rerun path.
</action>
<verify>
Run `test -f .planning/phases/26-browser-deck-react-shell-polish/26-UAT-rerun-2026-05-27.md && test -f .planning/phases/26-browser-deck-react-shell-polish/26-VERIFICATION.md && rg -n "26-05-PLAN.md|logoFull|STARTING|root_cause|rerun" .planning/phases/26-browser-deck-react-shell-polish/26-UAT-rerun-2026-05-27.md .planning/phases/26-browser-deck-react-shell-polish/26-VERIFICATION.md`.
</verify>
<done>
The remaining visual gap stays documented and both rerun artifacts point the next execution pass at the placeholder-simplification plan.
</done>
</task>
