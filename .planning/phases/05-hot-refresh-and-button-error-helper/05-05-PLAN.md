---
wave: 4
depends_on:
  - 05-03-PLAN.md
  - 05-04-PLAN.md
gap_closure: true
files_modified:
  - .planning/phases/05-hot-refresh-and-button-error-helper/05-UAT.md
  - packages/cli/fixtures/phase-5/README.md
  - .planning/phases/05-hot-refresh-and-button-error-helper/05-VERIFICATION.md
autonomous: true
single_layer_justified: true
objective: "Close the Phase 5 verification-script gap by making the apiVersion-mismatch fixture explicitly expect startup exit with a clear addon apiVersion error, then preserve that rerun truth in the UAT and verification artifacts so the correct product behavior is no longer misread as a failure."
must_haves:
  truths:
    - "Phase 5 UAT wording now states explicitly that `config.api-version-mismatch.yml` is expected to exit during startup with an addon `apiVersion` error before any button helper can render."
    - "The Phase 5 fixture README and verification artifact match that same startup-exit contract so reruns do not reopen the same wording ambiguity."
    - "The original UAT report and diagnosed root cause remain inspectable, but the rerun path points directly at this wording-closure plan instead of implying a runtime bug."
  artifacts:
    - .planning/phases/05-hot-refresh-and-button-error-helper/05-UAT.md
    - packages/cli/fixtures/phase-5/README.md
    - .planning/phases/05-hot-refresh-and-button-error-helper/05-VERIFICATION.md
  key_links:
    - "`packages/cli/src/cli/commands/start.ts` already implements exit-on-apiVersion-mismatch, so this closure is a verification/docs truth sync rather than a code-path change."
    - "The fixture README, UAT test wording, and verification summary should all describe the same expected startup-exit outcome for the mismatch fixture."
---

# Plan 05-05: Close apiVersion Mismatch UAT Wording Gap

<objective>
Close the second Phase 5 UAT issue honestly: the product already behaves correctly for addon apiVersion mismatch, but the verification materials under-specified the expected startup-exit outcome. This plan tightens those docs and artifacts so future reruns test the real contract instead of treating the correct exit path as a failure.
</objective>

## Tasks

<task id="05-05-01">
<title>Rewrite the mismatch-fixture expectation across UAT and fixture docs</title>
<files>
- .planning/phases/05-hot-refresh-and-button-error-helper/05-UAT.md
- packages/cli/fixtures/phase-5/README.md
</files>
<action>
Update the Phase 5 UAT Test 3 wording and the corresponding gap entry so they say plainly that `config.api-version-mismatch.yml` is supposed to exit during startup with a clear addon `apiVersion` error before any runtime/button helper appears. Keep the original user report and diagnosed root cause intact, but add an explicit rerun note pointing to `05-05-PLAN.md`. Then tighten `packages/cli/fixtures/phase-5/README.md` so its mismatch-fixture note uses the same explicit startup-exit language.
</action>
<verify>
Read `.planning/phases/05-hot-refresh-and-button-error-helper/05-UAT.md` and `packages/cli/fixtures/phase-5/README.md` and confirm both now describe startup exit as the expected mismatch-fixture outcome and mention `05-05-PLAN.md` as the rerun path.
</verify>
<done>
The mismatch fixture wording is explicit and consistent across UAT and fixture docs, so the correct startup exit is no longer ambiguous during rerun.
</done>
</task>

<task id="05-05-02">
<title>Keep the Phase 5 verification artifact aligned with the clarified mismatch contract</title>
<files>
- .planning/phases/05-hot-refresh-and-button-error-helper/05-VERIFICATION.md
- .planning/phases/05-hot-refresh-and-button-error-helper/05-UAT.md
</files>
<action>
Update `05-VERIFICATION.md` only where needed so its integration/summary wording stays aligned with the clarified mismatch-fixture contract and explicitly notes that the startup-exit expectation was a UAT wording gap, not a failed runtime boundary. Keep the verification results truthful and preserve the blocker history by cross-linking back to the diagnosed UAT gap and `05-05-PLAN.md` rerun path.
</action>
<verify>
Run `rg -n "apiVersion|startup exit|05-05-PLAN.md|UAT wording" .planning/phases/05-hot-refresh-and-button-error-helper/05-UAT.md .planning/phases/05-hot-refresh-and-button-error-helper/05-VERIFICATION.md packages/cli/fixtures/phase-5/README.md` and confirm the clarified contract appears consistently across all three files.
</verify>
<done>
Phase 5 verification, UAT, and fixture docs all describe the same expected mismatch-fixture behavior, and the wording-only blocker remains inspectable without implying a code regression.
</done>
</task>
