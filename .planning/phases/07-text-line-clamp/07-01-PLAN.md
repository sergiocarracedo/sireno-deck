---
phase: 07
plan: 01
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/ui/primitives/Text.tsx
  - packages/cli/src/ui/primitives/__tests__/Text.test.tsx
autonomous: true
single_layer_justified: false
objective: Extend TextFit type with object form for line-clamp and implement line-clamp rendering using Tailwind utilities
---

# Plan 07-01: Line-Clamp Type + Rendering

## Objective

Extend the `TextFit` type in `Text.tsx` to accept an object form `{ type: 'line-clamp', lines: number, reserveSpace?: boolean }` alongside the existing string aliases. When the object form is provided, render with Tailwind's `line-clamp-N` utility class.

## Tasks

<task>
<file>packages/cli/src/ui/primitives/Text.tsx</file>
<action>Extend `TextFit` type to include the object form. Keep all existing string members for backward compatibility.</action>
<verify>TypeScript compiles with the new union type. No errors in Text.tsx.</verify>
<done>TextFit = 'ellipsis' | 'shrink' | 'wrap' | 'hidden' | { type: 'line-clamp'; lines: number; reserveSpace?: boolean }</done>
</task>

<task>
<file>packages/cli/src/ui/primitives/Text.tsx</file>
<action>Add a `resolveFit()` helper function that normalizes the fit prop: returns `{ kind: 'mode', mode: string } | { kind: 'line-clamp', lines: number, reserveSpace: boolean }`.</action>
<verify>Helper function correctly narrows union and extracts lines/reserveSpace values.</verify>
<done>resolveFit() returns discriminated union for type-safe handling downstream.</done>
</task>

<task>
<file>packages/cli/src/ui/primitives/Text.tsx</file>
<action>Update the component body to use resolveFit(). For line-clamp mode, apply `line-clamp-${lines}` Tailwind class. For other modes, use existing fitModesClasses lookup.</action>
<verify>Existing fit="ellipsis" usage still works. New fit={{ type: 'line-clamp', lines: 2 }} renders with line-clamp-2 class.</verify>
<done>Line-clamp rendering works for lines 1-6.</done>
</task>

<task>
<file>packages/cli/src/ui/primitives/__tests__/Text.test.tsx</file>
<action>Create test file with cases: (a) backward-compat string fit modes, (b) line-clamp with lines=2 renders line-clamp-2 class, (c) line-clamp with lines=6 renders line-clamp-6 class, (d) invalid lines fallback behavior.</action>
<verify>All tests pass. Coverage for both string and object fit prop forms.</verify>
<done>Text component has dedicated test file with 4+ test cases.</done>
</task>

## Must-Haves

- [ ] `TextFit` type accepts both string and object forms
- [ ] `fit="ellipsis"` still works (backward-compatible)
- [ ] `fit={{ type: 'line-clamp', lines: 2 }}` renders with `line-clamp-2` class
- [ ] Tests cover both string and object forms
- [ ] No regression on existing Text usage (Chip, weather, media, value-display, brightness)
