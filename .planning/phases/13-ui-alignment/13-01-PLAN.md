---
phase: 13-ui-alignment
plan: 01
wave: 1
depends_on: []
autonomous: true
objective: |
  Replace all --color-* token values in themes/default/theme.css and themes/light/theme.css
  with the exact legacy hex values from /works/opensource/sireno-deck/.../theme.css.
  Add --color-frame, --color-primary, --color-success, --color-danger.
  Replace font families with IBM Plex Sans/Mono. Add @font-face rules for IBM Plex fonts.
  Update frontend/index.css color-scheme back to dark light (was changed to only dark).
files_modified:
  - packages/cli/src/themes/default/theme.css
  - packages/cli/src/themes/light/theme.css
  - packages/cli/frontend/src/index.css
  - packages/cli/frontend-emulator/src/index.css (if exists)
---

# Phase 13 Plan 01: theme CSS tokens + fonts

## Tasks

### Task 1: Update default/theme.css with legacy colors + fonts

- `--color-bg: #2e3540` (was #0a0a0a)
- `--color-fg: #eef2f7` (was #fafafa)
- `--color-accent: #7dd3fc` (was #38bdf8)
- `--color-ring: #eef2f7` (was #fafafa)
- `--color-tap: #7dd3fc` (was #38bdf8)
- `--color-muted: adjust to complement #2e3540 (use #8a9bb5 or closest)
- `--color-bar: #1a1f28` (adjust to complement #2e3540)
- `--color-bar-accent: #7dd3fc`
- `--color-frame: #53738B` (NEW)
- `--color-primary: #7dd3fc` (NEW, alias for accent)
- `--color-success: #34d399` (NEW)
- `--color-danger: #FFB4AB` (NEW)
- `--color-foreground-contrast: #000000` (NEW)
- `--font-mono: "IBM Plex Mono", ui-monospace, monospace`
- `--font-main: "IBM Plex Sans", ui-sans-serif, system-ui, sans-serif`
- `--font-aux: "IBM Plex Sans", ui-sans-serif, system-ui, sans-serif`

### Task 2: Add @font-face rules for IBM Plex Sans/Mono

Add at the top of default/theme.css (before @theme):
```css
@font-face {
  font-family: "IBM Plex Sans";
  font-style: normal;
  font-weight: 300;
  src: url("./assets/IBM_Plex_Sans/static/IBMPlexSans-Regular.ttf") format("truetype");
}
@font-face {
  font-family: "IBM Plex Sans";
  font-style: normal;
  font-weight: 700;
  src: url("./assets/IBM_Plex_Sans/static/IBMPlexSans-SemiBold.ttf") format("truetype");
}
@font-face {
  font-family: "IBM Plex Mono";
  font-style: normal;
  font-weight: 700;
  src: url("./assets/IBM_Plex_Mono/IBMPlexMono-Bold.ttf") format("truetype");
}
```
These are in the legacy theme CSS. Copy verbatim.

### Task 3: Update light/theme.css

Same color replacements for the light variant:
- `--color-bg: #fafafa` → `#eef2f7` (legacy in light mode)
- `--color-fg: #0a0a0a` → `#2e3540`
- `--color-accent: #0284c7` → `#7dd3fc`
- `--color-frame: #53738B`
- Same font family changes.

### Task 4: Revert frontend/index.css color-scheme

Change `color-scheme: only dark` back to `color-scheme: dark light` (the default theme no longer has a light media query, but the light theme still does).

### Task 5: Verify

- `pnpm test` passes.
- agent-browser check: `getComputedStyle(body).backgroundColor` returns `rgb(46, 53, 64)` (#2e3540).
- agent-browser check: button `getComputedStyle(borderColor)` returns `rgb(83, 115, 139)` (#53738B).
