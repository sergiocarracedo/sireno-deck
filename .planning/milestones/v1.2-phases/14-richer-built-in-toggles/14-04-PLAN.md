---
wave: 4
depends_on:
  - 14-03
gap_closure: true
files_modified:
  - builtin-addons/core-buttons/src/index.ts
  - packages/cli/src/deck/runtime.ts
  - packages/cli/src/deck/runtime.test.ts
  - packages/cli/src/cli/commands/start.ts
  - .planning/phases/14-richer-built-in-toggles/14-UAT.md
autonomous: true
single_layer_justified: true
objective: "Close the Phase 14 UAT gap where the real `get-set` review fixture can stay visually stuck in the pending state by reproducing the initial render/write race, sequencing the first authoritative render so settled truth cannot be overwritten, and proving the fix on the real fixture path."
must_haves:
  truths:
    - "The Phase 14 `get-set` review fixture no longer remains visually stuck in the pending state on the real CLI/device surface once the first authoritative read succeeds."
    - "The initial settled authoritative render cannot be overwritten by a stale deck-wide pending write during startup."
    - "Focused tests cover the real render/write ordering that caused the UAT failure instead of only closure-local state transitions."
  artifacts:
    - builtin-addons/core-buttons/src/index.ts
    - packages/cli/src/deck/runtime.ts
    - packages/cli/src/deck/runtime.test.ts
    - packages/cli/src/cli/commands/start.ts
    - .planning/phases/14-richer-built-in-toggles/14-UAT.md
  key_links:
    - "`packages/cli/src/deck/runtime.ts` and/or `packages/cli/src/cli/commands/start.ts` must own the startup render sequencing fix because the UAT failure appears on the real device surface, not only inside the addon closure state."
    - "`packages/cli/src/deck/runtime.test.ts` must reproduce the startup write-order race and prove the settled authoritative render wins."
---

# Plan 14-04: Close The Get-Set Pending-State UAT Gap

<objective>
Fix the specific UAT regression discovered during Phase 14 verification: the real `get-set` fixture can remain visually indeterminate even though the command read path should settle to a known state. Reproduce the startup ordering problem, fix it at the runtime/render boundary, and update the UAT record with the diagnosed closure.
</objective>

## Tasks

<task id="14-04-01">
<title>Reproduce and fix the startup render race on the get-set review path</title>
<files>
- builtin-addons/core-buttons/src/index.ts
- packages/cli/src/deck/runtime.ts
- packages/cli/src/deck/runtime.test.ts
- packages/cli/src/cli/commands/start.ts
</files>
<action>
Add a focused reproduction for the startup write ordering that left the `get-set` fixture visually stuck in pending on the real surface. Then implement the smallest correct fix so that once the first authoritative read resolves, the settled state render cannot be overwritten by the stale startup deck render. Keep the fix narrow to this race; do not redesign the broader runtime.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/deck/runtime.test.ts`.
</verify>
<done>
The real startup path no longer leaves `get-set` toggles visually stuck in pending after a successful first read.
</done>
</task>

<task id="14-04-02">
<title>Update UAT with the diagnosed fix outcome</title>
<files>
- .planning/phases/14-richer-built-in-toggles/14-UAT.md
</files>
<action>
Update the existing Phase 14 UAT record to reflect the diagnosed root cause and the closure path for the fixed `get-set` pending-state issue. Keep the UAT focused on what failed, why it failed, and that the review fixture should be rerun after the gap closure lands.
</action>
<verify>
Run `test -f .planning/phases/14-richer-built-in-toggles/14-UAT.md`.
</verify>
<done>
The UAT record clearly links the failed review to its root cause and the gap-closure plan.
</done>
</task>
