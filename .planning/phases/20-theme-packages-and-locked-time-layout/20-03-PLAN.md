---
wave: 3
depends_on:
  - 20-01-PLAN.md
  - 20-02-PLAN.md
files_modified:
  - packages/cli/src/deck/runtime.ts
  - packages/cli/src/deck/runtime.test.ts
  - packages/cli/src/builtin-addons/date-time/index.ts
  - packages/cli/src/builtin-addons/date-time/index.test.ts
  - packages/cli/fixtures/phase-20/config.locked-time-layout.yml
  - .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md
  - .planning/phases/20-theme-packages-and-locked-time-layout/20-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
single_layer_justified: false
objective: "When no explicit `session.locked_deck` is configured, the locked-session fallback shows a centered five-button `[H][H][:][M][M]` time layout on buttons `5..9`, while explicit locked decks still remain authoritative and restorable."
must_haves:
  truths:
    - "The implicit locked-session fallback no longer renders as a single date-time button at position 0"
    - "The core-owned fallback renders a fixed centered `[H][H][:][M][M]` layout on buttons `5..9` only when `session.locked_deck` is absent"
    - "Configured locked decks still take precedence and unlock still restores the exact pre-lock navigation stack"
  artifacts:
    - packages/cli/src/deck/runtime.ts
    - packages/cli/src/deck/runtime.test.ts
    - packages/cli/fixtures/phase-20/config.locked-time-layout.yml
    - .planning/phases/20-theme-packages-and-locked-time-layout/20-VERIFICATION.md
  key_links:
    - "packages/cli/src/deck/runtime.ts keeps core ownership of lock-mode control flow while changing only the implicit fallback surface"
    - "packages/cli/src/deck/runtime.test.ts proves explicit `session.locked_deck` still wins and the unlock restore contract remains intact"
    - ".planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md and 20-VERIFICATION.md close the loop on the real centered time layout proof"
---

# Plan 20-03: Centered Locked Time Fallback

<objective>
Finish Phase 20 by changing the one remaining user-facing runtime behavior the phase scoped: the implicit locked fallback surface. This slice keeps lock-mode control flow and explicit `session.locked_deck` authority in core, but replaces the old single-button fallback with a centered five-button `[H][H][:][M][M]` time layout on the real browser/device path and closes the verification/state artifacts that must move with it.
</objective>

## Tasks

<task id="20-03-01">
<title>Replace the implicit locked fallback deck with a fixed centered five-button time surface</title>
<files>
- packages/cli/src/deck/runtime.ts
- packages/cli/src/deck/runtime.test.ts
- packages/cli/src/builtin-addons/date-time/index.ts
- packages/cli/src/builtin-addons/date-time/index.test.ts
</files>
<action>
Refactor `createImplicitLockedDeck()` so the core-owned fallback generates buttons `5..9` as `[H][H][:][M][M]` instead of a single `date-time` button at position 0. Reuse the bundled date-time formatting seam by introducing one narrow locked-fallback-specific renderer path in the bundled date-time addon for digit and colon tiles so the fallback still updates on a one-second cadence. Keep the implementation scoped to the implicit fallback only: explicit `session.locked_deck` behavior, lock navigation isolation, and exact unlock restore must remain unchanged.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/deck/runtime.test.ts src/builtin-addons/date-time/index.test.ts` and confirm the implicit lock tests assert positions `5..9` plus explicit locked-deck precedence.
</verify>
<done>
Lock mode without `session.locked_deck` renders the centered five-button time fallback, while configured locked decks and unlock restore behavior keep working exactly as before.
</done>
</task>

<task id="20-03-02">
<title>Ship the locked-layout fixture and verification artifacts as one consistency set</title>
<files>
- packages/cli/fixtures/phase-20/config.locked-time-layout.yml
- .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md
- .planning/phases/20-theme-packages-and-locked-time-layout/20-VERIFICATION.md
- .planning/ROADMAP.md
- .planning/STATE.md
</files>
<action>
Add a committed Phase 20 fixture that exercises the implicit locked fallback and makes the centered `HH:MM` row visually reviewable on the real device path. Update the UAT and verification docs so they explicitly describe the fixed `5..9` placement, the colon in its own center button, and the rule that explicit `session.locked_deck` still wins. Normalize roadmap/state verification notes at the same time so the repo does not repeat the earlier stale-UAT-gap metadata drift after reruns.
</action>
<verify>
Run `grep -n "5..9\|HH:MM\|locked_deck\|implicit" packages/cli/fixtures/phase-20/config.locked-time-layout.yml .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md .planning/phases/20-theme-packages-and-locked-time-layout/20-VERIFICATION.md .planning/STATE.md .planning/ROADMAP.md`.
</verify>
<done>
The locked fallback behavior is reviewable on the shipped path and all workflow/state artifacts describe the same post-change contract.
</done>
</task>
