---
wave: 4
depends_on:
  - 17-03
gap_closure: true
files_modified:
  - packages/cli/src/cli/commands/start.ts
  - packages/cli/src/cli/commands/start.test.ts
  - .planning/phases/17-custom-wrapper-primitives-with-addon/17-UAT.md
autonomous: true
single_layer_justified: false
objective: "Phase 17's real CLI/device render path honors explicit `full_surface` so the committed review fixture visibly bypasses the base card on the actual device surface."
must_haves:
  truths:
    - "The live CLI/device render path forwards `full_surface` into `renderTextImage()` instead of dropping it before image generation."
    - "The shipped Phase 17 review fixture can visibly bypass the base card on the real device path when `full_surface: true` is configured."
    - "Focused start-path coverage proves explicit `full_surface` suppresses wrapper compatibility without depending only on renderer-unit tests."
  artifacts:
    - packages/cli/src/cli/commands/start.ts
    - packages/cli/src/cli/commands/start.test.ts
    - .planning/phases/17-custom-wrapper-primitives-with-addon/17-UAT.md
  key_links:
    - "packages/cli/src/cli/commands/start.ts must pass `full_surface` through to `renderTextImage()` on both per-button and whole-deck render paths."
    - "packages/cli/src/cli/commands/start.test.ts must prove the start-path primitive resolution keeps wrapper compatibility for legacy buttons while explicit `full_surface` reaches the live render seam."
---

# Plan 17-04: Close The Full-Surface Device-Path Gap

<objective>
Deliver the gap-closure slice for Phase 17 by fixing the one upstream seam that makes UAT fail: the real CLI/device render path drops `full_surface` before image generation. This plan keeps the fix narrow and observable on the shipped path instead of widening the design around helper-vs-component debates that did not cause the reported regression.
</objective>

## Tasks

<task id="17-04-01">
<title>Forward explicit full-surface rendering on the shipped start path</title>
<files>
- packages/cli/src/cli/commands/start.ts
</files>
<action>
Update both render call sites in `packages/cli/src/cli/commands/start.ts` so the live CLI/device path passes `full_surface: description.full_surface` / `button.full_surface` into `renderTextImage()` alongside the existing wrapper/style primitive resolution. Keep the compatibility rule narrow: legacy `wrapper_id` still resolves to the shared wrapper when `full_surface` is absent, but explicit full-surface buttons must reach the renderer with `full_surface: true` so the base card is actually bypassed on-device.
</action>
<verify>
Confirm `packages/cli/src/cli/commands/start.ts` passes `full_surface` in both `renderTextImage({...})` calls.
</verify>
<done>
The live render seam no longer drops the explicit full-surface contract before image generation.
</done>
</task>

<task id="17-04-02">
<title>Prove the start-path fix with focused regression coverage</title>
<files>
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Add a focused regression test around the start-path render seam that proves explicit `full_surface` reaches the real render integration and suppresses wrapper compatibility there, not just inside `renderTextImage()` unit tests. Keep the test local to `start.ts` behavior so it fails if a future change drops `full_surface` again on the device path.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts`.
</verify>
<done>
There is one automated proof on the shipped start path for the exact UAT regression.
</done>
</task>

<task id="17-04-03">
<title>Reset the Phase 17 review path for rerun</title>
<files>
- .planning/phases/17-custom-wrapper-primitives-with-addon/17-UAT.md
</files>
<action>
Update the Phase 17 UAT artifact only as needed to reflect the diagnosed root cause and make the phase ready for rerun after execution. Preserve the user-reported failures and diagnosed root causes so the gap closure remains auditable.
</action>
<verify>
Confirm `.planning/phases/17-custom-wrapper-primitives-with-addon/17-UAT.md` contains both failed truths with `root_cause` and `affected_files` entries referencing the start-path seam.
</verify>
<done>
The gap closure is traceable from failed UAT behavior to the exact upstream fix seam.
</done>
</task>

<must_haves>
- The gap fix stays upstream and minimal: correct the start-path transport instead of redesigning the renderer model.
- The shipped Phase 17 fixture becomes rerunnable evidence for the full-surface escape hatch on the real device path.
- Future regressions that drop `full_surface` from the start path are caught by focused automated coverage.
</must_haves>
