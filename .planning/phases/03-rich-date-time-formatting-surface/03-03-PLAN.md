---
wave: 3
depends_on:
  - 03-01-PLAN.md
  - 03-02-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/render/theme-utilities.ts
  - packages/cli/src/builtin-addons/date-time/index.test.ts
  - packages/cli/fixtures/phase-22/config.emulator-demo.yml
  - .planning/phases/03-rich-date-time-formatting-surface/03-UAT.md
  - .planning/phases/03-rich-date-time-formatting-surface/03-VERIFICATION.md
autonomous: true
single_layer_justified: true
objective: "Close the Phase 3 UAT gap by making the main date-time review path use a truly larger shared size token than its outer shell and by widening the shared size ladder so adjacent text levels read more distinctly on real deck buttons."
must_haves:
  truths:
    - "The committed Phase 3 review path renders the main-deck time line with a genuinely larger shared size token than the surrounding date-time shell, so the UAT expectation matches the real visual contract."
    - "Shared `Text` keeps the existing size vocabulary (`xs`, `sm`, `md`, `lg`, `xl`, `2xl`) but its utility multipliers now create more evident visual separation between adjacent sizes."
    - "Focused date-time coverage proves the mounted rich date-time path against the corrected larger-size contract instead of only proving that size tags survive parsing."
    - "Phase 3 UAT and verification artifacts preserve the original visual-gap report, record the root cause, and point rerun directly at `03-03-PLAN.md`."
  artifacts:
    - packages/cli/src/render/theme-utilities.ts
    - packages/cli/src/builtin-addons/date-time/index.test.ts
    - packages/cli/fixtures/phase-22/config.emulator-demo.yml
    - .planning/phases/03-rich-date-time-formatting-surface/03-UAT.md
    - .planning/phases/03-rich-date-time-formatting-surface/03-VERIFICATION.md
  key_links:
    - "`packages/cli/src/builtin-addons/date-time/buttons/date-time.tsx` already sets the outer baseline with `Text size=\"xl\"`, so the review fixture must ask for a higher shared token than `xl` if the first line is supposed to look larger."
    - "`packages/cli/src/render/theme-utilities.ts` owns the shared size ladder, so making adjacent sizes more evident belongs there rather than in widget-local styling."
---

# Plan 03-03: Close The Phase 3 Size-Contrast UAT Gap

<objective>
Close the real Phase 3 visual mismatch instead of papering over it in prose. The current review path claims the first line is larger, but the fixture uses `<xl>` inside an outer `Text size="xl"` shell, so the button is rendering exactly what the code asks for: no size change. This closure slice keeps the shared size vocabulary stable, makes the review path honest by using a truly larger token, and slightly widens the shared ladder so adjacent sizes are easier to distinguish on real hardware and emulator buttons.
</objective>

## Tasks

<task id="03-03-01">
<title>Make the Phase 3 review path ask for a real larger-than-baseline time line and prove it in mounted coverage</title>
<files>
- packages/cli/fixtures/phase-22/config.emulator-demo.yml
- packages/cli/src/builtin-addons/date-time/index.test.ts
</files>
<action>
Update the committed Phase 3 review fixture so the main deck button uses the top shared size token (`<2xl>...</2xl>`) for the primary `HH:mm` line instead of `<xl>...</xl>`, while keeping the single-field `format` contract, blink behavior, and fallback deck path intact. Then align `packages/cli/src/builtin-addons/date-time/index.test.ts` to the same honest contract: the mounted rich date-time test should use the corrected larger-size markup, assert the rendered HTML still contains the expected rich tags, and verify the chosen larger size token rather than only a same-size tag that cannot visibly differ from the outer shell.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/builtin-addons/date-time/index.test.ts`.
</verify>
<done>
The real Phase 3 review path now encodes a genuinely larger first line, and the mounted date-time regression would fail if that larger-size contract drifted again.
</done>
</task>

<task id="03-03-02">
<title>Widen the shared text size ladder and keep Phase 3 failure evidence linked to this closure plan</title>
<files>
- packages/cli/src/render/theme-utilities.ts
- .planning/phases/03-rich-date-time-formatting-surface/03-UAT.md
- .planning/phases/03-rich-date-time-formatting-surface/03-VERIFICATION.md
</files>
<action>
Adjust `TEXT_SIZE_MULTIPLIERS` in `packages/cli/src/render/theme-utilities.ts` so the existing shared size tokens remain the only vocabulary but adjacent levels are more visually distinct than the current moderate ladder. Keep `md` as the role-base token and avoid inventing new public size names. Then update the Phase 3 UAT and verification artifacts so they preserve the original report that the main-deck time line was not visibly larger, record the confirmed root cause (fixture used `<xl>` against an outer `xl` baseline), and make `03-03-PLAN.md` the explicit rerun path instead of leaving Phase 3 verification in a falsely clean state.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/dom-host.test.tsx src/builtin-addons/date-time/index.test.ts && rg -n "03-03-PLAN.md|2xl|root_cause|size ladder|larger" .planning/phases/03-rich-date-time-formatting-surface/03-UAT.md .planning/phases/03-rich-date-time-formatting-surface/03-VERIFICATION.md packages/cli/src/render/theme-utilities.ts`.
</verify>
<done>
The shared size ladder is more legible, and the Phase 3 planning artifacts truthfully record the gap plus the rerun path needed to close it.
</done>
</task>
