name: System Status — Web Snapshot
buttons:
  - position: 1
    type: "system-status:system-status"
    config:
      pages:
        - type: bars
          metrics:
            - cpu
  - position: 2
    type: "system-status:system-status"
    config:
      pages:
        - type: bars
          metrics:
            - cpu
            - ram
  - position: 3
    type: "system-status:system-status"
    config:
      pages:
        - type: bars
          metrics:
            - cpu
            - ram
            - disk
  - position: 6
    type: "system-status:system-status"
    config:
      pages:
        - type: kpis
          metrics:
            - cpu
  - position: 7
    type: "system-status:system-status"
    config:
      pages:
        - type: kpis
          metrics:
            - cpu
            - ram
  - position: 8
    type: "system-status:system-status"
    config:
      pages:
        - type: kpis
          metrics:
            - cpu
            - ram
            - disk
  - position: 11
    type: "system-status:system-status"
    config:
      pages:
        - type: chart
          metrics:
            - cpu
          windowSeconds: 60
  - position: 12
    type: "system-status:system-status"
    config:
      pages:
        - type: chart
          metrics:
            - cpu
            - ram
          windowSeconds: 60
