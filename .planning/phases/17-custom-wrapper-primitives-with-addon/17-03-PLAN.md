---
wave: 3
depends_on:
  - 17-02
files_modified:
  - packages/cli/src/render/text-image.ts
  - packages/cli/src/render/text-image.test.ts
  - packages/cli/src/deck/runtime.test.ts
  - packages/cli/fixtures/phase-17/config.button-shape-composition.yml
  - .planning/phases/17-custom-wrapper-primitives-with-addon/17-UAT.md
autonomous: true
single_layer_justified: false
objective: "Phase 17 ships an observable review path proving both the default base-shape composition and the explicit full-surface escape hatch on the real review surface."
must_haves:
  truths:
    - "The explicit full-surface escape hatch is observable and test-covered, not just implied by omitted shape config."
    - "The default base-shape path and the full-surface path are both visible in committed review/UAT artifacts."
    - "Bespoke/custom visuals still work honestly through the full-surface path without forcing them through the base-shape helper model."
  artifacts:
    - packages/cli/src/render/text-image.ts
    - packages/cli/src/render/text-image.test.ts
    - packages/cli/fixtures/phase-17/config.button-shape-composition.yml
    - .planning/phases/17-custom-wrapper-primitives-with-addon/17-UAT.md
  key_links:
    - "packages/cli/src/render/text-image.ts and packages/cli/src/render/text-image.test.ts must make the difference between default base-shape rendering and full-surface rendering observable."
    - "The Phase 17 fixture and UAT file must expose both contracts on the real CLI/device review path instead of proving them only with synthetic tests."
---

# Plan 17-03: Prove The Escape Hatch On The Review Surface

<objective>
Deliver the final Phase 17 tracer bullet as one demoable user-facing behavior: reviewers can see the default base-shape composition and the explicit full-surface escape hatch side by side on a committed review path.
</objective>

## Tasks

<task id="17-03-01">
<title>Make the explicit full-surface path visible and testable</title>
<files>
- packages/cli/src/render/text-image.ts
- packages/cli/src/render/text-image.test.ts
- packages/cli/src/deck/runtime.test.ts
</files>
<action>
Finish the narrow escape hatch by making explicit full-surface rendering visibly different from the default base-shape path and pinning that behavior in focused renderer/runtime tests. Keep the contract honest: full-surface rendering must be explicitly requested, and missing shape config must still resolve to the default base shape.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/text-image.test.ts src/deck/runtime.test.ts`.
</verify>
<done>
The phase's most important behavior split is observable in automated verification instead of being a naming exercise.
</done>
</task>

<task id="17-03-02">
<title>Commit a reviewable Phase 17 fixture and UAT path</title>
<files>
- packages/cli/fixtures/phase-17/config.button-shape-composition.yml
- .planning/phases/17-custom-wrapper-primitives-with-addon/17-UAT.md
</files>
<action>
Add one committed Phase 17 fixture and manual review/UAT script that expose both sides of the new contract: at least one default base-shaped button using explicit content helpers and at least one explicit full-surface custom render. The artifact should make it easy to judge the real visual boundary without dragging bespoke variants into forced migration.
</action>
<verify>
Confirm `packages/cli/fixtures/phase-17/config.button-shape-composition.yml` contains at least one default base-shaped helper consumer and one explicit full-surface consumer, and confirm `17-UAT.md` includes concrete steps plus expected visual outcomes for both buttons on the real CLI/device path.
</verify>
<done>
Phase 17 has a real review surface, not just unit-level proof.
</done>
</task>

<must_haves>
- Reviewers can observe both the default base-shape path and the explicit full-surface path.
- `packages/cli/fixtures/phase-17/config.button-shape-composition.yml` contains one default base-shaped helper consumer and one explicit full-surface consumer.
- `17-UAT.md` documents concrete review steps and expected visual outcomes for both rendering paths on the real CLI/device surface.
</must_haves>
