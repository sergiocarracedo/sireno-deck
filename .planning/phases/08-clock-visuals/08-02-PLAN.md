---
wave: 2
depends_on:
  - 08-01-PLAN.md
files_modified:
  - packages/cli/fixtures/phase-8/config.analog-clock.yml
  - .planning/phases/08-clock-visuals/08-UAT.md
  - packages/cli/src/render/text-image.test.ts
  - builtin-addons/date-time/src/index.test.ts
autonomous: true
single_layer_justified: false
objective: "Reviewers can run a committed Phase 8 analog-clock fixture and verify on the real CLI/device path that the shipped clock visual and cadence remain legible and contractually correct."
must_haves:
  truths:
    - "The repo ships a committed Phase 8 config fixture that exercises the real analog-clock button type rather than a synthetic test-only render call."
    - "Manual review instructions tell the reviewer exactly how to verify legibility and live cadence on the CLI/device path."
    - "Focused test coverage protects the concrete review path so future changes cannot keep unit tests green while breaking the shipped analog-clock review setup."
  artifacts:
    - packages/cli/fixtures/phase-8/config.analog-clock.yml
    - .planning/phases/08-clock-visuals/08-UAT.md
    - packages/cli/src/render/text-image.test.ts
    - builtin-addons/date-time/src/index.test.ts
  key_links:
    - "The Phase 8 fixture must exercise the exact bundled `analog-clock` type added in 08-01"
    - "The UAT doc must reference the committed fixture and observable live-clock behavior, not an abstract implementation detail"
---

# Plan 08-02: Commit The Analog Clock Review Path

<objective>
Close the same class of gap that bit Phase 7: make the shipped analog-clock review path explicit, committed, and verifiable on the real renderer/device flow instead of trusting only synthetic unit coverage. This plan turns the first analog visual into something reviewers can actually run and judge.
</objective>

## Tasks

<task id="08-02-01">
<title>Add a committed Phase 8 review fixture that exercises the real analog-clock type</title>
<files>
- packages/cli/fixtures/phase-8/config.analog-clock.yml
</files>
<action>
Create a focused `packages/cli/fixtures/phase-8/config.analog-clock.yml` fixture that declares at least one built-in `analog-clock` button through the normal addon/config path. Keep the fixture narrow and review-friendly: it should show the new visual clearly without mixing in unrelated milestone work or extra decorative text. Reuse existing fixture conventions from earlier phases so the config is runnable from `packages/cli` during review.
</action>
<verify>
Read the fixture and confirm it references the bundled `analog-clock` type rather than the older digital `date-time` type.
</verify>
<done>
The repo contains a real Phase 8 config that reviewers can run to see the analog clock through the same CLI path users will use.
</done>
</task>

<task id="08-02-02">
<title>Write the analog-clock UAT path around observable visual and cadence checks</title>
<files>
- .planning/phases/08-clock-visuals/08-UAT.md
- packages/cli/fixtures/phase-8/config.analog-clock.yml
</files>
<action>
Add `.planning/phases/08-clock-visuals/08-UAT.md` with a concrete review script for the new fixture. The instructions should tell the reviewer how to start the CLI with the committed config, what visual qualities to inspect on-device or in the rendered output, and how to confirm the analog clock updates at the expected once-per-second cadence through core scheduling. Include specific pass/fail wording around legibility of the hour, minute, and second hands so the review remains anchored to Phase 8's actual product risk.
</action>
<verify>
Check that `08-UAT.md` references `packages/cli/fixtures/phase-8/config.analog-clock.yml` and includes observable cadence and legibility expectations.
</verify>
<done>
Phase 8 has a committed human review path that verifies the real analog visual instead of relying on code inspection alone.
</done>
</task>

<task id="08-02-03">
<title>Protect the committed review path with focused regression coverage</title>
<files>
- packages/cli/src/render/text-image.test.ts
- builtin-addons/date-time/src/index.test.ts
</files>
<action>
Tighten the tests around the exact Phase 8 review scenario instead of leaving coverage purely synthetic. Extend either the renderer tests, the addon tests, or both so they reference the concrete analog-clock contract used by the committed fixture: separate button type, 1000ms default cadence, and a visibly non-default analog render path. Keep the assertions narrow and cheap, but make sure they would fail if the shipped review fixture silently regressed back to a text/default-card output or lost its live cadence declaration.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/text-image.test.ts ../../builtin-addons/date-time/src/index.test.ts`.
</verify>
<done>
Phase 8 verification now protects the concrete analog-clock review path, reducing the chance of another green-tests/broken-UAT mismatch.
</done>
</task>

<must_haves>
- `UIW-12` is satisfied by shipping the Phase 8 review fixture and documenting how to verify the analog-clock behavior manually.
- This plan is demoable on its own after 08-01 because a reviewer can run the committed fixture and observe the shipped analog-clock behavior end-to-end.
</must_haves>
