---
wave: 2
depends_on: [59-01]
files_modified:
  - packages/cli/src/core/schemas.ts
  - packages/cli/src/config/loader.ts
  - packages/cli/src/cli/commands/start.ts
  - packages/cli/src/deck/runtime.ts
  - .planning/phases/59-emoji-keystroke-injection-and-category-fix/59-VERIFICATION.md
autonomous: true
single_layer_justified: true
objective: Add `paste.keystroke` config field to SirenoConfig (default true), wire it through CLI → runtime → `methods.pasteText`, document EMO-17 category audit outcome in VERIFICATION.md (EMO-15, EMO-16, EMO-17 finalization)
created: 2026-06-11
---

# 59-02 — paste config field + EMO-17 audit documentation

> Plan 59-01 added the runtime option `pasteKeystrokeEnabled`. This plan wires it to user config (`paste.keystroke: false` in config.yml), updates the SirenoConfig schema, and finalizes the phase with VERIFICATION.md (including the EMO-17 category audit outcome).

## Context

Phase 59 decisions (CONTEXT.md):
- Opt-out: config-level `paste.keystroke: false` (default `true`).
- EMO-17: confirm only, document in VERIFICATION.md. Phase 57 already ran `comm -12` and found zero overlap.

The runtime option is already in place after Plan 59-01 (`options.pasteKeystrokeEnabled`). This plan:
1. Adds `paste: { keystroke: boolean }` to `SirenoConfig` schema.
2. Updates the config loader if needed.
3. Wires the config value through `start.ts` → `createDeckRuntime` options.
4. Updates `config.yml` documentation to mention the new field.
5. Writes 59-VERIFICATION.md with EMO-15/16/17 traced to evidence.

## Tasks

### Task 1: Add `paste.keystroke` to SirenoConfig schema

**File:** `packages/cli/src/core/schemas.ts`

Add a new `PasteSchema` near the other config-level schemas (around `LoggingSchema` at line 25-31):

```typescript
const PasteSchema = z
  .object({
    keystroke: z.boolean().default(true),
  })
  .strict()
```

Extend the `SirenoConfig` interface (line 205-217):

```typescript
export interface SirenoConfig {
  // ... existing fields
  paste?: z.infer<typeof PasteSchema>
}
```

Default value of `true` is handled by zod's `.default(true)` — if the user doesn't set `paste.keystroke`, it's `true` (i.e. paste keystroke is on by default).

**Verify:** `pnpm --filter sireno-deck-cli build` exits 0. Existing config fixtures still validate.
**Done:** Schema accepts the new field with default.

### Task 2: Wire `paste.keystroke` through start.ts to createDeckRuntime

**File:** `packages/cli/src/cli/commands/start.ts`

Find where the SirenoConfig is passed to `createDeckRuntime` (look for `createDeckRuntime({` in start.ts). Add a new option `pasteKeystrokeEnabled: config.paste?.keystroke ?? true` to the call.

If the wiring requires a small refactor (e.g. extracting the config slice), do the minimum. Goal: when a user has `paste.keystroke: false` in config.yml, the runtime option is set to false, and Plan 59-01's skip path fires.

**Verify:** Build succeeds. Manually trace: config.yml `paste.keystroke: false` → SirenoConfig.paste.keystroke = false → start.ts passes `pasteKeystrokeEnabled: false` → `createDeckRuntime` constructor → `methods.pasteText` skips the keystroke.
**Done:** Wiring complete; the runtime option is now driven by config.

### Task 3: Update config.yml example to document the new field

**File:** `/works/opensource/sireno-deck/config.yml`

Add a top-level `paste:` section near the other config fields (logging, device, etc.):

```yaml
# Paste behavior — when methods.pasteText() is called, the runtime writes
# to the clipboard AND simulates the OS paste keystroke (Ctrl+V / Cmd+V).
# Set paste.keystroke: false to disable the keystroke (clipboard write
# still happens; you must paste manually). Useful on platforms where the
# keystroke simulation interferes with other input.
paste:
  keystroke: true
```

**Verify:** `cat config.yml` shows the new section with a comment.
**Done:** config.yml updated.

### Task 4: Add one test for the config → runtime wiring

**File:** `packages/cli/src/deck/__tests__/runtime.test.ts`

Add one test that confirms the runtime reads `pasteKeystrokeEnabled` from options and skips the keystroke when set to false. This is essentially a 1-test version of the Plan 59-01 Task 2 "opt-out" test, but specifically verifies the option name in the wiring.

Actually, this is already covered by Plan 59-01 Task 2 test #4. **Skip this task** if Plan 59-01 covers it.

**Verify:** Plan 59-01 test #4 exists and passes. If not, add it here.
**Done:** Wiring verified.

### Task 5: Document EMO-17 audit + write 59-VERIFICATION.md

**File:** `.planning/phases/59-emoji-keystroke-injection-and-category-fix/59-VERIFICATION.md`

Trace each requirement to evidence:

| ID     | Requirement                                                                                                       | Status | Evidence                                                                                                  |
| ------ | ----------------------------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------- |
| EMO-15 | Tapping an emoji writes to clipboard AND simulates the OS paste keystroke                                          | ✓      | Plan 59-01: `methods.pasteText` now calls `keyMacroProvider.send(platformPasteKey)` after `clipboardy.write`. Unit tests cover Linux/macOS/Windows. |
| EMO-16 | Double-tapping an emoji copies the shortcode to clipboard AND performs the paste keystroke                          | ✓      | Same — `emojiEntryButton` double-tap handler at `entry.tsx:35` calls `methods.pasteText(shortcode)`. The new `pasteText` is the same code path as tap. |
| EMO-17 | The emoji category data is audited and deduplicated so smiles/people (and any other overlapping categories) show distinct emoji sets | ✓      | Phase 57 RESEARCH.md "RES-03" section: `comm -12` between all 11 categories returns 0 chars. No code change needed. The audit is a documentation outcome. |

Verification commands:

- `pnpm --filter sireno-deck-cli test` — 6+ new tests pass, no regressions.
- `pnpm --filter sireno-deck-cli build` — clean.
- Manual UAT on real hardware (xdotool on Linux, osascript on macOS, SendInput on Windows): open a text editor, tap an emoji in the emoji selector, verify the emoji character is inserted at the cursor.

**Verify:** File exists with all 3 EMO-* requirements traced to evidence. Status: passed.
**Done:** VERIFICATION.md committed.

## Must Haves

- [ ] `paste.keystroke` field in SirenoConfig schema (default `true`)
- [ ] `start.ts` wires `config.paste?.keystroke ?? true` to `createDeckRuntime` options
- [ ] `config.yml` documents the new field with a comment
- [ ] 59-VERIFICATION.md exists with status: passed and EMO-15/16/17 traced to evidence
- [ ] No regressions in any existing test suite
- [ ] Build is clean
