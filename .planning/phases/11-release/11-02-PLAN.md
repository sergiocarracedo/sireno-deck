---
phase: 11-release
plan: 02
wave: 2
depends_on: [11-01]
autonomous: true
single_layer_justified: false
objective: |
  Ship the v0.1.0 CHANGELOG.md (Keep-a-Changelog format, v0.1.0 only,
  auto-generated from Conventional Commits, hand-curated). Mark v0.1.0 as
  shipped in ROADMAP.md and STATE.md (the docs-release ceremony per
  CONTEXT.md decision). Update AGENTS.md Current Phase block.
files_modified:
  - CHANGELOG.md (NEW at repo root)
  - .planning/ROADMAP.md (mark phase 11 ✅ done)
  - .planning/STATE.md (mark milestone shipped)
  - .planning/AGENTS.md (Current Phase → milestone shipped)
---

# Phase 11 Plan 02: CHANGELOG + v0.1.0 shipped

## Tasks

### Task 1: Generate `CHANGELOG.md`

- File: `/works/opensource/sireno-deck-2/CHANGELOG.md` (new)
- Header per Keep-a-Changelog format (https://keepachangelog.com/en/1.1.0/):
  ```
  # Changelog

  All notable changes to this project will be documented in this file.

  The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
  and this project adheres to [Semantic Versioning](https://semver.org/).
  ```
- Single `## [0.1.0] - 2026-06-27` section.
- Sections in order: Added → Changed → Fixed → Removed.
- **Added**: headline features per phase (01-11), pulled from `.planning/ROADMAP.md`. Examples:
  - "Workspace scaffold: TS 7.0 RC, oxlint/oxfmt, vitest, yargs, pino, daemon helpers"
  - "Config-driven addon system: YAML loader with line info, `@file.yml` expander, addon registry, hot-reload watcher"
  - "Deck runtime: pub-sub bus, gesture state machine (tap/dbl-tap/hold), store, deck navigation, 3 core built-in addons"
  - "WS bridge v3 with token handshake + vite plugin + React 19 / Tailwind 4 frontend + WS client"
  - "Emulator shell: device models (mk2=15, plus=32, mini=6, xl=32), mouse-to-gesture, side panel + iframe"
  - "Hardware: Playwright render pipeline, device enumeration + interactive prompt, udev rules helper"
  - "OS providers: Linux (dbus-next, gnome-shell, playerctl), macOS (osascript), Windows (PowerShell + UIA)"
  - "Built-in themes: `default` + `light` with Tailwind 4 `@theme` directive, 4 surfaces (IconLabel, Bars, LabelValueList, SplitAction)"
  - "Built-in addons: date-time (6 buttons), emoji-selector, media-player, system-status, value-display, weather, brightness"
  - "Daemon lifecycle: PID + token + children files in `$XDG_RUNTIME_DIR`, graceful shutdown (SIGTERM 5s → SIGKILL), concurrent-start conflict prompt"
  - "Prod HTTP server: Node `http` with per-request WS token injection (`<script>window.__SIRENO_TOKEN__ = "..."</script>`)"
  - "npm addon loader via `~/.cache/sireno-deck-2/node_modules/` (opencode.ai plugin pattern)"
- **Changed**: anything significant (e.g., "Migrated from dev token via `SIRENO_TOKEN` env to per-request runtime injection").
- **Fixed**: bug fixes worth surfacing (e.g., "Cross-button double-tap state machine bug", "emulator gesture thresholds at 500ms", "frontend emulator button overlay now transparent").
- **Removed**: nothing in v0.1.0 (no prior versions).

### Task 2: Mark v0.1.0 as shipped in `ROADMAP.md`

- File: `.planning/ROADMAP.md`
- Change the `| 11 | release | pending |` row to `| 11 | release | ✅ done |`.
- Optionally update the milestone success criteria checklist at the top to mark items complete (or add a "✓ Shipped 2026-06-27" line).

### Task 3: Mark v0.1.0 as shipped in `STATE.md`

- File: `.planning/STATE.md`
- Replace the `current_phase: 11-release` frontmatter with `current_phase: shipped` (or a similar milestone-complete marker).
- Set `phase_status: complete`.
- Add a new section: `## Milestone shipped: v0.1.0` with the date and a summary of what's shipped (phases 01-11).

### Task 4: Update `AGENTS.md` Current Phase block

- File: `.planning/AGENTS.md`
- Update the `## Current Phase` block to reflect "Milestone v0.1.0 shipped; next: phase 12 (addon-frontend-registry) is the next planned work".
- Note that phase 12 was planned during quick task 011 (real button surfaces in emulator) and is ready to plan.

## Acceptance

- `CHANGELOG.md` exists at the repo root with a single `## [0.1.0] - 2026-06-27` section.
- CHANGELOG renders correctly on GitHub (verify the markdown locally).
- `ROADMAP.md` row 11 is `✅ done`.
- `STATE.md` reflects milestone shipped.
- `AGENTS.md` Current Phase block points to phase 12 as the next planned work.
