---
wave: 3
depends_on:
  - 24-02-PLAN.md
  - 24-03-PLAN.md
files_modified:
  - packages/cli/src/builtin-addons/core-buttons/
  - packages/cli/src/builtin-addons/date-time/
  - packages/cli/src/builtin-addons/emoji-selector/
  - packages/cli/src/addon/loader.test.ts
  - packages/cli/src/deck/runtime.test.ts
  - packages/cli/src/render/dom-host.test.tsx
  - packages/cli/fixtures/phase-24/
  - .planning/codebase/ARCHITECTURE.md
  - AGENTS.md
autonomous: true
single_layer_justified: false
objective: "Shipped built-in buttons, fixtures, and architecture docs all reflect the new mounted render contract, so the repo's real examples and documentation match the runtime users actually get."
must_haves:
  truths:
    - "Shipped built-in buttons no longer depend on the old instance-first contract as the primary authoring model."
    - "A committed Phase 24 review fixture exercises the new mounted active-deck/store/runtime boundary end to end with shipped built-ins or proof addons."
    - "Focused builtin/runtime/render tests cover the migrated contract from workspace-safe fixture paths."
    - "Architecture-facing docs stop claiming the runtime is intentionally non-DOM/static-instance-first once the new contract lands."
  artifacts:
    - packages/cli/src/builtin-addons/core-buttons/
    - packages/cli/src/builtin-addons/date-time/
    - packages/cli/src/builtin-addons/emoji-selector/
    - packages/cli/fixtures/phase-24/
    - .planning/codebase/ARCHITECTURE.md
    - AGENTS.md
  key_links:
    - "Built-in addons become the repo's primary proof that the new contract is the real one, not an experimental side path."
    - "Architecture docs and AGENTS.md stay in sync with the shipped runtime so future planning is not based on stale assumptions."
---

# Plan 24-04: Built-In Migration, Proof Fixtures, and Doc Truthfulness

<objective>
Finish the phase honestly: the repo's shipped built-ins, tests, proof fixtures, and architecture docs should all reflect the mounted addon render contract instead of leaving the new model half-adopted and the written architecture lying about what the runtime now does.
</objective>

## Tasks

<task id="24-04-01">
<title>Migrate shipped built-ins onto the new public contract</title>
<files>
- packages/cli/src/builtin-addons/core-buttons/
- packages/cli/src/builtin-addons/date-time/
- packages/cli/src/builtin-addons/emoji-selector/
</files>
<action>
Migrate the shipped built-in button definitions so their primary authoring path uses the new mounted `render(props)` contract, definition-level handlers, runtime methods, and (where needed) the addon session store. Keep the migration narrow and explicit: do not opportunistically redesign unrelated visuals or widen renderer primitives beyond what Phase 24 requires. Preserve the behavior users already rely on while moving the contract seam for the built-ins that the repo ships as examples.
</action>
<verify>
`grep -R "createInstance" packages/cli/src/builtin-addons` shows only intentionally retained compatibility code, if any, and the migrated built-ins compile/tests target the new contract.
</verify>
<done>
The built-ins stop teaching the old authoring model by example and instead prove the new Phase 24 contract on real shipped surfaces.
</done>
</task>

<must_haves>
- Phase 24 is post-roadmap follow-on work and does not claim a new v1.2 requirement ID; this slice keeps shipped examples, fixtures, and architecture docs truthful about the new contract.
- Built-ins become the primary proof surface for the new addon contract instead of leaving it as a side-path fixture experiment.
- Architecture-facing docs must stop claiming the runtime is intentionally static/non-DOM once the mounted active-deck path ships.
</must_haves>

<task id="24-04-02">
<title>Lock the migration with workspace-safe tests and review fixtures</title>
<files>
- packages/cli/src/addon/loader.test.ts
- packages/cli/src/deck/runtime.test.ts
- packages/cli/src/render/dom-host.test.tsx
- packages/cli/fixtures/phase-24/
</files>
<action>
Add or update focused tests and committed Phase 24 fixtures so the new mounted render/store/runtime boundary is proven on the shipped review path, not only through synthetic unit seams. Anchor every fixture path relative to the test file location using `import.meta.url`-style resolution so workspace-root and package-scoped runs stay aligned. Include at least one reviewable proof that exercises a mounted active deck, runtime-driven transient props, and addon-store-backed coordination together.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/addon/loader.test.ts src/deck/runtime.test.ts src/render/dom-host.test.tsx` and confirm the Phase 24 fixture paths are file-relative rather than cwd-relative.
</verify>
<done>
The repo has a stable, reviewable proof that the full Phase 24 contract works end to end and does not regress under different test invocation roots.
</done>
</task>

<task id="24-04-03">
<title>Update architecture-facing docs so they match the shipped runtime</title>
<files>
- .planning/codebase/ARCHITECTURE.md
- AGENTS.md
</files>
<action>
Update `.planning/codebase/ARCHITECTURE.md` and any phase/status-facing repo docs that still describe the runtime as instance-first and intentionally non-DOM/static-markup-based. Keep the edits surgical and factual: document that Node still owns hardware semantics, navigation, polling, and command execution, while the active deck now mounts as a React tree backed by a core-owned addon session store. Ensure the `AGENTS.md` current-phase block also reflects the planning/execution state required by the workflow when this phase moves forward.
</action>
<verify>
`grep -n "Stateful button instances\|not DOM-based\|mounted" .planning/codebase/ARCHITECTURE.md AGENTS.md` shows the stale claims removed or updated to the new architecture.
</verify>
<done>
Future planning and implementation read an architecture description that matches the live contract instead of fighting stale assumptions from pre-Phase-24 docs.
</done>
</task>
