---
wave: 3
depends_on:
  - 05-01-PLAN.md
  - 05-02-PLAN.md
files_modified:
  - builtin-addons/core-buttons/src/index.ts
  - builtin-addons/core-buttons/src/index.test.ts
  - builtin-addons/emoji-selector/package.json
  - builtin-addons/emoji-selector/src/index.ts
  - builtin-addons/emoji-selector/src/index.test.ts
  - packages/cli/src/addon/api.ts
  - packages/cli/src/addon/loader.ts
  - packages/cli/src/core/schemas.ts
  - packages/cli/src/deck/controller.ts
  - packages/cli/src/deck/runtime.ts
  - packages/cli/src/deck/runtime.test.ts
  - packages/cli/src/render/reconciler.ts
  - packages/cli/src/render/text-image.ts
  - config.yml
autonomous: true
single_layer_justified: false
objective: "User can use addon-provided assets, custom deck types, and the bundled emoji selector addon, proving the full addon model from manifest through config through runtime navigation."
must_haves:
  truths:
    - "A bundled addon can expose reusable assets that button config can reference by path"
    - "The addon contract supports registration of a custom deck type with preconfigured button layout generation"
    - "`builtin-addons/emoji-selector/src/index.ts` boots through the same addon loader path as other bundled addons"
    - "The emoji selector addon renders category navigation and selection behavior through the addon-first runtime"
    - "Runtime tests cover addon-driven navigation/deck generation and emoji selector favorites behavior"
  artifacts:
    - builtin-addons/emoji-selector/src/index.ts
    - packages/cli/src/deck/runtime.ts
    - packages/cli/src/deck/controller.ts
    - config.yml
  key_links:
    - "`packages/cli/src/addon/loader.ts` loads the emoji selector addon through the same bundled/external registry path used earlier in the phase"
    - "`packages/cli/src/deck/runtime.ts` and `packages/cli/src/deck/controller.ts` host addon-provided deck types without reintroducing button-specific coupling"
---

# Plan 05-03: Prove assets, deck types, and emoji selector through the addon model

<objective>
Deliver the final Phase 5 proof that the addon model is real, not just a button-registry abstraction. This plan adds addon-shipped assets, addon-provided deck types, and the bundled emoji selector addon so the extension contract is exercised across rendering, navigation, and configuration.
</objective>

## Tasks

<task id="05-03-01">
<title>Add addon asset and deck-type registration to the core contract</title>
<files>
- packages/cli/src/addon/api.ts
- packages/cli/src/addon/loader.ts
- packages/cli/src/core/schemas.ts
- packages/cli/src/deck/controller.ts
- packages/cli/src/deck/runtime.ts
</files>
<action>
Extend the addon API just enough to support the remaining Phase 5 requirements: reusable asset exposure and custom deck-type registration with preconfigured button layout generation. Keep the button contract decisions intact; do not reopen the API into a large capability bag. Update the loader, config validation, and runtime/deck-controller integration so deck instances can resolve addon-provided deck definitions and asset references without reintroducing feature-specific button logic into the core runtime.
</action>
<verify>
grep 'deck' packages/cli/src/addon/api.ts && grep 'asset' packages/cli/src/addon/api.ts && grep 'deck type' packages/cli/src/deck/runtime.ts
</verify>
<done>
The core addon contract now covers the remaining deck-type and asset requirements needed for the final Phase 5 proof.
</done>
</task>

<task id="05-03-02">
<title>Implement the bundled emoji selector addon through the real addon path</title>
<files>
- builtin-addons/emoji-selector/package.json
- builtin-addons/emoji-selector/src/index.ts
- builtin-addons/emoji-selector/src/index.test.ts
- builtin-addons/core-buttons/src/index.ts
- config.yml
</files>
<action>
Create the bundled `builtin-addons/emoji-selector/` workspace package and implement it through the same addon manifest/entrypoint path used for all other addons. The addon must expose the emoji selector behavior required by ADDN-10: category browsing, selection, and favorites-aware behavior, using addon-defined button and deck registrations rather than special runtime branches. Update `config.yml` with an example that boots the emoji selector through the new addon-first config model.
</action>
<verify>
ls builtin-addons/emoji-selector/package.json && grep 'favorites' builtin-addons/emoji-selector/src/index.ts && grep 'emoji' config.yml
</verify>
<done>
The repo has a real bundled emoji-selector addon that exercises the same loader and registration model as the rest of Phase 5.
</done>
</task>

<task id="05-03-03">
<title>Cover deck-type, asset, and emoji-selector behavior with integration-style tests</title>
<files>
- builtin-addons/emoji-selector/src/index.test.ts
- builtin-addons/core-buttons/src/index.test.ts
- packages/cli/src/deck/runtime.test.ts
</files>
<action>
Add tests that prove addon-provided assets can be resolved and referenced from config, addon-provided deck types can generate navigable deck surfaces, and the emoji selector addon can boot, navigate categories, and respect favorites configuration through the generic addon-first runtime. Keep the tests focused on the runtime and addon package boundaries so they pin the contract without overspecifying internals.
</action>
<verify>
grep 'emoji' builtin-addons/emoji-selector/src/index.test.ts && grep 'favorites' builtin-addons/emoji-selector/src/index.test.ts && grep 'deck' packages/cli/src/deck/runtime.test.ts
</verify>
<done>
The full addon model is regression-covered from addon package registration through runtime navigation and visual behavior.
</done>
</task>
