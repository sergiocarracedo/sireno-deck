---
wave: 2
depends_on:
  - 05-01-PLAN.md
files_modified:
  - packages/cli/src/deck/runtime.ts
  - packages/cli/src/deck/runtime.test.ts
  - packages/cli/src/util/errors.ts
  - packages/cli/src/render/dom-host-deck-document.tsx
autonomous: true
single_layer_justified: false
objective: "When a specific button render/action/runtime path fails, that button shows one shared warning-triangle plus four-digit error helper while structured logs record deck id, button position, button type, and error code, without disturbing the existing full-deck config error surface."
must_haves:
  truths:
    - "`packages/cli/src/deck/runtime.ts` routes button-scoped failures through one shared helper that assigns a stable four-digit code and keeps config reload failures on the separate temporary full-deck error deck."
    - "Focused runtime tests prove a button-level failure renders the compact helper for that button and emits deck/button-aware diagnostics including deck id, button position, button type, and error code."
    - "The helper reuses the existing visual system and does not turn browser-shell warnings or config-validation failures into the new button-local surface."
  artifacts:
    - packages/cli/src/deck/runtime.ts
    - packages/cli/src/deck/runtime.test.ts
  key_links:
    - "Runtime tests assert both the button-facing helper output and the corresponding structured log payload."
---

# Plan 05-03: Shared Button Error Helper

<objective>
Add the new compact button-facing runtime error helper at the runtime-owned failure seam so a broken button shows a warning triangle plus a four-digit code and the logs carry the actionable detail. This slice depends on the truthful reload boundary from Plan 05-01, because the helper should be integrated into runtime-owned button failure paths without disturbing the proven config-error deck contract.
</objective>

## Tasks

<task id="05-03-01">
<title>Introduce a runtime-owned button error helper with stable codes and structured diagnostics</title>
<files>
- packages/cli/src/deck/runtime.ts
- packages/cli/src/util/errors.ts
</files>
<action>
Add the narrowest runtime-owned abstraction needed to represent button-scoped failures: a helper that can map a failure to a four-digit code, capture deck id, button position, and button type from the runtime context, and emit structured logs without replacing the separate config reload error path. Integrate it into the existing runtime-owned render/action failure catches near `reportRuntimeError(...)` or the nearest narrower button-specific seam confirmed during implementation.
</action>
<verify>
Read the touched runtime/error files and confirm button failures now flow through one helper that produces a compact code plus structured deck/button-aware diagnostics, while config-validation failures still use `showTemporaryErrorDeck(...)`.
</verify>
<done>
There is one shared runtime-owned path for button-scoped failures, and it records both the on-device code and the structured diagnostic context needed for debugging.
</done>
</task>

<task id="05-03-02">
<title>Prove the helper on runtime surfaces and keep it visually narrow</title>
<files>
- packages/cli/src/deck/runtime.test.ts
- packages/cli/src/render/dom-host-deck-document.tsx
</files>
<action>
Add focused tests that trigger a button-level render/action failure and assert the resulting button output is the compact warning-triangle plus four-digit code helper, not a deck-wide text wall. Only touch `dom-host-deck-document.tsx` if the runtime-owned helper needs a minimal render primitive shared with the browser/emulator path; do not collapse emulator shell warnings or inline browser warnings into this helper.
</action>
<verify>
Run the focused runtime test command, such as `pnpm --filter ./packages/cli test -- packages/cli/src/deck/runtime.test.ts`, and confirm the failure-helper assertions pass.
</verify>
<done>
The new helper is covered by focused tests, stays visually compact, and remains isolated from the existing full-deck config error surface and browser-shell warning surfaces.
</done>
</task>
