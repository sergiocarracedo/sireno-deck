---
wave: 4
depends_on:
  - 28-01-PLAN.md
  - 28-02-PLAN.md
  - 28-03-PLAN.md
files_modified:
  - package.json
  - packages/cli/package.json
  - packages/cli/src/cli/commands/start.test.ts
  - CHANGELOG.md
  - .planning/STATE.md
autonomous: true
single_layer_justified: false
objective: "Developers can run one truthful workspace-root `cli:dev` loop that restarts the real raw-source CLI `start --config config.yml` seam on relevant code/config/theme/addon edits, and the repo records the helper-to-component cutover with an explicit changelog learning."
must_haves:
  truths:
    - "Workspace root exposes a `cli:dev` script that runs the real `packages/cli/src/cli/index.ts start --config config.yml` seam through `tsx watch`."
    - "The watch loop explicitly includes non-imported repo files such as root config, local themes, and addon runtime files so edits restart truthfully."
    - "The existing package-level `dev` script no longer pretends to be the primary Phase 28 development loop if it still only builds with `tsdown --watch`."
    - "The repo records what broke and what was learned about the component-first cutover in `CHANGELOG.md` and state breadcrumbs."
  artifacts:
    - package.json
    - packages/cli/package.json
    - packages/cli/src/cli/commands/start.test.ts
    - CHANGELOG.md
    - .planning/STATE.md
  key_links:
    - "package.json becomes the honest entrypoint for the phase's requested `p cli:dev` workflow."
    - "The final proof stays on the exact `start --config config.yml` seam the product really runs instead of a synthetic wrapper."
---

# Plan 28-04: Truthful `cli:dev` Loop And Phase 28 Learnings

<objective>
Finish Phase 28 with the developer workflow and closure artifacts the roadmap promised: a workspace-root `cli:dev` command that exercises the real CLI/runtime seam, not just bundler output, plus explicit documentation of the cutover and what it taught us. This slice stays last because the watch loop only matters once the actual authoring/runtime surface has already been migrated to the new component-first contract.
</objective>

## Tasks

<task id="28-04-01">
<title>Add the workspace-root `cli:dev` watch loop on the real raw-source start seam</title>
<files>
- package.json
- packages/cli/package.json
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Add a workspace-root `cli:dev` script that runs the real raw-source CLI entry through `tsx watch` and invokes `start --config config.yml`, with explicit watch coverage for the concrete non-imported repo paths the phase context locked: `packages/cli/src/**/*`, `config.yml`, `themes/**/*`, `addons/**/*`, and `builtin-addons/**/*`. Update any adjacent package/root scripts or tests needed so the repo no longer implies `packages/cli`'s `tsdown --watch` script is the primary Phase 28 developer loop. Keep this truthful to the existing runtime seam: do not add a synthetic wrapper command, emulator-only shortcut, or separate dev server product.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts` and `pnpm exec node -e "const pkg=require('./package.json'); const script=pkg.scripts['cli:dev']; if (!script) throw new Error('missing cli:dev'); for (const token of ['tsx watch','packages/cli/src/cli/index.ts','start --config config.yml','--include ./packages/cli/src/**/*','--include ./config.yml','--include ./themes/**/*','--include ./addons/**/*','--include ./builtin-addons/**/*']) { if (!script.includes(token)) throw new Error('missing '+token) } console.log(script)"`.
</verify>
<done>
Developers have one workspace-root watch command whose script text and regression coverage both point at the actual CLI start seam and its explicit include graph.
</done>
</task>

<must_haves>
- The watch graph must include non-imported files explicitly; research already proved plain dependency watching is insufficient.
- Do not create a second development product surface such as a special wrapper CLI, bespoke dev daemon, or emulator-first script.
- Keep the loop on raw source via `tsx`, because truthful TSX runtime behavior is part of the phase contract.
</must_haves>

<task id="28-04-02">
<title>Record the helper-cutover outcome and final proof path</title>
<files>
- CHANGELOG.md
- .planning/STATE.md
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Add the required dated `CHANGELOG.md` entry for this significant change with Features, Fixes, and Learnings sections that explain the root cause behind the helper-factory drift and why the component-first cutover plus truthful `cli:dev` loop fix it. Update `.planning/STATE.md` with a concise Phase 28 execution breadcrumb matching the repo's existing style, and extend the start-path test only as needed so there is an explicit final verification seam for the raw-source `cli:dev` start path after the migration.
</action>
<verify>
Run `grep -n "Phase 28\|helper\|component-first\|cli:dev\|Learnings" CHANGELOG.md .planning/STATE.md` and `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts`.
</verify>
<done>
Phase 28 ends with an inspectable explanation of what changed, why the old helper model drifted, and how the truthful watch/start proof should be rerun.
</done>
</task>
