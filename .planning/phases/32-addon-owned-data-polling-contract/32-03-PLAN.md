---
wave: 3
depends_on:
  - 32-01-PLAN.md
  - 32-02-PLAN.md
files_modified:
  - packages/cli/src/builtin-addons/media-player/index.ts
  - packages/cli/src/builtin-addons/media-player/schemas.ts
  - packages/cli/src/builtin-addons/media-player/button.tsx
  - packages/cli/src/builtin-addons/media-player/index.test.ts
  - packages/cli/src/builtin-addons/media-player/domain/media-controller.ts
  - packages/cli/src/builtin-addons/media-player/domain/linux-media-controller.ts
  - packages/cli/src/builtin-addons/media-player/domain/macos-media-controller.ts
  - packages/cli/src/builtin-addons/media-player/domain/windows-media-controller.ts
  - packages/cli/src/addon/builtin.ts
  - packages/cli/src/core/schemas.ts
autonomous: true
single_layer_justified: false
objective: "Media-player runs as an addon-owned polling and adapter domain that returns typed playback payloads into render props, preserving truthful play/pause/stop behavior and honest unsupported-host degradation."
must_haves:
  truths:
    - "Media-player polling callback and payload typing are addon-owned, and render reads playback snapshot payload from runtime props."
    - "OS media adapters move into media-player addon-local domain modules rather than core `/system/*` ownership."
    - "Tap remains fixed to play/pause with optional hold behavior, while unsupported hosts continue to render honest unavailable state."
    - "Media-player addon schema owns cadence defaults/validation under the split poll/render contract."
  artifacts:
    - packages/cli/src/builtin-addons/media-player/index.ts
    - packages/cli/src/builtin-addons/media-player/button.tsx
    - packages/cli/src/builtin-addons/media-player/schemas.ts
    - packages/cli/src/builtin-addons/media-player/index.test.ts
    - packages/cli/src/builtin-addons/media-player/domain/media-controller.ts
    - packages/cli/src/builtin-addons/media-player/domain/linux-media-controller.ts
    - packages/cli/src/builtin-addons/media-player/domain/macos-media-controller.ts
    - packages/cli/src/builtin-addons/media-player/domain/windows-media-controller.ts
  key_links:
    - "`packages/cli/src/builtin-addons/media-player/button.tsx` is the user-visible seam where payload-driven status and controls must remain truthful."
    - "`packages/cli/src/builtin-addons/media-player/index.test.ts` guards unsupported-host and tap/hold behavior through the shipped addon surface."
---

# Plan 32-03: Migrate Media-Player To Addon-Owned Polling And OS Adapters

<objective>
Deliver a full media-player tracer bullet on the new Phase 32 contract: addon-local polling callbacks fetch playback snapshots via addon-owned OS adapters, runtime forwards payload into render props, and user-visible controls/unsupported behavior stay truthful. This keeps core runtime generic and removes media capability ownership drift from core `/system/*` seams.
</objective>

## Tasks

<task id="32-03-01">
<title>Move media-controller and OS adapters into media-player addon domain and wire payload polling</title>
<files>
- packages/cli/src/builtin-addons/media-player/domain/media-controller.ts
- packages/cli/src/builtin-addons/media-player/domain/linux-media-controller.ts
- packages/cli/src/builtin-addons/media-player/domain/macos-media-controller.ts
- packages/cli/src/builtin-addons/media-player/domain/windows-media-controller.ts
- packages/cli/src/builtin-addons/media-player/index.ts
- packages/cli/src/builtin-addons/media-player/button.tsx
</files>
<action>
Create addon-local media-controller domain modules for media-player, including Linux/macOS/Windows adapter entry points previously owned under core `/system/*`. Update media-player button definition so addon polling callback returns typed playback payload (status/metadata/progress/availability), and render logic reads payload from render props instead of calling controller snapshot directly in render. Preserve fixed tap play/pause behavior and existing optional hold command semantics.
</action>
<verify>
Run `grep -n "from '../../system/media-controller\|from '../system/media-controller" packages/cli/src/builtin-addons/media-player/**/*.ts* && grep -n "payload\|poll" packages/cli/src/builtin-addons/media-player/index.ts`.
</verify>
<done>
Media-player capability logic and OS adapters are addon-owned, and the button render path is payload-driven under the new runtime contract.
</done>
</task>

<must_haves>
- Keep unsupported host behavior explicit and honest; do not fabricate metadata/status.
- Do not make tap action configurable away from play/pause.
- Reuse existing shared presentation behavior (including marquee/text helpers) instead of adding a separate rendering subsystem.
</must_haves>

<task id="32-03-02">
<title>Move cadence schema ownership into media-player addon and lock migration with tests</title>
<files>
- packages/cli/src/builtin-addons/media-player/schemas.ts
- packages/cli/src/builtin-addons/media-player/index.test.ts
- packages/cli/src/addon/builtin.ts
- packages/cli/src/core/schemas.ts
</files>
<action>
Update media-player schema so poll/render cadence defaults and validation are addon-owned and aligned with split intervals. Ensure built-in registration continues through standard addon registry path, and remove any redundant core-owned cadence assumptions in shared core schema surface. Expand focused media-player tests to verify payload-driven render behavior, tap/hold semantics, and unsupported-host degradation remain unchanged across the migration.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/builtin-addons/media-player/index.test.ts src/addon/loader.test.ts src/deck/runtime.test.ts -t "media-player\|unsupported\|tap\|hold"`.
</verify>
<done>
Media-player cadence semantics are addon-owned, and regression tests prove user-facing behavior remains truthful on the shipped runtime/addon path.
</done>
</task>
