---
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - config.yml
  - packages/cli/src/config/loader.test.ts
autonomous: true
single_layer_justified: false
objective: "The shipped Phase 5 example config points at real addon examples so local-folder and npm addon startup can be demonstrated during UAT instead of only warning about missing example addons."
must_haves:
  truths:
    - "`config.yml` no longer references nonexistent example addons"
    - "The Phase 5 example config still demonstrates both local-folder and npm addon declarations in a UAT-able way"
    - "Config-loader coverage documents the example addon declarations that ship with the repo"
  artifacts:
    - config.yml
    - packages/cli/src/config/loader.test.ts
  key_links:
    - "The shipped config example matches real addon fixtures or intentionally disabled placeholders that do not misrepresent successful startup"
---

# Plan 05-04: Fix stale addon examples in the shipped config

<objective>
Close the UAT gaps caused by `config.yml` pointing at addons that do not exist. This plan updates the shipped example so it still documents local and npm addon declarations, but only in a way that matches real repo state and can be demonstrated during UAT.
</objective>

## Tasks

<task id="05-04-01">
<title>Replace broken shipped addon examples with real or safely disabled examples</title>
<files>
- config.yml
- packages/cli/src/config/loader.test.ts
</files>
<action>
Update `config.yml` so the local-folder and npm addon examples do not claim working addons that are absent from the repo. Prefer a minimal, YAML-friendly example that either references real fixtures available in the repo or marks the sample addon entries `enabled: false` with names/paths that are explicitly illustrative. Adjust config-loader coverage only as needed so tests pin the shipped example behavior and prevent future drift back to nonexistent addon examples.
</action>
<verify>
grep 'enabled' config.yml && grep 'addons:' config.yml && grep 'config.yml' packages/cli/src/config/loader.test.ts
</verify>
<done>
The shipped config example no longer causes false-negative UAT failures by referencing missing addon examples as if they are ready to use.
</done>
</task>
