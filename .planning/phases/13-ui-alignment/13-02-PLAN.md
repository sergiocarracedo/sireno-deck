---
phase: 13-ui-alignment
plan: 02
wave: 2
depends_on: [13-01]
autonomous: true
objective: |
  Re-implement 5 theme components (Icon, Label, Chip, TapIndicator, Text) and
  4 surfaces (IconLabel, Bars, LabelValueList, SplitAction) matching legacy
  visual structure exactly. Use Tailwind v4 classes; no cn/clsx.
files_modified:
  - packages/cli/src/themes/default/components/Icon.tsx
  - packages/cli/src/themes/default/components/Label.tsx
  - packages/cli/src/themes/default/components/Chip.tsx
  - packages/cli/src/themes/default/components/TapIndicator.tsx
  - packages/cli/src/themes/default/components/Text.tsx
  - packages/cli/src/themes/default/surfaces/IconLabel.tsx
  - packages/cli/src/themes/default/surfaces/Bars.tsx
  - packages/cli/src/themes/default/surfaces/LabelValueList.tsx
  - packages/cli/src/themes/default/surfaces/SplitAction.tsx
---

# Phase 13 Plan 02: components + surfaces

## Tasks

### Task 1: Update ButtonFrame to use border-frame

File: `themes/default/ButtonFrame.tsx`

- Change `border-fg/10` to `border-frame` (now defined via --color-frame).

### Task 2: Re-implement Text (update legacy values)

File: `themes/default/components/Text.tsx`

- Already ported in quick-008. Change `text-fg` to `text-foreground` and `text-accent` to `text-primary` to match legacy class naming. Add `data-sireno-ui-text="true"` attribute.

### Task 3: Re-implement Icon

File: `themes/default/components/Icon.tsx`

- Read legacy `ui/Icon.tsx` (3.6K). Key: renders SVG icons with `data-sireno-ui-icon="true"`, supports size/tone. Re-implement: `<span data-sireno-ui-icon className="text-fg text-lg">{children}</span>`.

### Task 4: Re-implement Label

File: `themes/default/components/Label.tsx`

- Read legacy `ui/Label.tsx`. Simple component that styles a label string. Re-implement as a `<span>` with `font-aux text-foreground`.

### Task 5: Re-implement Chip

File: `themes/default/components/Chip.tsx`

- Read legacy `ui/Chip.tsx`. Renders a badge/chip with `bg-background/78%` and `text-foreground`. Re-implement using `bg-bg/80 text-fg text-[10px] rounded-full`.

### Task 6: Re-implement TapIndicator

File: `themes/default/components/TapIndicator.tsx`

- Read legacy `ui/TapIndicator.tsx` (1.3K). Renders a tap ripple effect. Re-implement using `sireno-tap` CSS class.

### Task 7: Re-implement IconLabel surface

File: `themes/default/surfaces/IconLabel.tsx`

- Read legacy `ui/surfaces/IconLabelSurface.tsx`. Simple: icon + label stacked or side-by-side. Use our `<Icon>` + `<Text>` components.

### Task 8: Re-implement Bars surface

File: `themes/default/surfaces/Bars.tsx`

- Read legacy `ui/surfaces/BarsSurface.tsx` (3.5K). Renders horizontal bar chart. Each bar has an optional accent color. Use `div` with `h-1.5 rounded bg-bar` + inner fill `bg-bar-accent`.

### Task 9: Re-implement LabelValueList surface

File: `themes/default/surfaces/LabelValueList.tsx`

- Read legacy `ui/surfaces/LabelValueListSurface.tsx` (4.0K). Renders rows of label-value pairs. Use our `<Text>` component for each row: `<Text size="xs" tone="muted">{label}</Text><Text size="xs" tone="fg">{value}</Text>`.

### Task 10: Re-implement SplitAction surface

File: `themes/default/surfaces/SplitAction.tsx`

- Read legacy `ui/surfaces/SplitActionSurface.tsx` (1.5K). Renders a left/right split button. Use `grid-cols-2`.

### Task 11: Verify

- `pnpm test` passes.
- `pnpm --filter sireno-deck lint` clean.
- All component exports match existing imports in theme's index.tsx.
