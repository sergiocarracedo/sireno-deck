# Plan 17-01

**Archival note:** The original Phase 17 plan files were not preserved in the repo. This file was reconstructed during v1.2 milestone closeout reconciliation so the surviving execution summaries have a truthful plan-side anchor.

## Objective

Establish the explicit base-shape versus full-surface contract, keep legacy `wrapper_id` behavior as a narrow compatibility seam, and make the new transport reviewable on the shipped CLI/device path.

## Closure path

- Executed outcome is recorded in `17-01-SUMMARY.md`.
- The shipped fixture and manual review path live in `packages/cli/fixtures/phase-17/config.wrapper-compatibility.yml` and `17-UAT.md`.
