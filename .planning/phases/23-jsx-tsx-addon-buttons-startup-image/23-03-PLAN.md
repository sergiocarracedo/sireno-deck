---
wave: 3
depends_on:
  - 23-01-PLAN.md
  - 23-02-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/fixtures/phase-23/config.yml
  - packages/cli/src/cli/commands/start.test.ts
  - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md
  - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md
autonomous: true
single_layer_justified: true
objective: "Close the remaining Phase 23 UAT blocker so the shipped Phase 23 sample config starts successfully with the local raw `.tsx` addon fixture and the fixture cannot drift away from the addon's real registered button type again."
must_haves:
  truths:
    - "`packages/cli/fixtures/phase-23/config.yml` references the actual registered button type from the shipped local raw addon fixture, so the config no longer fails with `Unknown button type 'phase-23-local-raw-addon'`."
    - "Focused startup/config coverage proves the shipped Phase 23 sample config (or an equivalent config-level assertion) loads the raw addon fixture through the normal startup path using the corrected button type."
    - "Phase 23 UAT and verification artifacts preserve the failed evidence and point rerun work at this gap-closure plan."
  artifacts:
    - packages/cli/fixtures/phase-23/config.yml
    - packages/cli/src/cli/commands/start.test.ts
    - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md
    - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md
  key_links:
    - "`packages/cli/fixtures/phase-23/config.yml` must use the real addon button definition id registered in `packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx`, not the addon package name"
    - "`packages/cli/src/core/schemas.ts` will continue validating button `type` against `registry.getButton(...)`, so the regression check must pin the config-to-registry match explicitly"
---

# Plan 23-03: Fix Phase 23 Fixture Button-Type Drift

<objective>
Close the one remaining Phase 23 UAT blocker at its actual source: the shipped Phase 23 sample config is using the addon package name where the runtime expects the registered button definition id. This plan realigns the fixture config with the addon's real button type, adds a focused regression check on the startup/config seam, and updates the UAT/verification artifacts so the failure history stays accurate while the rerun path is explicit.
</objective>

## Tasks

<task id="23-03-01">
<title>Realign the Phase 23 sample config with the registered raw-addon button type</title>
<files>
- packages/cli/fixtures/phase-23/config.yml
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Update `packages/cli/fixtures/phase-23/config.yml` so the deck button at line 10 uses the actual button `type` exported by the local raw addon fixture in `packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx` rather than the addon package name. Then extend `packages/cli/src/cli/commands/start.test.ts` with one focused assertion that the shipped Phase 23 sample config, or an equivalent config-level setup using the same fixture values, passes through the normal startup/config path with that corrected button type and yields the expected registered button in the addon registry.</action>
<verify>
Run `pnpm vitest run src/cli/commands/start.test.ts src/addon/loader.test.ts` from `packages/cli`.</verify>
<done>
The shipped Phase 23 sample config matches the raw addon's real registered button type, and focused startup/config coverage would fail again if the config drifted back to the addon package name.</done>
</task>

<task id="23-03-02">
<title>Relink the failed UAT evidence to the gap-closure rerun path</title>
<files>
- .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md
- .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md
</files>
<action>
Update the Phase 23 UAT and verification artifacts so they preserve the original failed config evidence, record that the root cause is the fixture/config button-type drift, and make `23-03-PLAN.md` the explicit rerun path for closing that gap. Keep both docs synchronized so downstream rerun work does not accidentally erase the fact that the original shipped sample config was wrong.</action>
<verify>
Run `test -f .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md && test -f .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md && rg -n "23-03-PLAN.md|phase-23-local-raw-addon|phase-23-local-raw-button|root cause|rerun" .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md`.</verify>
<done>
The failed UAT evidence stays intact, and both UAT and verification docs clearly point the next execution pass at this gap-closure plan.</done>
</task>
