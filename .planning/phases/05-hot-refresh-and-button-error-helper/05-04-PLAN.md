---
wave: 3
depends_on:
  - 05-03-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/deck/runtime.ts
  - packages/cli/src/ui/Icon.tsx
  - packages/cli/src/deck/runtime.test.ts
  - .planning/phases/05-hot-refresh-and-button-error-helper/05-UAT.md
autonomous: true
single_layer_justified: false
objective: "Close the Phase 5 UAT product gap by upgrading the button-scoped runtime warning helper from a plain text triangle to a clearer icon-led warning treatment while preserving the compact four-digit code, existing config-error deck separation, and deck/button-aware diagnostics."
must_haves:
  truths:
    - "The button-scoped runtime helper no longer renders as plain text `▲` only; it uses a clearer warning icon treatment that keeps the four-digit code legible on the button surface."
    - "Focused runtime coverage proves the upgraded helper still appears for button-scoped failures and does not replace the separate full-deck config reload error surface."
    - "Phase 5 UAT evidence is updated so the original visual gap, its root cause, and the rerun/closure path remain inspectable after execution."
  artifacts:
    - packages/cli/src/deck/runtime.ts
    - packages/cli/src/ui/Icon.tsx
    - packages/cli/src/deck/runtime.test.ts
    - .planning/phases/05-hot-refresh-and-button-error-helper/05-UAT.md
  key_links:
    - "`packages/cli/src/deck/runtime.ts` should consume the shared icon primitive from `packages/cli/src/ui/Icon.tsx` rather than inventing a new warning surface."
    - "The UAT gap for Test 2 is specifically about helper visual clarity, so the rerun note in `05-UAT.md` must point back to this closure plan."
---

# Plan 05-04: Close Button Warning Helper Visual Gap

<objective>
Close the real product issue found during Phase 5 UAT: the warning-isolation path works, but the button-scoped runtime helper looks underdesigned because it renders only a plain text triangle plus code. This plan upgrades that helper through the existing icon system, keeps the compact error-code contract intact, and preserves an honest paper trail in the UAT evidence so the gap can be rerun and closed cleanly.
</objective>

## Tasks

<task id="05-04-01">
<title>Upgrade the button runtime helper to a clearer icon-led warning treatment</title>
<files>
- packages/cli/src/ui/Icon.tsx
- packages/cli/src/deck/runtime.ts
</files>
<action>
Add the minimal generic warning icon needed in `packages/cli/src/ui/Icon.tsx`, then update `createRuntimeButtonErrorContent(...)` in `packages/cli/src/deck/runtime.ts` to use that shared icon primitive instead of a plain text `▲`. Keep the helper compact and preserve the existing four-digit code, danger tone, button-local scope, and full-deck config-error separation. Do not broaden the fix into a new error framework or browser-shell warning redesign.
</action>
<verify>
Read `packages/cli/src/ui/Icon.tsx` and `packages/cli/src/deck/runtime.ts` to confirm the helper now renders through a shared warning icon path and still includes the four-digit code in the button content.
</verify>
<done>
The runtime helper has a clearer warning-icon treatment, and the compact code helper remains visually legible and scoped to button-level failures.
</done>
</task>

<task id="05-04-02">
<title>Prove the upgraded helper and keep the UAT blocker trail truthful</title>
<files>
- packages/cli/src/deck/runtime.test.ts
- .planning/phases/05-hot-refresh-and-button-error-helper/05-UAT.md
</files>
<action>
Update the focused runtime helper tests so they assert the upgraded warning treatment instead of the old plain-text-only triangle expectation, while keeping the existing structured diagnostic checks and config-error-deck separation coverage. Then amend `05-UAT.md` so Test 2’s gap points rerun directly at `05-04-PLAN.md`, preserves the original user report, and records that this closure plan is the visual-fix path for the button helper contract.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/deck/runtime.test.ts -t "button runtime helper|temporary reload error deck|later rebuilt runtime recover"` and verify `05-UAT.md` mentions `05-04-PLAN.md` in the Test 2 gap/rerun trail.
</verify>
<done>
Focused regression coverage matches the upgraded helper visuals, and the UAT artifact still preserves the blocker history while pointing rerun at this closure plan.
</done>
</task>
