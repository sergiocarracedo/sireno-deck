---
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - packages/cli/src/render/dom-host.test.tsx
  - packages/cli/fixtures/phase-22/config.emulator-demo.yml
  - .planning/phases/02-live-shrink-fit-measurement/02-UAT.md
autonomous: true
single_layer_justified: false
objective: "The Phase 2 shrink-fit contract is locked by proof: browser-path regressions show live remeasurement and deterministic post-floor ellipsis behavior, while a committed review fixture/UAT path makes the measured shrink behavior inspectable without pretending mounted/static parity."
must_haves:
  truths:
    - "Regression coverage proves browser-path shrink-fit remeasures on content/container-affecting changes and does not regress into the old clamp illusion."
    - "Once shrink-fit reaches the readable minimum floor, the shipped fallback is deterministic ellipsis rather than hidden wrapping or infinite shrinking."
    - "Mounted/static paths are covered for honest degradation rather than fake measured parity."
    - "The repo ships one reviewable Phase 2 fixture/UAT path that makes live shrink behavior inspectable on the real browser/emulator seam."
  artifacts:
    - packages/cli/src/render/dom-host.test.tsx
    - packages/cli/fixtures/phase-22/config.emulator-demo.yml
    - .planning/phases/02-live-shrink-fit-measurement/02-UAT.md
  key_links:
    - "packages/cli/src/render/dom-host.test.tsx becomes the primary regression seam for live remeasurement, loop safety, and honest non-browser degradation."
    - "packages/cli/fixtures/phase-22/config.emulator-demo.yml and `.planning/phases/02-live-shrink-fit-measurement/02-UAT.md` provide the reviewable browser-path proof required by the roadmap and context."
---

# Plan 02-02: Lock Shrink-Fit Behavior With Browser-Path Proof

<objective>
Finish Phase 2 by proving the browser-only shrink contract visibly and regressively: content/container-affecting changes remeasure, the readable minimum floor falls back to ellipsis deterministically, mounted/static output degrades honestly, and the repo ships one reviewable fixture/UAT path instead of relying only on unit tests.
</objective>

## Tasks

<task id="02-02-01">
<title>Add focused browser-path regressions for live remeasurement, post-floor ellipsis, and honest non-browser degradation</title>
<files>
- packages/cli/src/render/dom-host.test.tsx
</files>
<action>
Extend the browser DOM-host regression suite so Phase 2 is proven at the seam that actually owns live layout behavior. Add focused cases that show `fit="shrink"` recomputes on content/container-affecting changes, respects the agreed readable minimum floor, and then falls back to deterministic ellipsis instead of wrapping or shrinking forever. Also add explicit proof that mounted/static output does not claim the same live measurement behavior. Keep this regression work narrow and phase-specific: do not widen into generic text-layout testing or unrelated widget-specific behavior.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/dom-host.test.tsx`.
</verify>
<done>
The regression suite locks Phase 2’s live browser shrink contract, deterministic post-floor ellipsis, and honest non-browser degradation.
</done>
</task>

<task id="02-02-02">
<title>Ship one reviewable Phase 2 browser/emulator path for live shrink-fit inspection</title>
<files>
- packages/cli/fixtures/phase-22/config.emulator-demo.yml
- .planning/phases/02-live-shrink-fit-measurement/02-UAT.md
</files>
<action>
Create or update one committed review path that makes the measured shrink behavior visibly inspectable on the real browser/emulator seam. Use an existing shipped fixture path if it can be extended narrowly, or create the smallest new fixture/UAT pairing that shows shrink-fit choosing the largest non-wrapping size, then ellipsizing after the readable floor when space is still insufficient. The UAT instructions must tell the reviewer exactly what to look at and should avoid promising mounted/static measurement parity.
</action>
<verify>
Run `grep -n "shrink\|ellipsis\|floor" packages/cli/fixtures/phase-22/config.emulator-demo.yml .planning/phases/02-live-shrink-fit-measurement/02-UAT.md`.
</verify>
<done>
The repo contains a committed, reviewable browser/emulator proof for Phase 2 shrink-fit behavior instead of leaving the contract proven only by automated tests.
</done>
</task>

<must_haves>
- `TRF-04` is fully satisfied: the readable minimum floor and deterministic ellipsis fallback are both proven.
- Verification stays anchored to the real browser/emulator seam, not a synthetic-only proxy.
- Mounted/static behavior is documented and tested as honest degradation, not parity.
</must_haves>
