---
wave: 3
depends_on:
  - 07-01-PLAN.md
  - 07-02-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/render/text-image.ts
  - packages/cli/src/render/text-image.test.ts
  - themes/dark.yml
  - themes/light.yml
  - packages/cli/fixtures/phase-7/config.shared-dark.yml
  - packages/cli/fixtures/phase-7/config.shared-light.yml
  - .planning/phases/07-typography-text-behavior/07-UAT.md
autonomous: true
single_layer_justified: true
objective: "Phase 7 shared-text UAT produces a reliably visible theme-driven typography difference on the shipped review fixtures instead of depending on host font fallback behavior."
must_haves:
  truths:
    - "The Phase 7 shared-text review fixtures produce an obvious visual typography change between dark and light without relying only on host-specific font-family availability."
    - "The renderer contract remains theme-driven and clip-only; the fix must not widen scope into marquee, ellipsis, or a mandatory wrapper migration."
    - "Verification coverage proves the real review path, not just a synthetic token swap inside a unit test."
  artifacts:
    - packages/cli/src/render/text-image.ts
    - packages/cli/src/render/text-image.test.ts
    - themes/dark.yml
    - themes/light.yml
    - packages/cli/fixtures/phase-7/config.shared-dark.yml
    - packages/cli/fixtures/phase-7/config.shared-light.yml
  key_links:
    - "The review fixtures and built-in themes must combine to produce a UAT-visible typography change through the same render path used on-device"
    - "Renderer tests must cover the concrete dark-vs-light review scenario that failed in UAT, not only an arbitrary font token mutation"
---

# Plan 07-03: Make Theme Typography Differences UAT-Visible

<objective>
Close the Phase 7 UAT gap where theme typography is technically threaded into SVG output but not reliably visible on the real CLI/device path. This fix should keep the Phase 7 contract narrow while making the shipped review fixtures produce an obvious theme-driven typography difference that does not collapse under host font fallback.
</objective>

## Tasks

<task id="07-03-01">
<title>Choose a typography distinction that survives the real render path</title>
<files>
- themes/dark.yml
- themes/light.yml
- packages/cli/fixtures/phase-7/config.shared-dark.yml
- packages/cli/fixtures/phase-7/config.shared-light.yml
- packages/cli/src/render/text-image.ts
</files>
<action>
Adjust the shipped Phase 7 review path so dark-versus-light typography differs in a way that remains visible even when host font-family fallback collapses to the same installed font. Prefer changes that stay inside the approved typography-token contract, such as clearly different font sizes, weights, or letter spacing on the shared review buttons, or a renderer-safe fallback strategy that makes the distinction observable without introducing font-asset loading. Keep the fix constrained to shared text output and the UAT fixtures; do not widen the phase into custom font packaging unless that is truly unavoidable.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/text-image.test.ts`.
</verify>
<done>
The shipped dark and light review fixtures now differ in a clearly visible shared-text way on the real renderer path, even on hosts where font-family names alone would fall back to the same font.
</done>
</task>

<task id="07-03-02">
<title>Replace the synthetic typography test with coverage that matches the UAT failure</title>
<files>
- packages/cli/src/render/text-image.test.ts
</files>
<action>
Update `packages/cli/src/render/text-image.test.ts` so Phase 7 verification proves the concrete review scenario that failed in UAT. Add coverage that compares the effective shared-text render output for the shipped dark and light typography setups used by the Phase 7 review fixtures, and assert that the difference is large enough to be meaningfully observable rather than a trivial pixel blip. Keep the clip-only coverage intact.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/text-image.test.ts` and confirm the new dark-vs-light typography test passes.
</verify>
<done>
Phase 7 tests now protect against future regressions where theme typography is technically wired but not observably different in the real review path.
</done>
</task>
