---
wave: 4
depends_on:
  - 23-03-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx
  - packages/cli/fixtures/phase-23/local-raw-addon/src/content.tsx
  - packages/cli/src/cli/commands/start.test.ts
  - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md
  - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md
autonomous: true
single_layer_justified: true
objective: "Close the rerun blocker where the shipped local raw `.tsx` addon fixture drifted back to raw JSX so the corrected Phase 23 sample config can actually render through the intended helper-based startup path without `React is not defined`."
must_haves:
  truths:
    - "`packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx` routes its rendered output through the helper-based root-export contract again instead of returning raw JSX that depends on ambient React."
    - "Focused startup/config coverage proves the shipped Phase 23 sample config loads the local raw `.tsx` addon fixture and reaches a valid renderable button output without `React is not defined`."
    - "Phase 23 UAT and verification artifacts preserve the rerun failure evidence and point the next rerun at this render-contract closure plan."
  artifacts:
    - packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx
    - packages/cli/fixtures/phase-23/local-raw-addon/src/content.tsx
    - packages/cli/src/cli/commands/start.test.ts
    - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md
    - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md
  key_links:
    - "`packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx` must stay aligned with `packages/cli/fixtures/phase-23/local-raw-addon/src/content.tsx` and the root-export helper path from `sireno-deck-cli`, not raw JSX"
    - "`packages/cli/src/cli/commands/start.test.ts` must pin the shipped fixture's render contract, not only addon registration/config loading, so the Phase 23 sample addon cannot drift back to `React is not defined`"
---

# Plan 23-04: Fix Phase 23 Fixture Render-Contract Drift

<objective>
Close the new rerun blocker at the real source: the shipped raw-addon fixture entrypoint no longer matches the helper-based TSX authoring contract that Phase 23 was meant to prove. This plan restores the fixture entrypoint to the root-export helper path, adds a focused regression check on actual rendered output through the normal startup/runtime seam, and updates the phase artifacts so the rerun history stays accurate while the next rerun path is explicit.
</objective>

## Tasks

<task id="23-04-01">
<title>Restore the shipped raw addon fixture to the helper-based render contract and pin runtime renderability</title>
<files>
- packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx
- packages/cli/fixtures/phase-23/local-raw-addon/src/content.tsx
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Update `packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx` so the shipped fixture no longer returns raw JSX from `render()`. Route the fixture back through the existing helper-based content path in `src/content.tsx` (or an equivalent root-export-only helper implementation) so it matches the intended Phase 23 authoring contract and does not depend on ambient React globals. Then extend `packages/cli/src/cli/commands/start.test.ts` with one focused regression assertion that the shipped Phase 23 sample config and fixture addon reach a valid renderable button output through the normal startup/runtime path without throwing `React is not defined`.
</action>
<verify>
Run `pnpm vitest run src/cli/commands/start.test.ts src/addon/loader.test.ts` from `packages/cli`.
</verify>
<done>
The shipped Phase 23 raw addon fixture renders through the helper-based contract again, and focused startup/runtime coverage would fail if the fixture drifted back to raw JSX or an ambient-React dependency.
</done>
</task>

<task id="23-04-02">
<title>Relink the rerun failure evidence to the render-contract closure plan</title>
<files>
- .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md
- .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md
</files>
<action>
Update the Phase 23 UAT and verification artifacts so they preserve the rerun evidence showing `React is not defined` in the shipped raw addon fixture, record that the root cause is render-contract drift inside `packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx`, and make `23-04-PLAN.md` the explicit next rerun path. Keep both docs synchronized so the original rerun failure remains visible even after the fixture entrypoint is corrected.
</action>
<verify>
Run `test -f .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md && test -f .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md && rg -n "23-04-PLAN.md|React is not defined|phase-23-local-raw-button|root_cause|rerun" .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md`.
</verify>
<done>
The rerun blocker remains documented, and both UAT and verification artifacts point the next execution pass at the fixture render-contract closure plan.
</done>
</task>
