---
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - builtin-addons/emoji-selector/src/index.ts
  - builtin-addons/emoji-selector/src/index.test.ts
  - packages/cli/src/render/text-image.ts
  - packages/cli/src/render/text-image.test.ts
  - packages/cli/src/config/loader.test.ts
autonomous: true
single_layer_justified: false
objective: "Addon asset icons and emoji selector entries render correctly on-device so the Phase 5 addon proof is visually usable, not just navigable."
must_haves:
  truths:
    - "Addon asset-backed buttons render visible icons on-device instead of label-only cards"
    - "Emoji selector entries render identifiable emoji visuals instead of broken glyphs"
    - "Rendering tests cover the chosen asset and emoji rendering strategy"
  artifacts:
    - packages/cli/src/render/text-image.ts
    - builtin-addons/emoji-selector/src/index.ts
  key_links:
    - "The emoji selector addon and the text-image renderer agree on how emoji visuals are represented"
---

# Plan 05-05: Fix addon asset and emoji visual rendering

<objective>
Close the remaining Phase 5 visual gaps found in UAT. The current runtime and command flow work, but addon asset icons are not visible on-device and emoji glyphs do not render reliably, so this plan fixes the rendering path rather than changing navigation or selection behavior.
</objective>

## Tasks

<task id="05-05-01">
<title>Make addon asset icons render visibly through the image pipeline</title>
<files>
- packages/cli/src/render/text-image.ts
- packages/cli/src/render/text-image.test.ts
- packages/cli/src/config/loader.test.ts
</files>
<action>
Investigate and fix the SVG/image composition path in `packages/cli/src/render/text-image.ts` so addon asset-backed icons produce visible rendered output on-device. Keep the existing `addon://` config contract intact. Extend renderer tests only as needed to prove addon asset paths survive through rendering with visible image content rather than blank icon slots.
</action>
<verify>
grep 'image' packages/cli/src/render/text-image.ts && grep 'icon' packages/cli/src/render/text-image.test.ts
</verify>
<done>
Addon-backed icon buttons render visible icons through the existing image pipeline instead of falling back to label-only cards.
</done>
</task>

<task id="05-05-02">
<title>Render emoji selector entries with reliable visuals</title>
<files>
- builtin-addons/emoji-selector/src/index.ts
- builtin-addons/emoji-selector/src/index.test.ts
- packages/cli/src/render/text-image.ts
- packages/cli/src/render/text-image.test.ts
</files>
<action>
Replace the current raw-emoji-label rendering approach with a rendering strategy that is reliable on-device with the existing SVG/sharp pipeline. Keep favorites, selection commands, and deck generation behavior intact. The fix can use addon-provided image assets or a renderer-level emoji visual treatment, but it must be deterministic and testable without relying on the host system having a specific emoji font installed.
</action>
<verify>
grep 'emoji' builtin-addons/emoji-selector/src/index.ts && grep 'emoji' builtin-addons/emoji-selector/src/index.test.ts && grep 'emoji' packages/cli/src/render/text-image.test.ts
</verify>
<done>
Emoji selector tiles render identifiable emoji visuals reliably enough for UAT to distinguish and select them on-device.
</done>
</task>
