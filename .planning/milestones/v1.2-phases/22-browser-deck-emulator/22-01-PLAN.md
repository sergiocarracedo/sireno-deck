---
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/cli/commands/start.ts
  - packages/cli/src/cli/commands/start.test.ts
  - packages/cli/src/device/stream-deck.ts
  - packages/cli/src/render/browser-renderer.ts
  - packages/cli/src/render/dom-host.tsx
  - packages/cli/src/cli/
  - packages/cli/fixtures/phase-22/
autonomous: true
single_layer_justified: false
objective: "Users can launch a dedicated browser emulator command and see the real current deck render locally without attached hardware."
---

# Plan 22-01

## Objective

Ship the first honest browser-only runtime path: a dedicated CLI mode/command that starts without Stream Deck hardware, boots the normal config/runtime stack against a virtual device seam, and serves one local deck-focused page showing the current rendered deck.

## Tasks

<task>
<file>packages/cli/src/device/stream-deck.ts</file>
<action>Introduce a virtual-device lifecycle seam that mirrors the existing hardware lifecycle shape closely enough for `start.ts` to consume it: connection info with `keyCount`, key-event subscription, start/close methods, and a way for a browser-side input bridge to emit `StreamDeckKeyEvent` values without physical hardware.</action>
<verify>Type-level and focused lifecycle tests can construct the virtual lifecycle and observe emitted `down` / `up` events through the same listener contract used by `createDeckRuntime()`.</verify>
<done>The project has one transport-agnostic lifecycle contract that can represent either hardware or emulator mode.</done>
</task>

<task>
<file>packages/cli/src/cli/commands/start.ts</file>
<action>Add a dedicated emulator entry path or mode that skips physical device discovery, starts the browser renderer with a selected virtual key count, creates the runtime against the virtual lifecycle, and serves one local browser page containing the current deck surface plus minimal runtime/status metadata.</action>
<verify>Focused command tests prove emulator startup succeeds without attached hardware and still goes through the normal config/theme/runtime setup path.</verify>
<done>Users can start the emulator explicitly and get a live browser-rendered deck without owning hardware.</done>
</task>

<task>
<file>packages/cli/src/render/browser-renderer.ts</file>
<action>Reuse the existing browser-renderer key-count and deck-page logic to support emulator-mode rendering cleanly, including any small API adjustments needed so the browser page can stay open as the virtual surface while runtime updates continue to repaint it.</action>
<verify>Renderer-focused coverage still proves known key-count layouts render correctly in browser mode, and emulator boot does not require a second rendering pipeline.</verify>
<done>The emulator uses the shipped browser renderer rather than a parallel preview renderer.</done>
</task>

<task>
<file>packages/cli/src/cli/commands/start.test.ts</file>
<action>Add focused tests for the explicit emulator command/mode, including hardware-free startup, reuse of config/theme/runtime seams, and browser-renderer initialization from a virtual device shape.</action>
<verify>`vitest packages/cli/src/cli/commands/start.test.ts` passes with new emulator-mode coverage.</verify>
<done>The first tracer bullet is protected by targeted command-level tests.</done>
</task>

<task>
<file>packages/cli/fixtures/phase-22/</file>
<action>Add a committed review fixture and/or example config that is valid in emulator mode and visibly proves the browser page is rendering the real configured deck instead of a mock placeholder surface.</action>
<verify>The fixture is usable by follow-on plans and demonstrates a real browser-rendered deck as the review artifact for this slice.</verify>
<done>Plan 22-01 is demoable as "run the emulator and see the deck".</done>
</task>

## Must Haves

- A dedicated CLI emulator mode/command starts successfully with no attached Stream Deck.
- The emulator page shows the real configured deck rendered through the existing browser-renderer path.
- The startup path reuses the normal config/theme/runtime setup rather than a preview-only shortcut.
- Focused tests cover hardware-free emulator startup.
