---
wave: 5
depends_on:
  - 23-04-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx
  - packages/cli/src/cli/commands/start.test.ts
  - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md
  - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md
autonomous: true
single_layer_justified: true
objective: "Close the repeated Phase 23 rerun blocker by restoring the currently dirty raw fixture entrypoint back to the committed helper-based render contract and keeping the runtime-level regression plus rerun evidence aligned with that real source of drift."
must_haves:
  truths:
    - "`packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx` no longer returns raw JSX directly from `createInstance()` and instead routes render output through the helper-based contract again."
    - "Focused startup/runtime coverage passes against the live worktree and proves the shipped Phase 23 sample config reaches valid renderable output without `React is not defined`."
    - "Phase 23 UAT and verification artifacts preserve the repeated rerun failure evidence and point the next rerun at `23-05-PLAN.md`."
  artifacts:
    - packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx
    - packages/cli/src/cli/commands/start.test.ts
    - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md
    - .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md
  key_links:
    - "`packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx` must stay aligned with the helper-based contract proven by the runtime regression in `packages/cli/src/cli/commands/start.test.ts`, not drift back to direct JSX."
    - "The rerun evidence in `23-UAT.md` and `23-VERIFICATION.md` must remain visible while `23-05-PLAN.md` becomes the explicit next rerun path."
---

# Plan 23-05: Re-close Fixture Render Drift In The Live Worktree

<objective>
Close the repeated rerun blocker at the narrowest real seam: the shipped raw fixture entrypoint in the current worktree has drifted away from the helper-based render contract again, even though the previous closure already established the right pattern and regression. This plan restores that live fixture entrypoint, verifies the runtime regression passes in the current worktree, and updates the phase artifacts so the rerun history remains accurate.
</objective>

## Tasks

<task id="23-05-01">
<title>Restore the dirty fixture entrypoint and re-prove runtime renderability in the live worktree</title>
<files>
- packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Replace the current dirty `createInstance()` return path in `packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx` so it no longer returns raw JSX directly. Restore it to the helper-based render contract already established by the adjacent content helper and the previous Phase 23 closure work, and keep the focused runtime-level regression in `packages/cli/src/cli/commands/start.test.ts` passing against the current worktree so the shipped sample config reaches a real renderable button output without `React is not defined`.
</action>
<verify>
Run `pnpm vitest run src/cli/commands/start.test.ts src/addon/loader.test.ts` from `packages/cli`.
</verify>
<done>
The live worktree fixture entrypoint is back on the helper-based contract, and the runtime-level regression passes again instead of failing before `onRenderDeck`.
</done>
</task>

<task id="23-05-02">
<title>Relink the repeated rerun evidence to the new live-worktree closure plan</title>
<files>
- .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md
- .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md
</files>
<action>
Update the Phase 23 UAT and verification artifacts so they preserve the repeated rerun evidence showing `React is not defined`, record that this latest failure was caused by live worktree re-drift in `packages/cli/fixtures/phase-23/local-raw-addon/src/index.tsx`, and make `23-05-PLAN.md` the explicit next rerun path. Keep the prior evidence intact instead of rewriting the history to look clean.
</action>
<verify>
Run `test -f .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md && test -f .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md && rg -n "23-05-PLAN.md|React is not defined|root_cause|rerun|worktree" .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-UAT.md .planning/phases/23-jsx-tsx-addon-buttons-startup-image/23-VERIFICATION.md`.
</verify>
<done>
The repeated rerun blocker remains documented, and both UAT and verification artifacts point the next execution pass at the live-worktree render-contract closure plan.
</done>
</task>
