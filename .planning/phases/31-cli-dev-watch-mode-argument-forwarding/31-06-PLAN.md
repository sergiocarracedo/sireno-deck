---
wave: 6
depends_on:
  - 31-05-PLAN.md
gap_closure: true
files_modified:
  - package.json
  - packages/cli/src/cli/commands/start.test.ts
  - .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md
autonomous: true
single_layer_justified: false
objective: "Developers can run bare or forwarded `pnpm run cli:dev` from zsh without the shell aborting on unmatched watch globs before `tsx watch` starts."
must_haves:
  truths:
    - "Root `package.json#scripts.cli:dev` shell-proofs the `tsx watch --include` glob arguments so zsh does not expand missing repo-root patterns before the watch command runs."
    - "Bare `pnpm run cli:dev` and forwarded runs such as `pnpm run cli:dev emulate --port 8912` can both reach the real launcher/runtime seam instead of failing with `zsh: no matches found`."
    - "The shipped root-script regression in `packages/cli/src/cli/commands/start.test.ts` pins the shell-safe watch command strongly enough that future unquoted-glob regressions fail before manual UAT."
    - "Phase 31 UAT points the remaining bare and forwarded rerun gaps at this shell-safe root-script closure plan."
  artifacts:
    - package.json
    - packages/cli/src/cli/commands/start.test.ts
    - .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md
  key_links:
    - "The remaining rerun failures now happen before `packages/cli/src/cli/dev-watch.ts` runs at all, so the fix belongs in the root script seam, not the launcher/runtime seam already closed by 31-03 through 31-05."
    - "`packages/cli/src/cli/commands/start.test.ts` is the repo-owned seam that should pin the exact shell-safe root watch command seen by users."
---

# Plan 31-06: Shell-Proof The Root cli:dev Script

<objective>
Close the remaining Phase 31 rerun gaps by fixing the root script one layer earlier than the launcher/runtime seam. The live `cli:dev` contract is now correct in principle, but zsh expands the raw `--include` globs in `package.json#scripts.cli:dev` before `tsx watch` runs and aborts on repo-root paths that do not exist. This slice should make the root watch command shell-safe, pin that exact contract in the shipped regression seam, and update the preserved UAT history so both remaining rerun gaps point at this closure plan.
</objective>

## Tasks

<task id="31-06-01">
<title>Quote or otherwise shell-proof the root cli:dev watch command</title>
<files>
- package.json
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Update `package.json#scripts.cli:dev` so the `tsx watch --include` arguments are passed through to `tsx` literally on zsh instead of being expanded by the shell first. Keep the previously verified launcher target (`packages/cli/src/cli/dev-watch.ts`), `pnpm exec tsx watch`, and the same logical watch graph, but make the command safe for unmatched patterns in the user's shell. Then tighten `packages/cli/src/cli/commands/start.test.ts` so the shipped regression explicitly proves the root script is shell-safe and still describes the full-process raw-source restart seam truthfully.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts -t "documents the workspace-root cli:dev script as the full-process raw-source restart seam"` and `pnpm run cli:dev --help` from the repo root far enough to confirm the shell no longer aborts with `zsh: no matches found`.
</verify>
<done>
The workspace-root watch command starts from zsh without unmatched-glob shell errors, and the shipped regression pins that shell-safe contract.
</done>
</task>

<task id="31-06-02">
<title>Point the preserved Phase 31 rerun gaps at the shell-safe root-script closure</title>
<files>
- .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md
</files>
<action>
Update the remaining rerun-specific bare and forwarded gaps in `31-UAT.md` so they clearly state that the current blocker is the shell-level root-script seam and that `31-06-PLAN.md` is the explicit rerun plan. Preserve the original failed reports and all earlier closure history; do not rewrite the prior runtime-bug chronology.
</action>
<verify>
Run `rg -n "31-06-PLAN.md|zsh: no matches found|package.json#scripts.cli:dev|shell" .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md`.
</verify>
<done>
The preserved UAT history now points the two remaining rerun failures at the correct shell-safe root-script closure plan.
</done>
</task>
