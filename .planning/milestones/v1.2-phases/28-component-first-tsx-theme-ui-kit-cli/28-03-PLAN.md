---
wave: 3
depends_on:
  - 28-01-PLAN.md
  - 28-02-PLAN.md
files_modified:
  - packages/cli/src/addon/api.ts
  - packages/cli/src/index.ts
  - packages/cli/src/render/dom-host.test.tsx
  - packages/cli/src/cli/commands/start.test.ts
  - packages/cli/src/builtin-addons/date-time/index.ts
  - packages/cli/src/builtin-addons/date-time/buttons/date-time.tsx
  - packages/cli/src/builtin-addons/date-time/index.test.ts
  - packages/cli/src/builtin-addons/emoji-selector/index.ts
  - packages/cli/src/builtin-addons/emoji-selector/index.test.ts
  - packages/cli/src/deck/runtime.test.ts
  - packages/cli/fixtures/phase-23/local-raw-addon/src/content.tsx
  - packages/cli/fixtures/phase-23/README.md
  - packages/cli/fixtures/phase-9/jsx-addon-authoring-example.tsx
  - README.md
autonomous: true
single_layer_justified: false
objective: "The remaining shipped addon families, public exports, committed authoring fixtures, and public docs finish the hard cut from helper factories to the component-first TSX kit, so no supported helper-based authoring contract remains in parallel."
must_haves:
  truths:
    - "Date-time and emoji-selector stop depending on helper factories and render through the new TSX kit on their real runtime seams."
    - "Public exports, committed authoring fixtures, and addon-authoring docs stop advertising helper factories as a supported surface."
    - "Focused tests prove the remaining shipped addon families and raw-source authoring examples survived the hard cut."
  artifacts:
    - packages/cli/src/addon/api.ts
    - packages/cli/src/builtin-addons/date-time/index.ts
    - packages/cli/src/builtin-addons/emoji-selector/index.ts
    - packages/cli/fixtures/phase-9/jsx-addon-authoring-example.tsx
    - README.md
  key_links:
    - "packages/cli/src/addon/api.ts and packages/cli/src/index.ts stop advertising helper factories as a supported public authoring surface."
    - "README.md and the committed Phase 9/23 authoring fixtures become the public proof that Phase 28 really cut over to the TSX kit."
---

# Plan 28-03: Finish The Hard Cut Across Remaining Addons, Fixtures, And Docs

<objective>
Finish the hard cut promised in the context: the remaining shipped addon families, public exports, committed authoring fixtures, and public docs must stop depending on helper factories. This slice depends on the first two because it is the cleanup and breadth-completion pass that removes the last supported helper-based authoring story after the first fixture proof and the core-button/runtime migration are already in place.
</objective>

## Tasks

<task id="28-03-01">
<title>Migrate remaining shipped addon families and remove helper exports from the public surface</title>
<files>
- packages/cli/src/addon/api.ts
- packages/cli/src/index.ts
- packages/cli/src/cli/commands/start.test.ts
- packages/cli/src/deck/runtime.test.ts
- packages/cli/src/builtin-addons/date-time/index.ts
- packages/cli/src/builtin-addons/date-time/buttons/date-time.tsx
- packages/cli/src/builtin-addons/date-time/index.test.ts
- packages/cli/src/builtin-addons/emoji-selector/index.ts
- packages/cli/src/builtin-addons/emoji-selector/index.test.ts
</files>
<action>
Migrate the remaining helper-dependent shipped addon families, specifically date-time and emoji-selector, onto `Icon`, `Text`, `Chip`, normal TSX composition, and `ButtonSurface` where needed. In the same cut, remove helper-factory exports from `packages/cli/src/addon/api.ts` and `packages/cli/src/index.ts` so the public surface stops advertising the deleted authoring path once all shipped addon families have landed on the new kit. Keep verification anchored on the live seams already used by the repo: mounted button rendering, startup/runtime integration, and the shipped addon tests.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/builtin-addons/date-time/index.test.ts src/builtin-addons/emoji-selector/index.test.ts src/deck/runtime.test.ts src/cli/commands/start.test.ts`.
</verify>
<done>
The remaining shipped addon families no longer rely on helper factories, and the public addon surface has stopped exporting that deleted contract.
</done>
</task>

<must_haves>
- Do not leave helper factories available as a shadow compatibility API once the remaining shipped addon families are migrated.
- Keep runtime-owned semantics and start-path proofs authoritative while changing only the authoring/presentation surface.
- Public docs and sample fixtures must match the shipped authoring contract after the cut.
</must_haves>

<task id="28-03-02">
<title>Update authoring fixtures and docs so the supported story matches the shipped cutover</title>
<files>
- packages/cli/fixtures/phase-23/local-raw-addon/src/content.tsx
- packages/cli/fixtures/phase-23/README.md
- packages/cli/fixtures/phase-9/jsx-addon-authoring-example.tsx
- README.md
- packages/cli/src/render/dom-host.test.tsx
</files>
<action>
Update the committed authoring examples and public docs so they describe the real Phase 28 surface instead of helper-based alternatives. That includes migrating the helper-based Phase 9 JSX example, refreshing the Phase 23 raw-source addon fixture note, and rewriting the public README guidance so addon authors are pointed at the component-first `Icon`/`Text`/`Chip` kit rather than deleted helper factories. Extend the narrowest focused render proof needed so docs/examples are still backed by executable coverage rather than prose alone.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/dom-host.test.tsx src/cli/commands/start.test.ts` and `grep -n "Icon\|Text\|Chip\|component-first" README.md packages/cli/fixtures/phase-9/jsx-addon-authoring-example.tsx packages/cli/fixtures/phase-23/README.md`.
</verify>
<done>
The committed authoring examples and public docs now tell the same TSX-kit story the shipped runtime and exports actually support.
</done>
</task>
