---
wave: 7
depends_on:
  - 24-07-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/cli/commands/start.ts
  - packages/cli/src/config/theme.ts
  - packages/cli/src/config/theme.test.ts
  - packages/cli/src/cli/commands/start.test.ts
  - .planning/phases/24-mounted-addon-render-contract/24-UAT.md
autonomous: true
single_layer_justified: false
objective: "The HTTP-served emulator preserves the mounted deck's theme CSS and loads theme fonts through browser-loadable asset URLs instead of dropping style blocks or leaking `file://` theme font paths."
must_haves:
  truths:
    - "The emulator page receives and applies the theme utility stylesheet plus theme asset stylesheet for the mounted deck instead of stripping them when it serves deck updates."
    - "Theme stylesheet asset URLs used by the emulator path are browser-loadable and no longer rely on `file://...` font URLs."
    - "Focused emulator/theme regression coverage proves the shipped dark theme CSS and fonts are present on the emulator page without regressing the existing asset or deck-patching fixes."
    - "Phase 24 rerun UAT evidence remains linked to this closure plan."
  artifacts:
    - packages/cli/src/cli/commands/start.ts
    - packages/cli/src/config/theme.ts
    - packages/cli/src/config/theme.test.ts
    - packages/cli/src/cli/commands/start.test.ts
    - .planning/phases/24-mounted-addon-render-contract/24-UAT.md
  key_links:
    - "`packages/cli/src/render/dom-host.tsx` remains the source of theme utility and theme asset styles for the mounted deck HTML."
    - "`packages/cli/src/cli/commands/start.ts` stops stripping those styles out of the emulator transport and serves any referenced theme assets through browser-loadable URLs."
---

# Plan 24-08: Restore Theme CSS And Fonts On The Emulator Path

<objective>
Close the remaining Phase 24 rerun UAT gap by making the user-facing emulator preserve the same theme CSS surface that `renderDomDeck(...)` already emits and by ensuring any theme font assets referenced from that CSS are browser-loadable over the HTTP emulator path. This should stay narrow: do not redesign the runtime or theme package model, just stop the emulator transport from dropping or breaking the theme stylesheet seam.
</objective>

## Tasks

<task id="24-08-01">
<title>Preserve mounted deck theme styles and browser-safe theme asset URLs on the emulator path</title>
<files>
- packages/cli/src/cli/commands/start.ts
- packages/cli/src/config/theme.ts
</files>
<action>
Refactor the emulator transport so it no longer reduces every deck update to just the extracted `#deck-root` subtree when the mounted deck HTML also carries required theme utility or theme asset stylesheet blocks. Preserve the existing keyed deck-root patching behavior from `24-06`, but add the narrowest mechanism that keeps the theme styles applied on the emulator page as the deck updates. At the same time, stop `packages/cli/src/config/theme.ts` from baking theme stylesheet asset URLs into `file://...` when the HTTP emulator path still needs browser-loadable font URLs; preserve early missing-asset failures and keep the non-emulator capture path truthful.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/config/theme.test.ts src/cli/commands/start.test.ts` and confirm the emulator path exposes theme CSS/fonts without regressing the existing deck asset or keyed patching tests.
</verify>
<done>
The emulator page keeps the mounted deck's theme CSS active and theme fonts load through browser-safe URLs instead of missing stylesheet blocks or `file://` font paths.
</done>
</task>

<task id="24-08-02">
<title>Lock the emulator theme fix with focused regression coverage and UAT linkage</title>
<files>
- packages/cli/src/config/theme.test.ts
- packages/cli/src/cli/commands/start.test.ts
- .planning/phases/24-mounted-addon-render-contract/24-UAT.md
</files>
<action>
Add focused regression coverage that proves the shipped dark theme utility/styles/font-face surface survives end-to-end on the emulator page and no longer depends on stripped style tags or `file://` font URLs. Then keep the rerun UAT artifact linked to this exact closure plan so the next rerun can explicitly close the remaining theme/css gap.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/config/theme.test.ts src/cli/commands/start.test.ts` and `rg -n "24-08-PLAN.md|theme css|fonts are loaded|root_cause" .planning/phases/24-mounted-addon-render-contract/24-UAT.md`.
</verify>
<done>
Theme-css/font loading on the emulator is regression-pinned and the rerun UAT gap points directly to this closure plan.
</done>
</task>
