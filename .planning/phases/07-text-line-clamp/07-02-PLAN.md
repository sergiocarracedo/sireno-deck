---
phase: 07
plan: 02
wave: 2
depends_on: [07-01]
files_modified:
  - packages/cli/src/ui/primitives/Text.tsx
  - packages/cli/src/ui/theme-presentation.tsx
  - packages/cli/src/ui/primitives/__tests__/Text.test.tsx
autonomous: true
single_layer_justified: false
objective: Add reserveSpace support to line-clamp fit and ensure theme presentation passes through the new fit form
---

# Plan 07-02: reserveSpace + Theme Integration

## Objective

Implement `reserveSpace: true` behavior using `min-height: (lines * lineHeight)` so empty content reserves vertical space. Ensure the theme presentation system can pass through the resolved fit config to theme implementations.

## Tasks

<task>
<file>packages/cli/src/ui/primitives/Text.tsx</file>
<action>When resolveFit() returns `{ kind: 'line-clamp', lines, reserveSpace: true }`, apply min-height to the composed style. Compute as `${lines * lineHeight}em` using the existing lineHeight prop (default 1).</action>
<verify>Empty content with reserveSpace: true renders with min-height equal to lines * lineHeight.</verify>
<done>reserveSpace adds min-height to container; empty content occupies space for N lines.</done>
</task>

<task>
<file>packages/cli/src/ui/primitives/Text.tsx</file>
<action>Update the themeUi.primitives.text() call to pass the resolved fit config (not the raw prop) so theme implementations receive a consistent shape.</action>
<verify>Theme override receives a normalized fit object with kind discriminator.</verify>
<done>Theme implementations can handle the normalized fit shape uniformly.</done>
</task>

<task>
<file>packages/cli/src/ui/primitives/__tests__/Text.test.tsx</file>
<action>Add test cases: (a) reserveSpace: true with empty content applies min-height, (b) reserveSpace: true with short content still reserves full height, (c) reserveSpace: false (or undefined) does not apply min-height.</action>
<verify>All reserveSpace test cases pass.</verify>
<done>reserveSpace behavior is tested in 3+ scenarios.</done>
</task>

<task>
<file>packages/cli/src/ui/primitives/Text.tsx</file>
<action>Verify typecheck and run full test suite for ui package. Confirm no regressions in Chip, weather, media, value-display, brightness addons that use Text.</action>
<verify>pnpm typecheck passes. pnpm test for ui primitives passes.</verify>
<done>No regressions; all existing Text usages continue to work.</done>
</task>

## Must-Haves

- [ ] `reserveSpace: true` applies min-height based on lines * lineHeight
- [ ] Empty content with reserveSpace reserves full vertical space
- [ ] Theme presentation receives normalized fit config
- [ ] Tests cover reserveSpace behavior in all scenarios
- [ ] Full typecheck passes with no errors
