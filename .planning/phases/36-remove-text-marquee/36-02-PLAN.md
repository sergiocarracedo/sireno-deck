---
wave: 2
depends_on:
  - 36-01
files_modified:
  - packages/cli/src/builtin-addons/media-player/media-player-button.tsx
  - packages/cli/src/render/dom-host.test.tsx
  - packages/cli/src/config/theme/theme.test.ts
  - packages/cli/src/builtin-addons/media-player/index.test.ts
autonomous: true
single_layer_justified: false
objective: "Migrate the media-player addon from fit=marquee to fit=ellipsis and update all test assertions so the full system compiles and tests pass without the marquee option."
must_haves:
  truths:
    - "Media-player title and artist `<Text>` elements use `fit=\"ellipsis\"` instead of `fit=\"marquee\"`."
    - "`dom-host.test.tsx` marquee assertions are replaced with equivalent ellipsis assertions."
    - "`theme.test.ts` fixture `fit: 'marquee'` is replaced with `fit: 'ellipsis'`."
    - "`media-player/index.test.ts` marquee assertion is replaced with an ellipsis assertion."
    - "The full test suite passes: `pnpm test`."
  artifacts:
    - packages/cli/src/builtin-addons/media-player/media-player-button.tsx
    - packages/cli/src/render/dom-host.test.tsx
    - packages/cli/src/config/theme/theme.test.ts
    - packages/cli/src/builtin-addons/media-player/index.test.ts
  key_links:
    - "Plan 36-01 removed the `'marquee'` string from `TextFit` — these files must stop referencing it to compile."
    - "Media-player title/artist overflow is now truncated with `...` instead of auto-scrolling."
---

# Plan 36-02: Migrate Consumers and Fix Tests

<objective>
After Plan 36-01 strips `'marquee'` from the type system, fix all compile errors by migrating the media-player addon to `fit="ellipsis"` and updating every test assertion that references marquee-related DOM attributes or CSS classes. After this plan, the full project compiles and all tests pass with zero references to marquee.
</objective>

## Tasks

<task id="36-02-01">
<title>Replace marquee with ellipsis in media-player-button.tsx</title>
<files>
- packages/cli/src/builtin-addons/media-player/media-player-button.tsx
</files>
<action>
In `packages/cli/src/builtin-addons/media-player/media-player-button.tsx`:
1. Line 202: change `fit="marquee"` to `fit="ellipsis"` on the title `<Text>`.
2. Line 205: change `fit="marquee"` to `fit="ellipsis"` on the artist `<Text>`.
</action>
<verify>
Run `pnpm typecheck` — media-player-button.tsx must compile without errors.
</verify>
<done>
Media-player title and artist now use `ellipsis` overflow instead of marquee scrolling.
</done>
</task>

<task id="36-02-02">
<title>Update test assertions from marquee to ellipsis</title>
<files>
- packages/cli/src/render/dom-host.test.tsx
- packages/cli/src/config/theme/theme.test.ts
- packages/cli/src/builtin-addons/media-player/index.test.ts
</files>
<action>
Update three test files:

1. `packages/cli/src/render/dom-host.test.tsx`:
   - Line 259: change `fit: 'marquee'` to `fit: 'ellipsis'` in the button config.
   - Line 282: change `data-sireno-default-text-fit="marquee"` assertion to `data-sireno-default-text-fit="ellipsis"`.
   - Line 287: change `data-sireno-text-fit="marquee"` assertion to `data-sireno-text-fit="ellipsis"`.
   - Line 289: remove the `sireno-marquee-track` class assertion (or replace with equivalent ellipsis check — the ellipsis path has no inner span, so use `data-sireno-text-fit="ellipsis"` or a content truncation assertion).

2. `packages/cli/src/config/theme/theme.test.ts`:
   - Line 79: change `fit: 'marquee'` to `fit: 'ellipsis'`.

3. `packages/cli/src/builtin-addons/media-player/index.test.ts`:
   - Line 154: change `data-sireno-text-fit="marquee"` assertion to `data-sireno-text-fit="ellipsis"`.
   - Line 114: update the test description from "marquee metadata" to "ellipsis metadata".
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run` to confirm all tests pass.
</verify>
<done>
All test assertions reference `ellipsis` instead of `marquee`, and the full test suite passes.
</done>
</task>

<must_haves>
- Do not leave any `'marquee'` string literal or `sireno-marquee-*` reference in the four files listed above.
- The media-player button must still render correctly — title and artist overflow should truncate with ellipsis instead of scrolling.
</must_haves>
