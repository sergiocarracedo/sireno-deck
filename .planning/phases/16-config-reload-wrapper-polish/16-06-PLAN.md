---
wave: 5
depends_on:
  - 16-04-PLAN.md
  - 16-05-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/render/text-image.ts
  - packages/cli/src/render/text-image.test.ts
  - packages/cli/src/render/reconciler.ts
  - packages/cli/src/cli/commands/start.ts
  - .planning/phases/16-config-reload-wrapper-polish/16-UAT.md
autonomous: true
single_layer_justified: false
objective: "Close the Phase 16 UAT gap where changing a shared-wrapper button's `accent` from `success` to `#7c3aed` reloads successfully but produces no visible accent change on-device, while keeping the accent override narrow to the existing shared/default render path."
must_haves:
  truths:
    - "On the shared/default wrapper path, changing a button `accent` override from a theme token to a raw hex color produces a visibly different rendered card without restarting the daemon."
    - "The fix stays narrow to the existing shared/default accent path and does not widen Phase 16 into a broader styling system."
    - "Focused render-path tests prove the visible difference between token-based and raw-color accent output for the same shared-wrapper card content."
    - "The Phase 16 UAT record links the shared-card accent-visibility gap to this closure plan and notes that Test 4 must be rerun on the real `/tmp/sireno-phase16-uat` fixture path."
  artifacts:
    - packages/cli/src/render/text-image.ts
    - packages/cli/src/render/text-image.test.ts
    - packages/cli/src/render/reconciler.ts
    - packages/cli/src/cli/commands/start.ts
    - .planning/phases/16-config-reload-wrapper-polish/16-UAT.md
  key_links:
    - "`packages/cli/src/render/reconciler.ts` and `packages/cli/src/cli/commands/start.ts` must carry the accent override actually used by the shared/default renderer on live reload."
    - "`packages/cli/src/render/text-image.ts` must apply the accent override to a visibly meaningful part of the shared/default card instead of resolving it into a path that leaves the card visually unchanged."
    - "`packages/cli/src/render/text-image.test.ts` must pin the exact token-vs-raw accent visibility contract that failed in manual UAT."
---

# Plan 16-06: Close The Shared-Card Accent Visibility UAT Gap

<objective>
Fix the specific Phase 16 UAT regression where the shared-wrapper accent override reloads cleanly but a raw color like `#7c3aed` does not visibly change the card on the real device path. A completed plan should make that exact UAT edit observably change the shared/default card while preserving the already-shipped narrow `accent` contract.
</objective>

## Tasks

<task id="16-06-01">
<title>Trace and fix the live shared-wrapper accent path so raw colors visibly affect the card</title>
<files>
- packages/cli/src/render/text-image.ts
- packages/cli/src/render/reconciler.ts
- packages/cli/src/cli/commands/start.ts
</files>
<action>
Trace the existing shared/default accent override path from render descriptions through the live startup/reload renderer and fix the specific seam that allows reload, footer removal, and deck continuity to work while still leaving the raw accent visually unchanged on-device. Keep the change surgical: use the existing shared/default renderer branch, preserve wrapper/style and explicit-prop authority rules, and do not introduce new styling concepts or broaden the override beyond the shared/default path.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/text-image.test.ts`.
</verify>
<done>
The shared/default renderer visibly changes when the same card switches from `accent: success` to `accent: '#7c3aed'`.
</done>
</task>

<task id="16-06-02">
<title>Pin the failed UAT expectation in tests and link the closure path in Phase 16 UAT</title>
<files>
- packages/cli/src/render/text-image.test.ts
- .planning/phases/16-config-reload-wrapper-polish/16-UAT.md
</files>
<action>
Add or tighten focused render verification so the test suite explicitly proves a visible output difference between the shared/default card rendered with a token accent and the same card rendered with a raw hex accent. Then update `16-UAT.md` so the new shared-card accent-visibility gap includes this closure plan and clearly instructs downstream work to rerun Test 4 on the real `/tmp/sireno-phase16-uat` fixture path after the fix lands.
</action>
<verify>
Run `test -f .planning/phases/16-config-reload-wrapper-polish/16-UAT.md && grep -n "closure_plan\|Test 4\|accent" .planning/phases/16-config-reload-wrapper-polish/16-UAT.md`.
</verify>
<done>
The failed accent-visibility contract is pinned in tests and the UAT record is wired to a concrete gap-closure rerun path.
</done>
</task>
