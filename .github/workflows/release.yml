<<<<<<< HEAD
name: release

on:
  push:
    tags:
      - "@sirenodeck/*"

permissions:
  contents: read

jobs:
  detect:
    name: Detect packages in tag ${{ github.ref_name }}
    runs-on: ubuntu-latest
    outputs:
      package: ${{ steps.parse.outputs.package }}
      package-path: ${{ steps.parse.outputs.package-path }}
      needs-build: ${{ steps.parse.outputs.needs-build }}
    steps:
      - id: parse
        env:
          TAG: ${{ github.ref_name }}
        run: |
          # Tag format: @sirenodeck/<component>-<version>
          # e.g. @sirenodeck/cli-0.1.1, @sirenodeck/addon-pomodoro-0.1.0
          PACKAGE="$TAG"
          case "$TAG" in
            '@sirenodeck/addon-app-shortcuts-'*)
              PATH='packages/addons/app-shortcuts'
              NEEDS_BUILD='true'
              ;;
            '@sirenodeck/addon-coding-agents-'*)
              PATH='packages/addons/coding-agents'
              NEEDS_BUILD='true'
              ;;
            '@sirenodeck/addon-pomodoro-'*)
              PATH='packages/addons/pomodoro'
              NEEDS_BUILD='true'
              ;;
            '@sirenodeck/theme-neon-grids-'*)
              PATH='packages/themes/neon-grids'
              NEEDS_BUILD='false'
              ;;
            '@sirenodeck/theme-riptide-'*)
              PATH='packages/themes/riptide'
              NEEDS_BUILD='false'
              ;;
            *)
              echo "Unknown release tag: $TAG"
              exit 1
              ;;
          esac
          echo "package=$PACKAGE" >> "$GITHUB_OUTPUT"
          echo "package-path=$PATH" >> "$GITHUB_OUTPUT"
          echo "needs-build=$NEEDS_BUILD" >> "$GITHUB_OUTPUT"

  publish:
    name: Publish ${{ needs.detect.outputs.package }}
    needs: detect
    uses: ./.github/workflows/publish-npm.yml
    with:
      package: ${{ needs.detect.outputs.package }}
      package-path: ${{ needs.detect.outputs.package-path }}
      needs-build: ${{ needs.detect.outputs.needs-build }}
    permissions:
      contents: read
      id-token: write
=======
name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  installers:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            script: installer:flatpak
            extra: |
              sudo apt-get update
              sudo apt-get install -y flatpak flatpak-builder
          - os: macos-latest
            script: installer:macos
            extra: |
              npm install -g create-dmg
          - os: windows-latest
            script: installer:windows
            extra: |
              choco install inno-setup -y

    runs-on: ${{ matrix.os }}
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: ${{ matrix.extra }}

      - name: Build CLI + frontend + emulator
        run: pnpm --filter sirenodeck build

      - name: Build installer
        run: pnpm --filter sirenodeck ${{ matrix.script }}

      - name: Upload installer artifacts
        uses: actions/upload-artifact@v4
        with:
          name: installers-${{ matrix.os }}
          path: packages/cli/dist/installer/
          if-no-files-found: error

  attach:
    needs: installers
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
>>>>>>> main
