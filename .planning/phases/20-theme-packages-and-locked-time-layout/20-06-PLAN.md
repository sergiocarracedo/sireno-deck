---
wave: 6
depends_on:
  - 20-05-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/builtin-addons/core-buttons/index.ts
  - packages/cli/src/builtin-addons/emoji-selector/index.ts
  - packages/cli/src/addon/registry.test.ts
  - packages/cli/src/config/loader.test.ts
  - .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md
  - .planning/phases/20-theme-packages-and-locked-time-layout/20-VERIFICATION.md
autonomous: true
single_layer_justified: true
objective: "Close the remaining Phase 20 shared-asset gap by correcting the built-in addon asset registration paths so `builtin://...` and `addon://...` refs resolve inside each addon's own asset directory, with focused tests that prove the real registered filesystem path before the existing Fixture 2 rerun."
must_haves:
  truths:
    - "Built-in addon asset definitions resolve from each addon's own directory instead of dropping the addon folder segment"
    - "Focused tests assert the resolved filesystem path for shipped builtin/addon asset refs, not only a filename fragment or `file://` prefix"
    - "Phase 20 UAT and verification artifacts point the remaining Fixture 2 rerun at this path-registration closure plan"
  artifacts:
    - packages/cli/src/builtin-addons/core-buttons/index.ts
    - packages/cli/src/builtin-addons/emoji-selector/index.ts
    - packages/cli/src/addon/registry.test.ts
    - packages/cli/src/config/loader.test.ts
    - .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md
  key_links:
    - "`packages/cli/src/builtin-addons/core-buttons/index.ts` and `packages/cli/src/builtin-addons/emoji-selector/index.ts` must register asset files relative to their own addon roots, not the parent builtin-addons directory"
    - "`packages/cli/src/addon/registry.test.ts` and `packages/cli/src/config/loader.test.ts` must prove `builtin://core-buttons/...` and `addon://emoji-selector/...` resolve to addon-local asset paths before browser rendering begins"
    - "`.planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md` and `20-VERIFICATION.md` must preserve the failed `20-05` evidence while making the next rerun path explicit"
---

# Plan 20-06: Fix Built-In Addon Asset Registration Paths

<objective>
Close the remaining Phase 20 asset failure at the true shared source: built-in addon asset declarations are registering paths outside their own addon directories, so every later layer receives the wrong filesystem target even when browser URL handling is correct. This plan fixes the asset declarations, pins the actual resolved filesystem path in focused tests, and updates the phase artifacts so Fixture 2 reruns against the right closure path.
</objective>

## Tasks

<task id="20-06-01">
<title>Correct built-in addon asset declarations and pin addon-local resolver paths</title>
<files>
- packages/cli/src/builtin-addons/core-buttons/index.ts
- packages/cli/src/builtin-addons/emoji-selector/index.ts
- packages/cli/src/addon/registry.test.ts
- packages/cli/src/config/loader.test.ts
</files>
<action>
Fix the built-in addon asset maps so every shipped asset path is registered relative to the addon file that declares it. In practice, the `new URL('../assets/...', import.meta.url)` entries under `core-buttons/index.ts` and `emoji-selector/index.ts` need to become addon-local `./assets/...` references so the registry records `.../core-buttons/assets/...` and `.../emoji-selector/assets/...` instead of `.../builtin-addons/assets/...`. Then strengthen focused resolver coverage so tests assert the full resolved filesystem path shape for `builtin://core-buttons/clock.svg` and at least one `addon://emoji-selector/...` asset before those refs are converted into browser `file://` URLs.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/addon/registry.test.ts src/config/loader.test.ts src/builtin-addons/emoji-selector/index.test.ts`.
</verify>
<done>
The shared asset pipeline now registers shipped builtin/addon asset refs under each addon's own `assets/` directory, and focused tests would fail again if the addon folder segment disappeared.
</done>
</task>

<task id="20-06-02">
<title>Relink the remaining Fixture 2 gap to the corrected asset-registration path</title>
<files>
- .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md
- .planning/phases/20-theme-packages-and-locked-time-layout/20-VERIFICATION.md
</files>
<action>
Update the Phase 20 UAT and verification artifacts so the failed shared-asset rerun points at `20-06-PLAN.md` as the current closure path, preserves the evidence that `20-05` fixed the browser capture seam but not the bad registered file paths, and explicitly instructs downstream work to rerun `packages/cli/fixtures/phase-20/config.asset-pipeline.yml` after the built-in addon asset declarations are corrected. Keep the docs synchronized so phase status cannot drift back to an already-fixed claim.
</action>
<verify>
Run `test -f .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md && test -f .planning/phases/20-theme-packages-and-locked-time-layout/20-VERIFICATION.md && rg -n "20-06-PLAN.md|20-05|addon folder segment|core-buttons/assets|emoji-selector/assets|Fixture 2" .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md .planning/phases/20-theme-packages-and-locked-time-layout/20-VERIFICATION.md`.
</verify>
<done>
The remaining Fixture 2 failure is linked to the corrected asset-registration plan and the next real browser/device rerun is explicit in both UAT and verification artifacts.
</done>
</task>
