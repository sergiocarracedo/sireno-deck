---
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/ui/Text.tsx
  - packages/cli/src/render/theme-utilities.ts
  - packages/cli/src/themes/default/ButtonFrame.tsx
autonomous: false
single_layer_justified: false
objective: "Strip 'marquee' from the TextFit union, its CSS class/rendering rules, and the theme frame contract so the option can no longer be selected."
must_haves:
  truths:
    - "`TextFit` union in `Text.tsx` is narrowed to `'ellipsis' | 'shrink' | 'wrap'`."
    - "`fitModesClasses.marquee` and the `<span className=\"sireno-marquee-track\">` conditional branch in `Text.tsx` are removed."
    - "`.sireno-text-fit-marquee`, `.sireno-marquee-track`, and `@keyframes sireno-marquee-scroll` are removed from `theme-utilities.ts`."
    - "`ThemeTextProps.fit` in `ButtonFrame.tsx` is narrowed to `'ellipsis' | 'shrink' | 'wrap'`."
  artifacts:
    - packages/cli/src/ui/Text.tsx
    - packages/cli/src/render/theme-utilities.ts
    - packages/cli/src/themes/default/ButtonFrame.tsx
  key_links:
    - "`ThemeTextProps.fit` mirrors `TextFit` — they must stay in sync or the theme wrapper type is misleading."
    - "CSS is inlined in TypeScript via `getSirenoRuntimeStylesheet()` — no separate CSS files to check."
---

# Plan 36-01: Strip Marquee from Core Text Surface

<objective>
Remove the `'marquee'` option from the `TextFit` discriminated union, its render branch in `Text`, its CSS animation rules in `theme-utilities.ts`, and its mirror in the theme frame contract `ThemeTextProps.fit`. After this plan, `'marquee'` can no longer be selected — but consumers (media-player, tests) will have compile errors that Plan 36-02 fixes.
</objective>

## Tasks

<task id="36-01-01">
<title>Remove marquee from Text.tsx type, class map, and render branch</title>
<files>
- packages/cli/src/ui/Text.tsx
</files>
<action>
In `packages/cli/src/ui/Text.tsx`:
1. Narrow `TextFit` type at line 239 from `'ellipsis' | 'marquee' | 'shrink' | 'wrap'` to `'ellipsis' | 'shrink' | 'wrap'`.
2. Remove the `marquee` entry from `fitModesClasses` at line 270.
3. Remove the `fit === 'marquee'` conditional branch (lines 291-297) — the `<span className="sireno-marquee-track">` wrapper and the ternary — collapsing to always render `renderedChildren` directly.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/ui/Text.test.ts` if it exists; otherwise run `pnpm typecheck` to confirm no internal type errors from Text.tsx itself.
</verify>
<done>
`Text.tsx` no longer accepts or renders `'marquee'` — the `TextFit` union is narrowed, the CSS class map has no marquee entry, and no conditional `<span>` wrapper is created for marquee fit.
</done>
</task>

<task id="36-01-02">
<title>Remove marquee CSS rules from theme-utilities.ts</title>
<files>
- packages/cli/src/render/theme-utilities.ts
</files>
<action>
In `packages/cli/src/render/theme-utilities.ts`, remove the three marquee-related CSS strings from the `getSirenoRuntimeStylesheet()` array (lines 100, 101, 103):
- `'.sireno-text-fit-marquee{position:relative;}'`
- `'.sireno-marquee-track{display:inline-block;min-width:max-content;padding-inline-end:100%;animation:sireno-marquee-scroll 8s linear infinite;}'`
- `'@keyframes sireno-marquee-scroll{0%{transform:translateX(0%);}100%{transform:translateX(-50%);}}'`
</action>
<verify>
No marquee CSS classes or keyframes remain in the file. Run `pnpm typecheck` to confirm no breakage.
</verify>
<done>
The CSS animation for marquee is fully removed — no browser repaints triggered by the marquee scroll keyframes.
</done>
</task>

<task id="36-01-03">
<title>Narrow ThemeTextProps.fit in ButtonFrame.tsx</title>
<files>
- packages/cli/src/themes/default/ButtonFrame.tsx
</files>
<action>
In `packages/cli/src/themes/default/ButtonFrame.tsx`, narrow `ThemeTextProps.fit` at line 24 from `'ellipsis' | 'marquee' | 'shrink' | 'wrap'` to `'ellipsis' | 'shrink' | 'wrap'`.
</action>
<verify>
Run `pnpm typecheck` — the theme frame file must compile without errors.
</verify>
<done>
`ThemeTextProps.fit` matches the narrowed `TextFit` union — no misleading dead type member.
</done>
</task>

<must_haves>
- Do not touch consumer files (media-player, tests) in this plan — Plan 36-02 handles those.
- Do not leave any `'marquee'` string literal in the three files listed above.
</must_haves>
