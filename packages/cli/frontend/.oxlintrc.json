{
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["**/emulator/**"],
            "message": "Frontend packages/cli/frontend cannot import from emulator subproject. Emulator and frontend communicate only via the bridge (WS)."
          }
        ]
      }
    ]
  }
}
