---
wave: 5
depends_on:
  - 20-04-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/render/browser-renderer.ts
  - packages/cli/src/render/browser-renderer.test.ts
  - packages/cli/src/cli/commands/start.ts
  - packages/cli/src/render/dom-host.tsx
  - .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md
autonomous: true
single_layer_justified: false
objective: "Close the remaining Phase 20 UAT gap where the real Playwright/browser-capture path still renders shared addon/config image assets as broken squares even after URL normalization, by reproducing the shipped failure in the browser renderer itself and fixing the actual browser-load/capture seam rather than only static-markup output."
must_haves:
  truths:
    - "The real browser-renderer capture path can load the shared asset fixture's addon/config image refs as visible images instead of returning a square plus broken-image indicator on the shipped browser/device path."
    - "Focused tests reproduce the actual browser-renderer failure mode using the browser capture seam (`createBrowserRenderer` / `page.setContent` / screenshot), not only static HTML assertions."
    - "The fix stays in the shared browser render/capture seam and does not introduce another emoji-selector-only patch."
    - "The Phase 20 UAT record links the remaining Fixture 2 failure to this closure plan and makes the rerun path explicit after the fix lands."
  artifacts:
    - packages/cli/src/render/browser-renderer.ts
    - packages/cli/src/render/browser-renderer.test.ts
    - packages/cli/src/cli/commands/start.ts
    - .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md
  key_links:
    - "`packages/cli/src/cli/commands/start.ts` and `packages/cli/src/render/browser-renderer.ts` together own the shipped browser/device path, so the fix must be visible there rather than only in `renderReactNodeToHtml(...)` output."
    - "`packages/cli/src/render/browser-renderer.test.ts` must pin the real image-load/capture contract that failed in manual UAT."
    - "`packages/cli/src/render/dom-host.tsx` may be touched only if the browser renderer needs a different deck-document contract; the phase must stay on the shared browser path rather than adding widget-local rendering branches."
---

# Plan 20-05: Close The Real Browser Capture Asset Rendering Gap

<objective>
Fix the remaining Phase 20 asset bug at the actual shipped layer: the browser renderer that feeds Playwright screenshots into Stream Deck key buffers. The previous closure attempt proved static markup looked browser-loadable, but real UAT still failed, so this plan must reproduce the failure in the capture path, repair that seam, and update the UAT record to point at the new rerun.
</objective>

## Tasks

<task id="20-05-01">
<title>Reproduce and fix the real browser-renderer asset load failure</title>
<files>
- packages/cli/src/render/browser-renderer.ts
- packages/cli/src/render/browser-renderer.test.ts
- packages/cli/src/cli/commands/start.ts
- packages/cli/src/render/dom-host.tsx
</files>
<action>
Trace the shipped browser/device path end-to-end from `renderDomDeckSurface(...)` into `createBrowserRenderer(...)`, `page.setContent(...)`, and screenshot capture, then add a focused reproduction for why image refs that look valid in static HTML still render as broken placeholders on the real surface. Implement the smallest shared fix at that layer — for example by changing how the browser page receives asset-backed HTML, adding the missing base/document context, or otherwise making the capture path actually able to load local assets. Keep the solution generic to all shared browser-consumed images and do not add another emoji-selector-specific branch.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/browser-renderer.test.ts src/render/dom-host.test.tsx src/config/loader.test.ts src/builtin-addons/emoji-selector/index.test.ts`.
</verify>
<done>
The browser-renderer capture seam itself proves that shared asset-backed images survive into captured key buffers instead of degrading into broken placeholders.
</done>
</task>

<task id="20-05-02">
<title>Update the Phase 20 UAT gap record with the new closure path</title>
<files>
- .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md
- .planning/phases/20-theme-packages-and-locked-time-layout/20-VERIFICATION.md
</files>
<action>
Update the existing Phase 20 UAT/verification artifacts so the failed Fixture 2 gap points at `20-05-PLAN.md` as the new closure path, preserves the evidence that `20-04` was insufficient, and clearly instructs downstream work to rerun `packages/cli/fixtures/phase-20/config.asset-pipeline.yml` on the real browser/device path after this fix lands. Keep the metadata synchronized so the repo does not regress into stale “already fixed” status again.
</action>
<verify>
Run `test -f .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md && test -f .planning/phases/20-theme-packages-and-locked-time-layout/20-VERIFICATION.md && grep -n "20-05-PLAN.md\|20-04\|Fixture 2\|broken image icon\|quare" .planning/phases/20-theme-packages-and-locked-time-layout/20-UAT.md .planning/phases/20-theme-packages-and-locked-time-layout/20-VERIFICATION.md`.
</verify>
<done>
The remaining asset-rendering gap is linked to the correct closure plan and the rerun path is explicit in both UAT and verification artifacts.
</done>
</task>
