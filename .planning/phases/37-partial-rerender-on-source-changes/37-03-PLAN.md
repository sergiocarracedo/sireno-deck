---
wave: 2
depends_on: ["37-01", "37-02"]
files_modified:
  - packages/cli/src/cli/commands/start.test.ts
  - packages/cli/src/deck/runtime.test.ts
autonomous: false
single_layer_justified: true
objective: "Add focused regression tests proving the addon source watching, registry-diff, CSS reload, and structural-change-fallback behaviors shipped in Plans 01 and 02."
---

## Tasks

### 37-03-01: Test addon source watcher in start.test.ts

- **file**: `packages/cli/src/cli/commands/start.test.ts`
- **action**: |
    Add tests for the new `watchAddonSources` function in the existing `start.test.ts`:
    1. "watches addon sources with recursive fs.watch and 100ms debounce" — mock `fs.watch`, advance fake timer, assert `updateAddonRegistry` called once after 100ms
    2. "reloads stylesheet immediately on CSS file change without debounce" — mock `fs.watch`, simulate `.css` change, assert `reloadStylesheet` called immediately (no timer)
    3. "cleanup stops watcher and clears timer" — call returned cleanup, assert timer cleared and watcher closed
    Place these tests in a new `describe('watchAddonSources')` block near the existing `describe('watchConfigFiles')` block. Use the same vi.fn() mocking pattern as the existing watch tests.
- **verify**: `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts -t "watchAddonSources"`
- **done**: When all three addon-watcher tests pass.

### 37-03-02: Test updateAddonRegistry structural vs non-structural

- **file**: `packages/cli/src/deck/runtime.test.ts`
- **action**: |
    Add tests for `updateAddonRegistry` in the existing `runtime.test.ts`:
    1. "non-structural change calls invalidateMountedStore without requestFullReload" — mock registry with same button types but different instance data, call `updateAddonRegistry`, assert `invalidateMountedStore` called, `requestFullReload` NOT called
    2. "structural change (new type added) calls requestFullReload" — mock registry with a new button type, call `updateAddonRegistry`, assert `requestFullReload` called, `invalidateMountedStore` NOT called
    3. "structural change (type removed) calls requestFullReload" — mock registry missing a previously-registered type, call `updateAddonRegistry`, assert `requestFullReload` called
    4. "CSS reload calls reloadStylesheet without invalidateMountedStore" — call `reloadStylesheet`, assert `reloadStylesheet` called, `invalidateMountedStore` NOT called
    Place these in a new `describe('updateAddonRegistry')` block.
- **verify**: `pnpm --filter sireno-deck-cli exec vitest run src/deck/runtime.test.ts -t "updateAddonRegistry"`
- **done**: When all four updateAddonRegistry tests pass.

### 37-03-03: Verify existing watch tests still pass

- **file**: `packages/cli/src/cli/commands/start.test.ts`
- **action**: |
    Run the full `start.test.ts` test suite to ensure no regressions:
    ```
    pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts
    ```
    All existing watchConfigFiles, reloadRuntime, startDaemon, and cli:dev tests must still pass. If any fail, investigate whether the new watcher wires have affected existing timing or cleanup behavior.
- **verify**: `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts`
- **done**: When the full start.test.ts suite passes.

## must_haves

- [ ] `watchAddonSources` tests cover: recursive watch, 100ms debounce, CSS immediate reload, cleanup
- [ ] `updateAddonRegistry` tests cover: non-structural → invalidateMountedStore, structural (add/remove) → requestFullReload, CSS reload → reloadStylesheet
- [ ] Full `start.test.ts` suite passes (no regressions in existing watch/reload tests)
- [ ] Full `runtime.test.ts` suite passes (no regressions in existing invalidate/render tests)