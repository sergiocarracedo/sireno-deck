---
wave: 2
depends_on:
  - 19-01-PLAN.md
files_modified:
  - packages/cli/src/render/button-frame.tsx
  - packages/cli/src/render/button-frame.test.tsx
  - packages/cli/src/addon/api.ts
  - packages/cli/src/index.ts
  - packages/cli/src/builtin-addons/date-time/index.ts
  - packages/cli/src/builtin-addons/date-time/index.test.ts
  - packages/cli/src/builtin-addons/core-buttons/index.test.ts
  - packages/cli/fixtures/phase-19/config.theme-typography.yml
  - .planning/phases/19-tailwind-button-theming-css-vars/19-UAT.md
  - .planning/phases/19-tailwind-button-theming-css-vars/19-VERIFICATION.md
autonomous: true
single_layer_justified: false
objective: "The shared browser chrome and core DOM helpers consume the new theme-token utility contract, and a real review fixture proves themed typography and shared frame styling on the shipped browser path."
must_haves:
  truths:
    - "ButtonFrame consumes variable-backed themed styling instead of fixed inline colors"
    - "Core DOM helpers align with className-first authoring without reviving a custom render DSL"
    - "A real browser review path proves typography- and frame-level theme differences visibly affect the shipped surface"
  artifacts:
    - packages/cli/src/render/button-frame.tsx
    - packages/cli/src/addon/api.ts
    - packages/cli/fixtures/phase-19/config.theme-typography.yml
    - .planning/phases/19-tailwind-button-theming-css-vars/19-VERIFICATION.md
  key_links:
    - "packages/cli/src/render/button-frame.tsx consumes the Phase 19 variable/utility layer"
    - "packages/cli/src/addon/api.ts exposes helper output that stays compatible with plain className-first authoring"
    - ".planning/phases/19-tailwind-button-theming-css-vars/19-UAT.md and 19-VERIFICATION.md map the visible proof to shipped code and fixtures"
---

# Plan 19-02: Shared Chrome, Helper Alignment, and Reviewable Proof

<objective>
Carry the Phase 19 theme bridge into the shared visible surface instead of leaving it as a one-button demo. This plan migrates `ButtonFrame` and core DOM helpers onto the new contract, then ships verification artifacts and a typography-aware browser review fixture that prove the theme bridge affects the real shipped path.
</objective>

## Tasks

<task id="19-02-01">
<title>Migrate shared browser chrome onto the variable-backed theme contract</title>
<files>
- packages/cli/src/render/button-frame.tsx
- packages/cli/src/render/button-frame.test.tsx
- packages/cli/src/builtin-addons/core-buttons/index.test.ts
- packages/cli/src/builtin-addons/date-time/index.ts
- packages/cli/src/builtin-addons/date-time/index.test.ts
</files>
<action>
Replace the hardcoded inline frame colors and fills in `button-frame.tsx` with the new variable-backed utility/theme contract so default framed buttons visibly follow the active Sireno theme. Extend the proof to at least one additional shipped browser-rendered surface such as a date/time button so the contract is not isolated to the first tracer bullet.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/button-frame.test.tsx src/builtin-addons/core-buttons/index.test.ts src/builtin-addons/date-time/index.test.ts`.
</verify>
<done>
The shared browser frame and more than one shipped button visibly consume the new theme-token utility contract.
</done>
</task>

<task id="19-02-02">
<title>Align core DOM helpers and ship reviewable typography verification</title>
<files>
- packages/cli/src/addon/api.ts
- packages/cli/src/index.ts
- packages/cli/fixtures/phase-19/config.theme-typography.yml
- .planning/phases/19-tailwind-button-theming-css-vars/19-UAT.md
- .planning/phases/19-tailwind-button-theming-css-vars/19-VERIFICATION.md
</files>
<action>
Adjust the core DOM helper surface so helper-generated content uses stable themed classes while keeping plain `className` authoring as the primary model. Add a committed browser review fixture and final verification artifact that make typography-role differences and shared frame theming observable on the real browser-rendered path, explicitly guarding against the earlier bug where theme typography changes were not visibly reviewable.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/dom-host.test.tsx src/render/button-frame.test.tsx src/builtin-addons/date-time/index.test.ts` and confirm `grep -n "theme\|typography\|text-primary\|buttonFrame" .planning/phases/19-tailwind-button-theming-css-vars/19-UAT.md .planning/phases/19-tailwind-button-theming-css-vars/19-VERIFICATION.md`.
</verify>
<done>
Phase 19 ends with a reviewable, documented browser-path proof that themed utilities and typography roles visibly affect the shared shipped surface without expanding into a full Tailwind clone.
</done>
</task>
