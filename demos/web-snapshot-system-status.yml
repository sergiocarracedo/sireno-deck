name: System Status — Web Snapshot
buttons:
  - position: 7
    type: "system-status:system-status"
    config:
      pages:
        - type: kpis
          metrics:
            - cpu
            - ram
