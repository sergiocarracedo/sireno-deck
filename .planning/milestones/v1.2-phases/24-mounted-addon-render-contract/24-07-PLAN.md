---
wave: 6
depends_on:
  - 24-06-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/core/schemas.ts
  - packages/cli/src/config/loader.test.ts
  - packages/cli/src/cli/commands/start.test.ts
  - .planning/phases/24-mounted-addon-render-contract/24-UAT.md
autonomous: true
single_layer_justified: false
objective: "Config-expanded addon/theme asset references stay rewriteable for the emulator path instead of being baked into `file://` URLs before the mounted DOM asset resolver can run."
must_haves:
  truths:
    - "Config/deck expansion no longer bakes shipped addon/theme icon references into `file://` URLs when those values still need environment-specific browser-path rewriting later."
    - "The emulator page can receive the original addon/theme asset reference or another rewriteable form so `start.ts` can serve browser-loadable HTTP asset URLs for generated emoji-selector deck buttons."
    - "Focused config/emulator regression coverage proves the Favorites emoji deck icon no longer leaks `file://...favorites.svg` into the HTTP-served emulator page."
    - "Phase 24 rerun UAT evidence remains linked to this closure plan."
  artifacts:
    - packages/cli/src/core/schemas.ts
    - packages/cli/src/config/loader.test.ts
    - packages/cli/src/cli/commands/start.test.ts
    - .planning/phases/24-mounted-addon-render-contract/24-UAT.md
  key_links:
    - "Config validation must still reject unknown addon/theme asset references without prematurely destroying the original reference needed by the emulator/browser seam."
    - "The mounted DOM asset resolver in `packages/cli/src/addon/api.ts` and emulator asset route in `packages/cli/src/cli/commands/start.ts` only help if generated button config still carries a rewriteable asset value into render time."
---

# Plan 24-07: Stop Config-Time Asset Rewrites From Bypassing Emulator Serving

<objective>
Close the rerun UAT image gap at the upstream seam that still leaks `file://` URLs into the emulator page. The current Wave 5 fix only helps when render-time DOM asset resolution still sees `addon://` / `builtin://` references, but config/deck expansion in `packages/cli/src/core/schemas.ts` is still converting some generated button icons into `file://...` too early. This slice keeps asset validation honest while preserving a rewriteable asset reference for the emulator path.
</objective>

## Tasks

<task id="24-07-01">
<title>Preserve rewriteable asset references through config validation for emulator-facing button icons</title>
<files>
- packages/cli/src/core/schemas.ts
- packages/cli/src/cli/commands/start.ts
</files>
<action>
Refactor the config asset-resolution path so generated button/deck icon values that need later browser-environment rewriting do not get baked into `file://` URLs during `validateConfig()`. Keep unknown asset references failing early and preserve the capture path behavior, but ensure shipped emoji-selector deck buttons reach the mounted DOM render seam with an asset value that `packages/cli/src/cli/commands/start.ts` can still route through `/__sireno/assets?ref=...` on the emulator page.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/config/loader.test.ts src/cli/commands/start.test.ts` and confirm the shipped emoji favorites icon no longer leaks `file://` into the HTTP-served emulator deck HTML.
</verify>
<done>
Opening the emulator and navigating into the shipped emoji selector no longer shows a `file://...favorites.svg` URL for the Favorites icon.
</done>
</task>

<task id="24-07-02">
<title>Lock the config-time asset fix with regression coverage and rerun-UAT linkage</title>
<files>
- packages/cli/src/config/loader.test.ts
- packages/cli/src/cli/commands/start.test.ts
- .planning/phases/24-mounted-addon-render-contract/24-UAT.md
</files>
<action>
Add or update focused regression coverage so the config loader still validates registered addon/theme assets honestly while the emulator/start path proves a generated emoji deck icon survives end-to-end without becoming a broken `file://` browser URL. Update `24-UAT.md` so the rerun image gap points at this exact plan file while preserving the user's report verbatim.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/config/loader.test.ts src/cli/commands/start.test.ts` and `rg -n "24-07-PLAN.md|favorites.svg|file:///works/opensource/sireno-deck/packages/cli/src/builtin-addons/emoji-selector/assets/favorites.svg|root_cause" .planning/phases/24-mounted-addon-render-contract/24-UAT.md`.
</verify>
<done>
The rerun image regression is pinned in tests, and the rerun UAT artifact points the next execution pass at this exact closure plan.
</done>
</task>
