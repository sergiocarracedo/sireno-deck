---
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - packages/cli/src/config/schemas.ts
  - packages/cli/src/cli/commands/run.ts
  - packages/cli/src/cli/commands/addon-decks.ts
  - packages/cli/src/config/__tests__/schemas.test.ts
  - packages/cli/src/cli/commands/__tests__/addon-decks.test.ts
  - .planning/ROADMAP.md
autonomous: true
single_layer_justified: false
objective: >
  Remove all quick-006 leftovers from the runtime and tests. After this
  plan, top-level `overlay:` is gone from the schema (strict rejection),
  the `materializeDeckFromConfig` helper is gone, the overlay-first
  iteration order in `buildRuntime` is reverted, and the quick-006
  describe-block tests are deleted. Phase 11 success criteria 5 + 7
  satisfied.
---

# Phase 11 Plan 03 — Revert quick-006 + cleanup

## Goal

After this plan, the top-level `overlay:` key from quick-006 is fully removed. Any leftover files / symbols / tests are cleaned up so the codebase has no dead code paths.

## Task 3.1 — Drop quick-006 schemas

<files>
- packages/cli/src/config/schemas.ts
</files>

<action>
1. Delete `AddonOverlayOverrideSchema`, `AddonOverlayDeckOverridesSchema`, `OverlayDeckEntrySchema`, `OverlayConfigSchema` exports.
2. Remove the `overlay:` field from `RawConfigSchema` (added in quick-006).
3. Verify no other source file imports these symbols (`pnpm rg "OverlayConfigSchema|AddonOverlayOverride|OverlayDeckEntry|AddonOverlayDeckOverrides" packages/cli/src`).

</action>

<verify>
`pnpm rg` returns 0 matches in source files (excluding this commit's deleted references).
</verify>

<done>
No symbols from quick-006 schemas remain. `RawConfigSchema.strict()` will reject top-level `overlay:` as an unknown key.
</done>

## Task 3.2 — Drop `materializeDeckFromConfig` helper and overlay-first ordering in `buildRuntime`

<files>
- packages/cli/src/cli/commands/run.ts
</files>

<action>
1. Delete the `materializeDeckFromConfig` function (added in quick-006).
2. Restore the inline `Object.entries(config.decks).flatMap(...)` loop in `buildRuntime`.
3. Restore `effectiveDecks` to the pre-quick-006 logic: `decks.length > 0 ? decks : [{ id: "main", ... }]`. No more `[...userOverlayDecks, ...decks]` prepend.
4. Remove the `userOverlayDecks` array and `addonOverrideMap` Map build.
5. Pass only the legacy parameters to `materializeAddonDecks` (no `addonOverrides`).

</action>

<verify>
`pnpm rg "materializeDeckFromConfig|userOverlayDecks|addonOverrideMap" packages/cli/src` returns 0 matches.
</verify>

<done>
`buildRuntime` no longer references quick-006 helpers; iteration order is back to legacy.
</done>

## Task 3.3 — Drop `addonOverrides` parameter from `materializeAddonDecks`

<files>
- packages/cli/src/cli/commands/addon-decks.ts
</files>

<action>
1. Remove the `addonOverrides?: ReadonlyMap<string, AddonOverlayOverride>` parameter from `materializeAddonDecks`.
2. Remove the `AddonOverlayOverride` interface export (only used by the parameter).
3. Remove the per-deck override-apply block in `materializeAddonDecks` (quick-006's override merge on top of `gdeck`).

</action>

<verify>
`pnpm rg "AddonOverlayOverride|addonOverrides" packages/cli/src` returns 0 matches.
</verify>

<done>
`materializeAddonDecks` signature is back to pre-quick-006: `(registry, userDecks, logger, keyCount, lockButtons?, addonConfigOverrides?, addonDeckOverrides?)` (the last two from 11-02 only).
</done>

## Task 3.4 — Delete quick-006 tests

<files>
- packages/cli/src/config/__tests__/schemas.test.ts
- packages/cli/src/cli/commands/__tests__/addon-decks.test.ts
</files>

<action>
1. Remove the entire `describe("OverlayConfigSchema", ...)` block from schemas.test.ts (5 tests).
2. Remove the entire `describe("materializeAddonDecks with addonOverrides", ...)` block from addon-decks.test.ts (3 tests).
3. Remove now-unused imports (`OverlayConfigSchema` from schemas.test.ts; `AddonOverlayOverride` from addon-decks.test.ts).

</action>

<verify>
`pnpm rg "OverlayConfigSchema|AddonOverlayOverride" packages/cli/src` returns 0 matches in test files.
</verify>

<done>
8 quick-006 tests deleted; no remaining references to the deleted symbols.
</done>

## Task 3.5 — Update ROADMAP Phase 11 success criteria checkboxes

<files>
- .planning/ROADMAP.md
</files>

<action>
1. Mark success criteria 5 (`Old {type, createDecks} manifest shape is rejected`) and 6 (`Top-level overlay: key is gone`) as `[x]`.
2. Mark success criterion 7 (`All existing tests pass; new tests cover ...`) as `[x]`.

</action>

<verify>
ROADMAP.md Phase 11 Success Criteria section shows the relevant boxes as `[x]`.
</verify>

<done>
Phase 11 success criteria reflected accurately in ROADMAP.
</done>

## Must Haves

- **truths:**
  - Top-level `overlay:` is rejected by `RawConfigSchema.strict()` as an unknown key
  - No leftover `materializeDeckFromConfig`, `userOverlayDecks`, `addonOverrideMap`, or `OverlayConfigSchema` references
  - All Phase 11 success criteria checked off in ROADMAP
- **artifacts:**
  - 8 quick-006 tests deleted
- **key_links:**
  - 11-02's `addons[i].config` schema + per-deck overrides still work (no test breaks)
  - 11-01's manifest array + addon migrations still work (no test breaks)