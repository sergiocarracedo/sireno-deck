---
wave: 2
depends_on:
  - 46-01-PLAN.md
files_modified:
  - packages/cli/src/builtin-addons/emoji-selector/index.test.ts
  - packages/cli/src/builtin-addons/emoji-selector/support.tsx
  - packages/cli/src/builtin-addons/emoji-selector/index.ts
autonomous: true
single_layer_justified: false
objective: "Cover edge cases in the emoji-selector pagination: exact-fit categories, single-page categories, empty categories, favorites integration with pagination, and prev/next button boundaries (no next on last page, no prev on page 1). Verify that the rendered nav-button output is correct."
---

## Tasks

### 46-02-01: Add edge-case test: exact emoji count matches page size

- **file**: `packages/cli/src/builtin-addons/emoji-selector/index.test.ts`
- **action**: Test that a category with exactly EMOJI_PAGE_SIZE emojis produces exactly one page deck (no pagination). Verify the deck has only emoji entry buttons (no nav buttons).
- **verify**: Test runs green
- **done**: Edge case covered.

### 46-02-02: Add edge-case test: emoji count is one more than page size

- **file**: `packages/cli/src/builtin-addons/emoji-selector/index.test.ts`
- **action**: Test that EMOJI_PAGE_SIZE + 1 emojis produces 2 pages. Page 1 has EMOJI_PAGE_SIZE emojis, page 2 has 1 emoji with prev button at position 0. Page 2 deck has no next button (it's the last page).
- **verify**: Test runs green
- **done**: Edge case covered.

### 46-02-03: Add edge-case test: no favorites configured

- **file**: `packages/cli/src/builtin-addons/emoji-selector/index.test.ts`
- **action**: Test that when `favorites: []` (empty), no favorites deck is created and category navigation deck still works. This ensures the favorites-first behavior still works with paginated categories.
- **verify**: Test runs green
- **done**: Edge case covered.

### 46-02-04: Add edge-case test: nav button boundaries

- **file**: `packages/cli/src/builtin-addons/emoji-selector/index.test.ts`
- **action**: For a 3-page category:
  1. Page 1 deck: assert no prev/next buttons, assert no emoji entry button at position keyCount-1 (system back slot)
  2. Page 2 deck: assert prev button at position 0 with target_deck pointing to page 1, assert next button at position keyCount-1 with target_deck pointing to page 3
  3. Page 3 deck: assert prev button at position 0 with target_deck pointing to page 2, assert NO next button (system back slot is empty)
- **verify**: Test runs green
- **done**: Boundary behavior verified.

### 46-02-05: Fix issues found by tests

- **file**: `packages/cli/src/builtin-addons/emoji-selector/support.tsx` and/or `index.ts`
- **action**: Run the full test suite. If any edge-case tests fail, fix the implementation — likely the pagination logic in `paginateEmojis` or nav button placement in `createDecks`.
- **verify**: `cd packages/cli && npx vitest run src/builtin-addons/emoji-selector/index.test.ts --reporter=verbose 2>&1 | tail -20` — all tests pass
- **done**: Implementation handles edge cases.

### 46-02-06: Run full addon test suite

- **file**: N/A (verification only)
- **action**: Run the full test suite to ensure no regressions:
  ```bash
  cd packages/cli && npx vitest run --reporter=verbose 2>&1 | tail -40
  ```
  Fix any regressions found.
- **verify**: All tests pass (0 failures, 0 errors)
- **done**: No regressions.

## Must Haves

- [ ] Test: exact-fit category (EMOJI_PAGE_SIZE emojis) produces 1 page without nav buttons
- [ ] Test: EMOJI_PAGE_SIZE+1 emojis produces 2 pages with correct nav button placement
- [ ] Test: empty favorites array still works with paginated categories
- [ ] Test: 3-page category has prev on page 2+3, next on page 2 only, no prev/next on page 1
- [ ] Test: last page deck leaves keyCount-1 slot empty (system back injection)
- [ ] All existing tests still pass
