---
wave: 4
depends_on:
  - 31-03-PLAN.md
gap_closure: true
files_modified:
  - packages/cli/src/cli/commands/start.ts
  - packages/cli/src/cli/commands/start.test.ts
  - README.md
  - .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md
  - .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-VERIFICATION.md
autonomous: true
single_layer_justified: false
objective: "Once the shared watch-loop blocker is closed, the bare `cli:dev` default-start path fails only on truthful startup errors, and the UAT/verification surfaces describe the repaired runtime contract that users can actually observe."
must_haves:
  truths:
    - "`startDaemon()` cleanup no longer throws `TypeError: Cannot read properties of undefined (reading 'catch')` when `sessionMonitor.stop()` is synchronous or returns `undefined`."
    - "Focused startup regression coverage proves the bare raw-source default-start seam can fail or stop cleanly without the cleanup-path type error that Phase 31 diagnosis exposed."
    - "Phase 31 UAT and verification artifacts preserve the diagnosed blocker history, record the bare-path cleanup defect as a separate downstream issue, and point rerun at `31-03-PLAN.md` and `31-04-PLAN.md` instead of claiming the README was stale."
    - "README only describes the repaired `cli:dev` contract the user can actually observe after the runtime seam is fixed."
  artifacts:
    - packages/cli/src/cli/commands/start.ts
    - packages/cli/src/cli/commands/start.test.ts
    - README.md
    - .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md
    - .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-VERIFICATION.md
  key_links:
    - "`packages/cli/src/system/session-monitor.ts` truthfully allows `stop()` to return `void`, so `start.ts` cleanup must tolerate that contract."
    - "After the shared watch-loop bug is fixed, the Phase 31 docs and verification surfaces need to reflect the real rerun truth rather than the original misleading no-output symptom."
---

# Plan 31-04: Harden Bare Startup Cleanup And Re-Sync Verification Truth

<objective>
Close the second defect that diagnosis exposed after the watch loop was peeled back: the bare default-start path still crashes in cleanup because `startDaemon()` assumes `sessionMonitor.stop()` is thenable even though the session monitor contract explicitly allows `void`. This slice hardens that cleanup path with focused regression coverage, then updates the Phase 31 UAT and verification surfaces so rerun guidance stays truthful about the shared watch-loop fix and the downstream bare-start cleanup fix.
</objective>

## Tasks

<task id="31-04-01">
<title>Make bare default-start cleanup tolerate synchronous session-monitor stop behavior</title>
<files>
- packages/cli/src/cli/commands/start.ts
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Update the `startDaemon()` error-cleanup path in `packages/cli/src/cli/commands/start.ts` so it no longer chains `.catch(...)` directly on `sessionMonitor?.stop()` and instead handles the session-monitor contract honestly when `stop()` returns either `Promise<void>` or `void`. Keep the change surgical to the existing cleanup boundary; do not redesign startup, session monitoring, or the repaired `cli:dev` launcher. Add a focused regression in `packages/cli/src/cli/commands/start.test.ts` that exercises the bare raw-source default-start path with a synchronous `sessionMonitor.stop()` seam and proves the cleanup path no longer throws the `reading 'catch'` type error surfaced during diagnosis.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts -t "documents the workspace-root cli:dev script as the full-process raw-source restart seam|synchronous session monitor stop"`.
</verify>
<done>
The bare default-start path no longer crashes in cleanup because the startup seam now honors the actual `SessionMonitor.stop()` contract.
</done>
</task>

<task id="31-04-02">
<title>Re-sync README, UAT, and verification artifacts with the diagnosed Phase 31 rerun truth</title>
<files>
- README.md
- .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md
- .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-VERIFICATION.md
</files>
<action>
After the runtime fixes are in place, update the Phase 31 UAT and verification artifacts so they preserve the original user reports, record the shared watch-loop root cause plus the downstream bare-start cleanup defect, and make `31-03-PLAN.md` and `31-04-PLAN.md` the explicit rerun path. Tighten `README.md` only if needed so it continues to describe the repaired bare-plus-forwarded `cli:dev` contract the user can now observe in practice, without implying that the earlier issue was merely documentation drift.
</action>
<verify>
Run `rg -n "31-03-PLAN.md|31-04-PLAN.md|theme runtime|sessionMonitor|reading 'catch'|cli:dev" .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-UAT.md .planning/phases/31-cli-dev-watch-mode-argument-forwarding/31-VERIFICATION.md README.md`.
</verify>
<done>
Phase 31's user-facing docs and verification history describe the fixed runtime truth, preserve the blocker history, and point rerun directly at the gap-closure plans that closed it.
</done>
</task>
