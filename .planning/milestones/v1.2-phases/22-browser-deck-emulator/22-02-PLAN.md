---
wave: 2
depends_on:
  - 22-01
files_modified:
  - packages/cli/src/cli/commands/start.ts
  - packages/cli/src/cli/commands/start.test.ts
  - packages/cli/src/deck/runtime.ts
  - packages/cli/src/deck/runtime.test.ts
  - packages/cli/src/render/dom-host.tsx
  - packages/cli/src/render/browser-renderer.ts
  - packages/cli/fixtures/phase-22/
autonomous: true
single_layer_justified: false
objective: "Users can press and release buttons in the browser emulator and get the same runtime behavior plus visible pressed-state feedback as the real deck path."
---

# Plan 22-02

## Objective

Make browser interaction honest: mouse input on the emulator page should drive the real runtime through `down` / `up` events, actions should execute normally, and the emulator should visibly reflect pressed/hold state transitions instead of collapsing interaction into click-only behavior.

## Tasks

<task>
<file>packages/cli/src/cli/commands/start.ts</file>
<action>Add the browser-to-runtime event bridge for emulator mode so mouse press/release events from the local page become `StreamDeckKeyEvent` messages on the virtual lifecycle instead of bypassing the runtime.</action>
<verify>Command-level tests can drive emulator input and observe runtime-side button activation through the same subscription/event seam used by hardware mode.</verify>
<done>The emulator page no longer acts as a passive preview; it is a real input surface.</done>
</task>

<task>
<file>packages/cli/src/deck/runtime.ts</file>
<action>Expose or preserve whatever runtime-side state/output is needed for the emulator page to reflect honest pressed and hold visuals while keeping action execution and button lifecycle behavior inside the runtime boundary.</action>
<verify>Runtime-focused tests prove `down` and `up` from the virtual seam produce the same action/tap behavior expected from the real deck path and expose visible interaction-state changes for the browser UI.</verify>
<done>Pressed-state behavior is runtime-owned and testable instead of being faked in the browser page.</done>
</task>

<task>
<file>packages/cli/src/render/dom-host.tsx</file>
<action>Extend the browser-host/emulator surface enough to reflect pressed or hold state visibly on the virtual deck without creating a second authoring model or abandoning the existing `buttonFrame`/DOM-host path.</action>
<verify>Focused DOM-host or browser-renderer tests prove the emulator surface can display non-idle interaction states on the affected virtual key.</verify>
<done>The user can see button press feedback while interacting with the emulator.</done>
</task>

<task>
<file>packages/cli/src/deck/runtime.test.ts</file>
<action>Add targeted tests for virtual `down` / `up` behavior, ensuring emulator-driven input preserves tap semantics, action execution, and any visible interaction-state metadata needed by the browser page.</action>
<verify>`vitest packages/cli/src/deck/runtime.test.ts` passes with emulator-input coverage.</verify>
<done>The input contract is locked at the runtime seam.</done>
</task>

<task>
<file>packages/cli/fixtures/phase-22/</file>
<action>Add or extend a review fixture that makes the pressed-state transition visibly obvious during browser interaction, so this slice can be judged as more than "click triggered something." </action>
<verify>The committed fixture/review path demonstrates both browser interaction and visible pressed feedback.</verify>
<done>Plan 22-02 is demoable as "press a virtual key and observe real runtime behavior plus visual feedback."</done>
</task>

## Must Haves

- Browser mouse input maps to real `down` / `up` runtime events.
- Emulator interactions execute the same real actions/status behavior as the normal runtime path.
- The virtual deck visibly reflects pressed/hold state changes.
- Focused runtime and command tests cover the emulator input contract.
