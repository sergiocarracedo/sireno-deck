---
wave: 4
depends_on:
  - 18-03-PLAN.md
files_modified:
  - packages/cli/src/render/browser-renderer.ts
  - packages/cli/src/render/browser-renderer.test.ts
  - packages/cli/src/addon/api.ts
  - packages/cli/src/render/dom-host.tsx
  - packages/cli/src/render/dom-host.test.tsx
  - packages/cli/src/builtin-addons/core-buttons/index.ts
  - packages/cli/src/builtin-addons/core-buttons/index.test.ts
  - packages/cli/fixtures/phase-18/config.media-sampling.yml
  - .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-UAT.md
  - .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-VERIFICATION.md
autonomous: true
single_layer_justified: false
objective: "A reviewer can run a real-device Phase 18 fixture that shows bounded GIF/video-style media sampling through the browser/react-dom deck surface and confirms the TSX-authored renderer has replaced the old SVG path for shipped buttons."
must_haves:
  truths:
    - "The browser renderer supports bounded sampled media capture for TSX-authored buttons without promising continuous playback"
    - "A committed Phase 18 media fixture makes sampled rich-media behavior reviewable on the real device"
    - "Phase 18 verification artifacts explicitly confirm the old SVG render path is no longer the shipped button rendering path for migrated buttons"
  artifacts:
    - packages/cli/fixtures/phase-18/config.media-sampling.yml
    - .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-UAT.md
    - .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-VERIFICATION.md
  key_links:
    - "packages/cli/src/render/browser-renderer.ts exposes bounded sampled media capture behavior"
    - ".planning/phases/18-react-dom-based-renderer-with-htmlcss/18-UAT.md references both live browser-rendered buttons and media-sampling fixtures"
---

# Plan 18-04: Bounded Media Sampling and Phase Verification

<objective>
Finish Phase 18 with the richer-media proof that motivated the browser renderer in the first place. This plan adds bounded GIF/video-style sampling behavior to the browser renderer, creates the review fixture and UAT script for real-device verification, and updates the verification artifact so the phase can be judged honestly against the browser-backed react-dom contract.
</objective>

## Tasks

<task id="18-04-01">
<title>Add bounded sampled media capture for TSX-authored buttons</title>
<files>
- packages/cli/src/render/browser-renderer.ts
- packages/cli/src/render/browser-renderer.test.ts
- packages/cli/src/addon/api.ts
- packages/cli/src/render/dom-host.tsx
- packages/cli/src/render/dom-host.test.tsx
</files>
<action>
Extend the browser renderer and DOM host so a button can declare media-oriented config (for example `src`, `loop`, or equivalent button-owned config) and participate in bounded screenshot sampling instead of static first-frame rendering. Keep the behavior honest: sampled snapshots only, no promise of continuous playback. Add focused renderer and host tests that prove sampled media updates obey the bounded queue/coalescing rules and remain expressible through normal React TSX authoring.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/render/browser-renderer.test.ts src/render/dom-host.test.tsx`.
</verify>
<done>
The renderer and authoring contract can express sampled media buttons and prove that the captures stay bounded.
</done>
</task>

<task id="18-04-02">
<title>Create the rich-media review fixture and final Phase 18 UAT flow</title>
<files>
- packages/cli/fixtures/phase-18/config.media-sampling.yml
- .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-UAT.md
</files>
<action>
Create the Phase 18 media fixture that reviewers can run on the real device to inspect sampled GIF/video-style behavior through the browser-backed renderer. Update `18-UAT.md` so it covers both the earlier browser-rendered/live-button fixtures and the new media-sampling review, with explicit pass/fail conditions around bounded sampling and visible browser-rendered output.
</action>
<verify>
Run `grep -n "media\|video\|sampling\|browser-backed" .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-UAT.md && ls packages/cli/fixtures/phase-18/config.media-sampling.yml`.
</verify>
<done>
Phase 18 has a real-device UAT script and fixture for bounded rich-media sampling.
</done>
</task>

<task id="18-04-03">
<title>Write the Phase 18 verification artifact for the browser/react-dom renderer replacement</title>
<files>
- .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-VERIFICATION.md
- packages/cli/src/builtin-addons/core-buttons/index.ts
- packages/cli/src/builtin-addons/core-buttons/index.test.ts
</files>
<action>
Add the final verification artifact for Phase 18, mapping the phase must-haves to the browser renderer, TSX authoring contract, `buttonFrame`, live-button migration, and bounded media-sampling review path. Ensure the shipped builtin surface no longer depends on the old SVG-render helper path for buttons covered by the phase, and update or add tests as needed to prove the final migrated state.
</action>
<verify>
Run `grep -n "buttonFrame\|browser\|media\|react-dom\|TSX" .planning/phases/18-react-dom-based-renderer-with-htmlcss/18-VERIFICATION.md && pnpm --filter sireno-deck-cli exec vitest run src/builtin-addons/core-buttons/index.test.ts`.
</verify>
<done>
Phase 18 has a verification artifact that can be used by `verify-work` to judge the browser/react-dom renderer replacement honestly.
</done>
</task>
