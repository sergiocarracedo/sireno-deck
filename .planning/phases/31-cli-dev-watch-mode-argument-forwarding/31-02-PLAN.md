---
wave: 2
depends_on:
  - 31-01-PLAN.md
files_modified:
  - packages/cli/src/cli/commands/start.test.ts
  - README.md
autonomous: true
single_layer_justified: false
objective: "Developers can trust the restored `cli:dev` contract because the README and the shipped regression seam both describe the same default-start and forwarded-args behavior."
must_haves:
  truths:
    - "`packages/cli/src/cli/commands/start.test.ts` pins the restored root-script contract, including the default `start --config config.yml` path and an explicit forwarded-args path such as `emulate --port 8912`."
    - "The regression seam still proves `cli:dev` is the full-process raw-source restart seam and not the `tsdown --watch` bundler path."
    - "`README.md` documents both bare `pnpm run cli:dev` usage and an explicit forwarded-args example without blurring the distinction from the narrower in-process config-owned reload seam."
  artifacts:
    - packages/cli/src/cli/commands/start.test.ts
    - README.md
  key_links:
    - "`packages/cli/src/cli/commands/start.test.ts` is the existing repo-owned guardrail that already caught this contract drift."
    - "`README.md` is the user-facing explanation of the seam and must match the live script contract exactly."
---

# Plan 31-02: Re-Sync Regression And README Truth

<objective>
Close the loop around the repaired runtime seam so this bug does not reappear through silent drift. Once the launcher is fixed, the regression seam and the README need to describe the exact same contract: bare `cli:dev` means `start --config config.yml`, explicit forwarded args mean the real CLI subcommand path, and none of that changes the narrower in-process config-owned reload seam.
</objective>

## Tasks

<task id="31-02-01">
<title>Pin the restored `cli:dev` contract in the shipped regression seam</title>
<files>
- packages/cli/src/cli/commands/start.test.ts
</files>
<action>
Update the existing `documents the workspace-root cli:dev script as the full-process raw-source restart seam` regression area in `packages/cli/src/cli/commands/start.test.ts` so it continues to assert the include globs and default `start --config config.yml` path, and also asserts the restored forwarded-args behavior at the string or helper-contract level chosen in Plan 31-01. Keep the test anchored on the real root-script contract and preserve the explicit guard that `cli:dev` is not `tsdown --watch`.
</action>
<verify>
Run `pnpm --filter sireno-deck-cli exec vitest run src/cli/commands/start.test.ts -t "documents the workspace-root cli:dev script as the full-process raw-source restart seam"`.
</verify>
<done>
The existing shipped regression seam now guards both halves of the restored contract: default-start behavior and truthful forwarded args.
</done>
</task>

<must_haves>
- Do not weaken the existing guard that `cli:dev` is distinct from `tsdown --watch`.
- Keep the test assertions anchored on the real root script contract, not a private implementation detail only the new launcher knows about.
</must_haves>

<task id="31-02-02">
<title>Update README so the documented watch seam matches the repaired runtime and regression contracts</title>
<files>
- README.md
</files>
<action>
Update the `## Development Refresh` section in `README.md` so it still describes `pnpm run cli:dev` as the full-process restart seam for raw source edits, but now also documents the explicit forwarded-args path with a concrete example such as `pnpm run cli:dev emulate --port 8912`. Preserve the existing explanation that the daemon's in-process config-owned reload path is a different, narrower seam.
</action>
<verify>
Run `grep -n 'cli:dev\|emulate --port 8912\|full-process restart seam\|in-process config-owned reload seam' README.md`.
</verify>
<done>
The user-facing docs, regression seam, and repaired runtime seam all describe the same `cli:dev` contract.
</done>
</task>
