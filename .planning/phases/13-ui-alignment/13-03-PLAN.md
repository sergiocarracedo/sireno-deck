---
phase: 13-ui-alignment
plan: 03
wave: 2
depends_on: [13-01, 13-02]
autonomous: false
objective: |
  Update all 7 user-facing addon frontend.tsx files to use the theme's
  components (Text, Icon, Chip, TapIndicator) and surfaces (IconLabel,
  Bars, LabelValueList, SplitAction) instead of hand-rolled spans.
files_modified:
  - packages/cli/src/builtin-addons/date-time/frontend.tsx
  - packages/cli/src/builtin-addons/weather/frontend.tsx
  - packages/cli/src/builtin-addons/system-status/frontend.tsx
  - packages/cli/src/builtin-addons/media-player/frontend.tsx
  - packages/cli/src/builtin-addons/value-display/frontend.tsx
  - packages/cli/src/builtin-addons/brightness/frontend.tsx
  - packages/cli/src/builtin-addons/emoji-selector/frontend.tsx
---

# Phase 13 Plan 03: addon frontend migration to legacy surfaces

## Tasks

### Task 1: date-time frontend

File: `builtin-addons/date-time/frontend.tsx`
- CoreTime: uses `<Text>` (already done in quick-008). Verify rich markup `<2xl>HH</2xl><dim>.</dim>mm` renders correctly.
- CoreDate: uses `<Text mono>` (already done).
- CoreClock: uses `<Text mono>` (already done).
- CoreAnalogClock: keep SVG. Add `data-sireno-ui-text` like legacy.
- CoreDateTime: uses `<Text mono>` with `expandTokens()` (done).
- CoreLockedTimeTile: uses `<Text mono 2xl>`.

### Task 2: weather frontend

File: `builtin-addons/weather/frontend.tsx`
- Replace `<span>` with `<Text size="sm" tone="fg">` for temperature.
- Replace `<span className="text-muted">` with `<Text size="xs" tone="muted">` for description/wind.
- Replace name `<span>` with `<Text size="xs" tone="accent">`.

### Task 3: system-status frontend

File: `builtin-addons/system-status/frontend.tsx`
- Already uses bars layout similar to legacy `<Bars>` surface. Replace inner `<span>` elements with `<Text size="xs" tone="muted">` for labels and `<Text size="xs" tone="fg">` for values.

### Task 4: media-player frontend

File: `builtin-addons/media-player/frontend.tsx`
- Replace `<span>` elements with `<Text>` components.
- Volume bar: use `bg-bar` + `bg-accent` fill.

### Task 5: value-display frontend

File: `builtin-addons/value-display/frontend.tsx`
- Replace `<span>` elements with `<Text size="xs" tone="muted">` + `<Text size="xs" tone="fg">`.

### Task 6: brightness frontend

File: `builtin-addons/brightness/frontend.tsx`
- Replace `<span>` elements with `<Text>`.
- Bar uses `bg-bar` + `bg-accent`.

### Task 7: emoji-selector frontend

File: `builtin-addons/emoji-selector/frontend.tsx`
- Already minimal (just renders the emoji). Keep as-is.

### Task 8: Verify (checkpoint — user review)

- Run `pnpm dev start --emulator`.
- Open browser and inspect each button visually.
- User confirms each addon button looks correct.
- `pnpm test` passes.
