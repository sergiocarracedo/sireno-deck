---
wave: 3
depends_on:
  - 22-01
  - 22-02
files_modified:
  - packages/cli/src/cli/commands/start.ts
  - packages/cli/src/cli/commands/start.test.ts
  - packages/cli/src/device/stream-deck.ts
  - packages/cli/src/render/browser-renderer.ts
  - packages/cli/src/render/dom-host.tsx
  - packages/cli/fixtures/phase-22/
  - .planning/phases/22-browser-deck-emulator/
autonomous: true
single_layer_justified: false
objective: "Users can switch virtual device layouts in the emulator page, get a clean runtime restart for the new shape, and see an explicit emulator error when the chosen layout cannot represent the configured deck."
---

# Plan 22-03

## Objective

Finish the first-rollout emulator control surface by adding in-page device selection, restart-on-change behavior, and explicit emulator-specific failure states for unsupported deck/layout combinations so the emulator remains honest instead of clipping or guessing.

## Tasks

<task>
<file>packages/cli/src/render/browser-renderer.ts</file>
<action>Reuse the existing known key-count/layout model to define the emulator’s selectable virtual devices and expose whatever metadata the local page needs to label and size those options consistently.</action>
<verify>Renderer/layout tests prove the supported virtual device shapes remain explicit and stable.</verify>
<done>The emulator’s device selector is grounded in the same layout rules as the renderer.</done>
</task>

<task>
<file>packages/cli/src/cli/commands/start.ts</file>
<action>Add the device-switch control flow: when the user selects a new virtual device in the page, tear down the current virtual runtime cleanly and restart the emulator against the new key-count shape rather than mutating the old runtime in place.</action>
<verify>Focused command tests prove device changes trigger clean restart behavior and do not require physical hardware or stale in-place state mutation.</verify>
<done>Changing device shape is explicit, predictable, and restart-based.</done>
</task>

<task>
<file>packages/cli/src/cli/commands/start.ts</file>
<action>Add an emulator-specific error surface for layout mismatches where the selected virtual device cannot represent the configured deck, and ensure the browser page reports that state clearly instead of clipping hidden keys or silently auto-switching devices.</action>
<verify>Tests and fixture coverage prove undersized virtual layouts fail honestly with a browser-visible emulator error state.</verify>
<done>The emulator preserves the repo’s fail-honestly principle for unsupported layouts.</done>
</task>

<task>
<file>packages/cli/src/cli/commands/start.test.ts</file>
<action>Add focused tests for in-page device switching, runtime restart on change, and explicit error handling for undersized virtual devices.</action>
<verify>`vitest packages/cli/src/cli/commands/start.test.ts` passes with device-switching and error-state coverage.</verify>
<done>The selector/error-control slice is protected against regressions.</done>
</task>

<task>
<file>packages/cli/fixtures/phase-22/</file>
<action>Add a committed review fixture or UAT note showing both a successful layout switch and an honest emulator-specific layout failure, so the final Phase 22 review path proves the tool does not lie under device mismatch pressure.</action>
<verify>The committed review artifact demonstrates successful device switching plus explicit mismatch failure behavior on the browser path.</verify>
<done>Plan 22-03 is demoable as "switch virtual devices and get honest restart/error behavior."</done>
</task>

## Must Haves

- The emulator page exposes an explicit device selector based on supported virtual key-count shapes.
- Changing device shape restarts the emulator runtime cleanly.
- Unsupported deck/layout combinations surface a clear emulator-specific error state.
- Tests and review artifacts cover both successful switching and failure behavior.
